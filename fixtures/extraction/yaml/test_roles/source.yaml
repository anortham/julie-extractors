schemaVersion: 2.0.0
commandTests:
  - name: run command
    command: [echo, hello]
    setup: setup.sh
    teardown: teardown.sh
  - name: "quoted command"
    command: [echo, quoted]
  - name: 123
    command: [echo, numeric]
    metadata:
      name: nested name
      setup: nested.sh
outside:
  commandTests:
    - name: nested command tests
      setup: nested-setup.sh
setup: outside.sh
teardown: outside.sh
