return_identity <- function(return) {
  return
}

hex_fraction <- 0x1.1p1

else_prefix <- function() {
  if (TRUE) 1
  else_result <- 2
  else_result
}

raw_text <- r"(line one
line two)"

# comment boundary
after_comment <- hex_fraction
