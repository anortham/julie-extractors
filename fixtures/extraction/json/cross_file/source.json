{
  "_comment": "Phase 4d fixture: JSON $ref pointers are intra-document by JSON Schema spec; cross-document $refs are URL strings, not symbol references. This fixture exercises intra-document refs to lock the no-pending classification.",
  "definitions": {
    "local_helper": {
      "type": "string"
    }
  },
  "properties": {
    "entry": {
      "$ref": "#/definitions/local_helper"
    }
  }
}
