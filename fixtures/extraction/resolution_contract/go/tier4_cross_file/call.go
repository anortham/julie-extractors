// Tier 4 (unique-language-global) reference half for Go: a cross-file call
// deferred to a pending row, resolved against the unique produceWidget.
package sample

func consume() { produceWidget() }
