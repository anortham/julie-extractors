<?php

describe('billing totals', function () {
    beforeEach(function () {
    });

    afterEach(function () {
    });

    it('adds line items', function () {
        expect(1 + 1)->toBe(2);
    });

    test('applies a discount', function () {
        expect(true)->toBeTrue();
    });
});

$ordinary = new stdClass();
$ordinary->test('member calls stay ordinary', function () {
});
