# SDD progress — erlang-xml-language-support

Plan: docs/plans/2026-07-31-erlang-xml-language-support-plan.md
Branch: erlang-xml-language-support (worktree .worktrees/erlang-xml-language-support)
Scope note: entries below belong ONLY to this plan. Reports use the suffix
`task-N-erlang-xml-report.md` because `.razorback/sdd/` is tracked and holds files
from earlier, unrelated plans — do not treat those as this plan's progress.

- Task 1: complete (commit fd962a50, Lead inline review clean). Phase-0 gate PASS; XML entry point = tree_sitter_xml::LANGUAGE_XML (DTD is a separate grammar); tree-sitter-erlang ships no tags/locals queries (hand-written walking as planned).
- Task 2: complete (commit eb7cb130, Lead inline review clean). Erlang registered at 37, symbols tier honest (SYMBOLS_ONLY_CAPABILITIES). Plan mismatches recorded in task-2 report §7: migration-plan doc is the open-capability registry; negative fixture required at registration when target relationships=true; per-language parity guards force source_regions/marker/body.rs/operations_contract touches; complexity_metrics left as typed open gap (unowned). Task 3 prompt must carry §7.3 file list.
- Task 3: complete (commit 8df10321, Lead inline review clean). XML registered at 38, DATA_ONLY honest (target==actual). Cardinality: 4,000 anon elements → 3 symbols. 4th parity guard found: capability_matrix DOMAIN_LANGUAGES const. Matrix lacks a not_targeted status (open rows overstate intent) — surface in final report. Next: Batch A (T4 ∥ T5, parallel-lead-commit).
- Task 5: complete (parallel-lead-commit, Lead inline review clean, lead commit e8bf8a4). Oversized transition removes rows on update+scan; update status no_change→unsupported for oversized (RELEASE-NOTE + check Miller ExtractReportLog handling at pin bump). cli.md touched outside ownership (justified, accepted).
- Task 4: complete (parallel-lead-commit, Lead inline review clean, lead commit c8924b2). Erlang identifiers shipped, DATA_ONLY tier. Literals gap re-pointed at Task 8 by worker (accepted as documentation pointer only — Task 8 does NOT implement literals). Registry wiring + orphaned-const removal accepted as forced touches.
- Task 6: complete (commit 57395bb, Lead inline review clean). Relationships+pending shipped; erlang at PENDING_NO_TYPES_CAPABILITIES. Accepted deviations: behaviour edge is pending not resolved (.erl = one module/file); cross_file golden is single-source (fixture schema limit). Residual gap pointers (erlang literals T4§8.3, behaviour_declaration structural facts T6§8.3) point at Task 8 misleadingly — Task 10 must normalize them. Known recall limit: calls into -include'd headers emit nothing (CLI resolver territory).
- Task 7: complete (commit 32d8baf, Lead inline review clean). Erlang FULL (capabilities==target, gaps empty). Test roles centralized in test_detection.rs; two test_roles fixtures (EUnit + CT split — one module per file). Cross-language wart for ticket: convert_types_map hardcodes is_inferred:true for declared types. Migration-plan Task 13 boxes ticked truthfully.
- Task 9: complete (commit 6639f1c, Lead inline review clean). 10 xml pattern ids (document/namespace + xsd type/element/import + wsdl service/port/operation/message/binding); document-level facts keep cardinality golden at 1 fact. XML gap pointers normalized to migration-plan Task 14. Deferred by scope note: portType fact, targetNamespace key (cheap follow-ups if wanted).
- Task 8 fix round: user APPROVED building the iterative resync recovery (has_error trigger, 32 re-parse cap, column-0 resume candidates, doc-string negative controls). Dispatched to impl-task8 after Task 9 landed.
- Task 8: complete (commits 890db84 + fix 4cfe193, Lead inline review clean, 2 fix rounds). Corpus gate: 10 files, exact baseline, telemetry.erl 8/8 exports via bounded resync recovery (user-approved; 32-parse cap, column-0 resume, doc-string negatives). Diagnostics baseline 45/2 unchanged by design. Caveats recorded: walkable containment scope, column-0 assumption, no cap-hit diagnostic, recovery is erlang-only (parity note). xtask real-world wiring deferred to Task 10.
- Task 10: complete (commit e2d39c0, Lead inline review clean). 13/14 gates green; strict data-quality gate red with 4 quality-bar debts (erlang complexity_metrics/literals/structural_facts, xml literals) — escalated. USER DECISION 2026-08-01: close all four on this branch. Plan extended with Task 11 (erlang complexity + erlang literals + xml literals) and Task 12 (erlang structural facts + strict gate re-run to 0 debts). Branch-gate ledger entry deferred until Task 12's re-run at final HEAD.
- Task 11: complete (commit 5051380, Lead inline review clean). Strict gate now 1 debt (erlang.structural_facts, Task 12's). Erlang ERLANG_CONFIG complexity (container+arm+guard convention; clause dispatch NOT a decision; parameter_count NULL — no closed parameter node-kind set); erlang string call-arg literals under verbatim callee carrier; xml attribute-value literals via tag_attribute_carrier (element-text literals NOT captured — matches migration-plan Task 14 registry scope; possible follow-up). Corpus baseline untouched and green. Lead-verified strict output. Concerns logged for final report: erlang body_span is a pre-existing text-heuristic artefact (claimed in capabilities but wrong — decide before merge); multi-clause symbol-scope complexity covers clause 1 only; tag_attribute_carrier lowercases attribute names; scorecard's final JSON line reports quality_bar_debts:0 while text section says 1 (script display bug). Task 12 must re-run reference-resolution-coverage --write after golden regen.
