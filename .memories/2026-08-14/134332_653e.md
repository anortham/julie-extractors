---
id: checkpoint_653e434d
timestamp: 2026-08-14T13:43:32.497Z
tags:
  - bug-fix
  - store-import
  - no-change
  - idempotency
  - resolution
git:
  branch: feature/miller-performance-recovery-producer
  commit: ebe09b4c
  files:
    - crates/julie-extract-cli/src/store/executor.rs
    - crates/julie-extract-cli/src/store/import.rs
    - crates/julie-extract-cli/tests/store_import_contract.rs
summary: Repaired producer no-change imports
type: checkpoint
evidence:
  - "RED: focused store_import_contract test failed first on resolution state
    `unbound` for a reused exact manifest."
  - "GREEN: focused unchanged import test passed with one terminal, zero request
    chunks, preserved generation/hash/counts, and exact resolution."
  - "GREEN: serial store_import_contract 30/30; store_cli_contract 14/14;
    store_operations_contract 25/25."
  - "GREEN: cargo check -p julie-extract-cli --all-targets and cargo clippy -p
    julie-extract-cli --all-targets -- -D warnings."
  - Owned files pass direct rustfmt check; repository-wide cargo fmt is blocked
    only by a pre-existing unrelated formatting diff.
symbols:
  - StoreRequestExecutor::execute_quantum
  - StoreRequestExecutor::all_planned_versions_complete
  - execute_import
  - populate_current_resolution
next: Commit the owned source/tests and this checkpoint, then lead review/replay.
confidence: 5
---

## Repaired producer no-change imports

- **WHAT:** Added a fresh ordinary Import fast path for `next_chunk_index == 0` that checks every planned file with the existing `(path, content_hash, EXTRACTION_IDENTITY_EPOCH, requested level)` lookup, republishes/compares the full manifest, and emits the normal single terminal without chunk progress when all versions are complete.
- **WHY:** Faithful unchanged Full imports were paying one fenced transaction per empty chunk and ordinary import reports did not expose durable exact resolution state.
- **HOW:** The fast path is restricted to Import requests with zero progress and no durable failures; missing, incomplete, epoch-mismatched, Update, and resumed requests retain the existing scheduling path. Import reports now project `views` resolution fields, including exact-at matching, after durable projection.
- **IMPACT:** Unchanged Full imports preserve generation/hash/counts, avoid extraction and request chunks, retain exact resolution state, and continue invalidating resolution on real manifest changes.
