---
id: checkpoint_9f4a433e
timestamp: 2026-08-14T21:52:37.171Z
tags:
  - performance-recovery
  - task-8
  - maintenance
  - verification
git:
  branch: feature/miller-performance-recovery-producer
  commit: f2ebd21c
  files:
    - crates/julie-extract-artifact/tests/store_coordinator_contract.rs
    - crates/julie-extract-artifact/tests/store_maintenance_contract.rs
summary: WHAT
type: checkpoint
impact: Removes a test-only parallelism flake and unblocks exact-source Task 8
  Linux and Windows verification.
evidence:
  - "cargo test -p julie-extract-artifact --test store_maintenance_contract
    heartbeat -- --nocapture: 3/3 passed"
  - "cargo test -p julie-extract-artifact --test store_maintenance_contract:
    23/23 passed"
  - "cargo test --workspace --quiet: all targets passed; extractor 3238 passed /
    7 ignored"
  - serialized coordinator 65/65; resolution 29/29; maintenance 23/23; property
    1/1; performance 3/3
  - cargo fmt --all -- --check passed; strict workspace Clippy passed; git diff
    --check passed
symbols:
  - BlockingCapacity
  - short_maintenance_lease_heartbeats_intent_and_writer_during_apply
  - short_maintenance_lease_heartbeats_during_plan_validation
  - maintenance_heartbeat_fails_closed_after_lease_takeover
next: Commit this checkpoint with the two test files, then restore this exact
  Julie source into Miller and run Task 8 integration gates.
---

## WHAT

Stabilized the Task 7A maintenance-heartbeat contract tests for ordinary parallel workspace execution without changing production code.

- Reformatted one pre-existing coordinator assertion so the repository formatter gate is clean.
- Increased the three synthetic maintenance lease windows from 100 ms to 1 s.
- Replaced polling with one-shot `mpsc` entry signals and retained five-second failure backstops.
- Observed renewal for 3.5 s and takeover loss after 500 ms.

## WHY

The tests passed serialized but their 100 ms leases expired before acquisition under the normal parallel workspace suite. The first correction used `Instant` polling, which the test-tier guard correctly rejected as timing work in the default suite.

## HOW

The final test-only seam signals the exact transition into the blocked capacity window, removing scheduler-dependent polling while preserving the load-bearing lease-renewal and fail-closed takeover assertions. No production behavior, feature gate, assertion, or test category changed.

## IMPACT

Task 8's Linux producer branch gates are green on the exact Julie candidate. The Miller source restore can now use this worktree once the checkpoint and test changes are committed.
