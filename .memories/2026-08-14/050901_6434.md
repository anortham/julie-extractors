---
id: checkpoint_64340b9e
timestamp: 2026-08-14T05:09:01.357Z
tags:
  - performance-recovery
  - coordinator
  - claim-recovery
  - task-2
git:
  branch: feature/miller-performance-recovery-producer
  commit: 51c2977f
  files:
    - crates/julie-extract-artifact/tests/store_coordinator_contract.rs
summary: WHAT
type: learning
impact: Turns the stranded-claim diagnosis into a permanent regression without
  duplicating recovery code.
evidence:
  - "`cargo test -p julie-extract-artifact --test store_coordinator_contract`:
    65 passed, 0 failed"
  - "`git diff --check`: passed"
  - "Producer base: 51c2977f"
next: Commit the coordinator characterization on the local producer recovery
  branch; do not push or pin.
---

## WHAT

Added the exact dead-import incident characterization to `store_coordinator_contract.rs`: a claimed import with a stopped heartbeat, dead owner, and expired 5-second writer lease is requeued during takeover; the replacement fencing token advances and the old token cannot heartbeat.

## WHY

A real store was wedged by a claimed import owned by a dead CLI. The recovery plan needed proof of the complete field state before changing coordinator code.

## HOW

The test acquires at logical time 10, takes over at 5,011, verifies the queued/cleared claim, and checks stale-token rejection. The full coordinator contract passes unchanged in production code, proving this is an adoption/regression gap rather than a missing recovery algorithm.

## IMPACT

No producer production change is justified. The existing generic fenced takeover already repairs dead import claims; packaging/adoption and exit diagnostics remain the relevant concerns.
