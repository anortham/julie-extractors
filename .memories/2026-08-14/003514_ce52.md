---
id: checkpoint_ce524535
timestamp: 2026-08-14T00:35:14.823Z
tags:
  - release
  - 2.33.2
  - ci
  - clippy
  - windows
git:
  branch: main
  commit: 4b3797d6
  files:
    - crates/julie-extract-cli/tests/store_maintenance_performance.rs
summary: WHAT
type: incident
context: The first remote CI run for release-prep commit 4b3797d6 passed
  compatibility and Windows store-resolution jobs but failed Linux Fast Gates at
  strict Clippy.
impact: Removes the target-specific release gate failure without changing
  product behavior or Windows coverage.
evidence:
  - "CI run 31757438040: compatibility passed; Windows Capacity Store Probe
    passed; Fast Gates failed only at unused run_scale_worker"
  - "Linux focused maintenance tests: 3/3"
  - "Linux strict package Clippy: passed"
  - "x86_64-pc-windows-msvc strict package Clippy: passed"
  - "cargo clippy --workspace --all-targets --all-features --no-deps -- -D
    warnings: passed locally"
  - "cargo fmt --all -- --check and git diff --check: passed"
next: Commit and push the one-line cfg correction, then wait for the new CI run
  to pass before publishing v2.33.2.
confidence: 5
---

## WHAT

Corrected the only remote-CI failure after pushing the 2.33.2 release-prep commit.

- Added `#[cfg(not(unix))]` to the Windows-only `run_scale_worker` helper in `store_maintenance_performance.rs`.

## WHY

The restored Windows maintenance test correctly launches the subprocess on Windows, but its helper was compiled unused on Linux. Remote strict Clippy treats dead code as an error. Local Windows Clippy could not expose this target-specific issue.

## HOW

Scoped the helper itself to the same non-Unix branch that calls it. No lint suppression was added and test behavior was not weakened.

## IMPACT

Linux focused maintenance tests, Linux strict package Clippy, Windows-target strict package Clippy, full local workspace strict Clippy, formatter, and diff check pass. A fresh remote CI run is required before tagging.
