---
id: checkpoint_10132d91
timestamp: 2026-08-18T19:35:34.404Z
tags:
  - release
  - 2.34.0
  - resolution-removal
git:
  branch: main
  commit: a0fb7f0b
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
    - docs/release-notes/README.md
    - docs/release-notes/v2.34.0.md
summary: Release prep 2.34.0 — resolution write-path retirement
type: checkpoint
---

## Release prep 2.34.0 — resolution write-path retirement

### WHAT
Prepared the 2.34.0 release on `main` after the user approved release execution.

- Crate versions 2.33.7 → 2.34.0 (`julie-extract-cli`, `julie-extract-artifact`, `julie-extractors`), `Cargo.lock` refreshed.
- `docs/release-notes/vNEXT.md` → `v2.34.0.md`; draft framing removed; README list updated.
- `Current published release:` pointer stays at v2.33.7 until the tag is live.

### WHY
Ships the resolution write-path removal (artifact schema v7, JSONL v5, `store resolve` gone, in-place store retirement) plus the manifest `.h` language fix. Classified as an intentional break against v2.33.7.

### Verification (this exact tree)
- `cargo fmt --check`, `cargo test -p xtask`, default tier, contract tier: green.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: clean.
- `cargo xtask release preflight --version 2.34.0`: ok, 4 targets, 35 inputs.
- `cargo xtask release package-list`, `scripts/check-agent-doc-sync.sh`, `git diff --check`: ok.
- `cargo xtask dogfood repo`: clean cold scan + no-change rescan, no resolution phase, export ~5,127 rows/s.
- Earlier on this branch: `cargo xtask compat-check` vs the v2.33.7 release binary — 18 fact tables byte-identical, removal classified intentional.

### IMPACT / next
Push `main` + tag `v2.34.0`; the tag triggers the Release Binaries workflow (4 targets). After live-asset verification: advance the release pointer, write release evidence, run the closeout checks. Then Miller's Phase 3 pin bump (Miller repo).
