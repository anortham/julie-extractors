---
id: checkpoint_a1dbd49f
timestamp: 2026-08-18T19:15:37.342Z
tags:
  - review
  - resolution-removal
  - clippy
  - branch-gate
git:
  branch: main
  commit: 73c3bb08
  files:
    - crates/julie-extract-artifact/src/store/layout.rs
    - crates/julie-extract-artifact/src/store/maintenance.rs
    - crates/julie-extract-artifact/tests/store_connection_contract.rs
    - crates/julie-extract-artifact/tests/store_coordinator_contract.rs
    - crates/julie-extract-artifact/tests/store_maintenance_schema_contract.rs
    - crates/julie-extract-artifact/tests/store_manifest_contract.rs
    - crates/julie-extract-artifact/tests/writer_contract.rs
    - crates/julie-extract-cli/tests/store_cli_contract.rs
summary: Post-merge review of the resolution write-path removal
type: checkpoint
evidence:
  - cargo xtask test default green
  - cargo xtask test contract green
  - "cargo clippy --workspace --all-targets --all-features: 0 warnings"
  - "cargo xtask compat-check vs v2.33.7: 18 tables byte-identical"
  - "focused scopes green: store_coordinator_contract, store_manifest_contract,
    store_maintenance_schema_contract, writer_contract,
    store_connection_contract, store_cli_contract, artifact lib"
next: Push and release remain approval-gated; Miller's Phase 3 pin bump happens
  after release.
---

## Post-merge review of the resolution write-path removal

Reviewed the 11-commit implementation of `docs/plans/2026-08-18-resolution-write-path-removal-plan.md` (base `270e984a`, head `73c3bb08`) and fixed the review findings.

### WHAT the review confirmed
- Every plan seam is correct: export snapshot isolation via one read transaction, coordinator `RetiredResolve` parse + reap, writer-only idempotent schema retirement with views rebuild + `foreign_key_check`, manifest language single-sourced from `file_versions.language`.
- Contracts landed: store stays v2, artifact schema v7, JSONL v5, docs + catalogs + `release_package_items` updated, compat exclusions classify the removal.
- Branch gate re-run green: fmt, xtask tests, default tier, contract tier, clippy, `git diff --check`.
- `cargo xtask compat-check` against the downloaded v2.33.7 release binary passed: 18 fact tables byte-identical, removal classified as intentional.

### WHY fixes were needed
Clippy was not clean (the plan's branch gate requires warnings clean), and two tests were weak.

### HOW (the fixes in this commit)
- Deleted three dead functions in `store/maintenance.rs` (`parse_scoped_generation`, `checked_base_path`, `orphan_base_files` — the last still queried the dropped `resolution_bases` table) and the now-unused `Component` import; fixed a needless borrow.
- Moved the `layout.rs` test module below all items.
- Deleted dead test helpers (`index_columns`, `insert_symbol_name`, `insert_symbol_named`, `bind_exact` — the last inserted into dropped tables) and two unused `OptionalExtension` imports.
- Renamed `..._historical_resolve_rows_remain_claimable` to `..._rows_parse` and cut its assertion-free tail; the drain-reap test covers the claimed case.
- The concurrent-export test now asserts the pause hook engaged instead of silently skipping the concurrency section.
- Allowed `clippy::too_many_arguments` on the `seed_historical_resolve` test helper, matching repo precedent.

### IMPACT
The branch now meets its own gate. Ready for the approval-gated release flow (push/tag still need user approval). Note: the original execution committed no checkpoints or verification ledger, contrary to the plan-package rule — this checkpoint closes that gap for the review.
