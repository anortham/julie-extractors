---
id: checkpoint_1d921987
timestamp: 2026-09-21T23:31:49.196Z
tags:
  - qt
  - qml
  - release-3.2.0
  - review
git:
  branch: feat/qt-first-class
  commit: a5de946b
  files:
    - docs/contracts/extraction-output-changes.md
    - docs/release-notes/v3.2.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Qt first-class branch: lead review of wave 4 done, release 3.2.0 ready
  on the branch"
type: checkpoint
next: Commit the two doc corrections, run the Codex review, then hand the
  release decision to the owner.
---

## Qt first-class branch: lead review of wave 4 done, release 3.2.0 ready on the branch

**WHAT**
- `feat/qt-first-class` holds waves 1 to 4 (plan tasks 1 to 18 plus release readiness): version 3.2.0 in all three crates, `docs/release-notes/v3.2.0.md`, ledger entry `## 3.2.0` (compatible) with the capability-table declarations, goldens that lock identifier metadata, `Behavior on color` signatures, refreshed `docs/languages/qml.md` corpus numbers.
- Lead review of e50438dc..a5de946b: no code findings. Two doc corrections by the lead: the `javascript.qml_directive.v1` bullet no longer claims `.import` lines become facts (they become `import` symbols), and the ledger quantifies the relationship drop that follows from removing plain binding symbols (plasma-framework: resolved 1,112 to 205, pending 2,360 to 1,776).

**Gates at a5de946b (worker run)**: fmt, clippy, golden, capability, contract, default (30 binaries), strict quality report, agent-doc sync, `release preflight --version 3.2.0` ok, compat-check against the published 3.1.3 binary reports every difference declared. `scripts/check-release-state.sh` fails by design until v3.2.0 is tagged and pushed (owner approval).

**Known consumer facts for code-kb**: the version guard rebuilds indexes only when the julie version string changes, so the local 3.1.3-versioned branch build needed a forced rebuild; the released 3.2.0 rebuilds once automatically.

**Next**: Codex adversarial review of caf1d1d0..HEAD (code only), then merge to main and release with owner approval; code-kb pin bump to 3.2.0 needs the six release checksums.
