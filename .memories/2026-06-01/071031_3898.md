---
id: checkpoint_3898a5f7
timestamp: 2026-06-01T07:10:31.018Z
tags:
  - release
  - github-actions
  - workflow
  - verification
git:
  branch: codex/release-binaries-workflow
  commit: "2336113"
  files:
    - .github/workflows/release-binaries.yml
    - docs/plans/2026-06-01-release-binaries-workflow.md
    - docs/release.md
    - xtask/tests/commands_contract.rs
summary: Added Release Binaries Workflow
type: checkpoint
impact: Adds verified release-binary workflow scaffolding ready to push and test
  in GitHub Actions.
evidence:
  - cargo fmt --check passed
  - git diff --check passed
  - Ruby YAML parse of .github/workflows/release-binaries.yml passed
  - cargo test -p xtask passed
  - cargo xtask test default passed
  - cargo xtask test contract passed
  - cargo build --release -p julie-extract-cli --bin julie-extract passed
  - cargo xtask release package --version 0.1.0 --target aarch64-apple-darwin
    --out-dir target/release-package/local-aarch64-apple-darwin --binary
    target/release/julie-extract passed
confidence: 5
---

## Added Release Binaries Workflow

- **WHAT:** Added a `Release Binaries` GitHub Actions workflow plan, workflow contract test, release docs, and the workflow file that builds `julie-extract` release binaries on Linux, macOS, and Windows, stages packages via `cargo xtask release package`, and uploads GitHub Actions artifacts.
- **WHY:** The project needs GitHub Actions to produce downloadable CLI binary packages for the standalone extraction product while keeping release packaging separate from fast CI.
- **HOW:** Used a workflow matrix, reused the existing xtask package-staging contract, added an xtask convention test to guard trigger/platform/package/upload behavior, and documented artifact-only release behavior.
- **IMPACT:** Local static and branch gates now prove the release workflow shape, and a local release build plus package staging proves the core build/package command path before pushing.
