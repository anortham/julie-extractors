---
id: checkpoint_98a19198
timestamp: 2026-06-01T15:00:39.549Z
tags:
  - julie-extractors
  - slice-4
  - performance-baseline
  - xtask
  - tdd
git:
  branch: codex/repeatable-performance-baseline
  commit: b75f945
  files:
    - docs/plans/2026-06-01-repeatable-performance-baseline.md
    - docs/testing-strategy.md
    - xtask/src/commands.rs
    - xtask/src/lib.rs
    - xtask/src/performance.rs
    - xtask/tests/commands_contract.rs
    - xtask/tests/performance_baseline_contract.rs
summary: Implemented Repeatable Baseline Runner
briefId: julie-extractors-product-completion-focus
type: checkpoint
evidence:
  - "RED: cargo test -p xtask --test performance_baseline_contract failed
    because xtask::performance did not exist."
  - "RED: baseline_summary_rejects_inconsistent_row_counts failed before stable
    evidence validation."
  - "GREEN: cargo test -p xtask passed after implementation and formatting."
next: Commit the tooling, then build the release binary and run a 3-run
  performance baseline under target/performance.
---

## Implemented Repeatable Baseline Runner

### WHAT

Added the `cargo xtask performance baseline` repo-tooling command and contract tests.

### WHY

Slice 4 needs repeatable same-machine release-profile evidence instead of one-off timing notes. The command must reuse dogfood hard validation and keep timing metrics report-only.

### HOW

- Added `xtask/src/performance.rs` with baseline argument parsing, repeated dogfood runs, stable evidence validation, min/median/max aggregation, and `baseline-summary.json` writing.
- Routed `performance baseline` through `xtask/src/commands.rs` and exported the module from `xtask/src/lib.rs`.
- Added `xtask/tests/performance_baseline_contract.rs` and dispatcher coverage in `xtask/tests/commands_contract.rs`.
- Documented the command in `docs/testing-strategy.md` as local release-evidence tooling, not regular CI.
- Updated the Slice 4 plan progress for Tasks 1 and 2.

### IMPACT

The product contracts remain unchanged. Repeated baseline evidence now has a stable command, hard dogfood validation, and machine-readable aggregate output under `target/performance/`.
