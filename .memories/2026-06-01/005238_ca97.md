---
id: checkpoint_ca97256b
timestamp: 2026-06-01T00:52:38.457Z
tags:
  - mcp
  - tooling
  - julie
git:
  branch: codex/post-bootstrap-release-readiness
  commit: a298e84
  files:
    - .mcp.json
    - xtask/tests/commands_contract.rs
summary: Added repo-local Julie MCP config
type: decision
decision: Add a repo-local `.mcp.json` pointing the Julie MCP server at
  `/Users/murphy/source/julie-extractors` via `JULIE_WORKSPACE`.
alternatives:
  - Rely only on Miller MCP, which is currently returning `Transport closed`.
  - Modify global Codex MCP config, which would affect more than this repository.
  - Do nothing and keep using shell reads, which does not improve future
    sessions.
evidence:
  - "`python3 -m json.tool .mcp.json >/dev/null` passed."
  - "`test -x /Users/murphy/source/julie/target/release/julie-server` passed."
next: Commit `.mcp.json`, then continue Task 1 xtask routing split.
---

## Added repo-local Julie MCP config

- **WHAT:** Created `.mcp.json` with a `julie` stdio MCP server entry pointing at `/Users/murphy/source/julie/target/release/julie-server` and setting `JULIE_WORKSPACE=/Users/murphy/source/julie-extractors`.
- **WHY:** Miller MCP is unavailable in this session with `Transport closed`, and future project sessions should have a project-local way to launch Julie against this repo instead of accidentally resolving the old Julie checkout.
- **HOW:** Matched the repo-local `mcpServers` shape used by other local projects and verified the JSON syntax plus the Julie server executable path.
- **IMPACT:** This is development tooling only. It does not add Julie MCP/server behavior to the `julie-extractors` product surface.
