---
id: checkpoint_8e5039e4
timestamp: 2026-06-01T07:25:38.330Z
tags:
  - github-actions
  - release
  - runner-labels
git:
  branch: main
  commit: f77d143
  files:
    - .github/workflows/release-binaries.yml
    - docs/plans/2026-06-01-release-binaries-workflow.md
    - xtask/tests/commands_contract.rs
summary: Pinned Release Workflow Runner Labels
type: checkpoint
impact: Reduces GitHub-hosted runner alias drift risk for release binary production.
evidence:
  - cargo fmt --check passed
  - git diff --check passed
  - Ruby YAML parse of .github/workflows/release-binaries.yml passed
  - cargo test -p xtask workflow_commands_keep_release_binary_workflow_explicit
    passed
  - cargo test -p xtask passed
confidence: 5
---

## Pinned Release Workflow Runner Labels

- **WHAT:** Changed the release binary workflow matrix from `macos-latest` to `macos-15` and from `windows-latest` to `windows-2025`, with matching convention-test and plan updates.
- **WHY:** A successful GitHub Actions run emitted a notice that `windows-latest` requests are being redirected by June 15, 2026, which makes release packaging unnecessarily sensitive to alias drift.
- **HOW:** Updated `.github/workflows/release-binaries.yml`, `xtask/tests/commands_contract.rs`, and the release-binaries plan. Kept Linux on `ubuntu-latest` because no warning was emitted and the contract target remains `x86_64-unknown-linux-gnu`.
- **IMPACT:** Release binaries should now build on explicit macOS arm64 and Windows x64 hosted runner labels instead of depending on changing latest aliases.
