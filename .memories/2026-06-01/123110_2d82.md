---
id: checkpoint_2d825181
timestamp: 2026-06-01T12:31:10.426Z
tags:
  - julie-extractors
  - dogfood
  - review-fix
  - performance
git:
  branch: codex/dogfood-performance-baseline
  commit: 28082ce
  files:
    - xtask/src/dogfood.rs
    - xtask/tests/dogfood_contract.rs
summary: Hardened Dogfood Rescan Validation
type: checkpoint
evidence:
  - cargo test -p xtask --test dogfood_contract failed before implementation
    because invalid rescan evidence passed validation
  - cargo test -p xtask --test dogfood_contract passed after implementation
  - cargo fmt --all -- --check passed
  - cargo test -p xtask passed
  - git diff --check passed
next: Commit the review fix, rerun dogfood at the new HEAD, refresh release
  evidence, then rerun branch gates and open the PR.
confidence: 5
---

## Hardened Dogfood Rescan Validation

- **WHAT:** Added contract coverage and xtask validation for the review finding that rescan evidence must prove no new revision and no row writes.
- **WHY:** The dogfood release evidence claimed `created_revision_id=null` and zero row churn, but validation only enforced `status=no_change` and file change counts.
- **HOW:** Added failing dogfood contract cases for a rescan report with `created_revision_id` set and with nonzero `counts.rows_written`, then updated `validate_rescan_report` to reject those reports.
- **IMPACT:** Dogfood now enforces the no-change path more directly and aligns validation with the evidence document.
