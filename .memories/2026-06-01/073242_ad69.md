---
id: checkpoint_ad69c203
timestamp: 2026-06-01T07:32:42.613Z
tags:
  - github-actions
  - release
  - runner-labels
  - windows
git:
  branch: main
  commit: 39ff963
  files:
    - .github/workflows/release-binaries.yml
    - docs/plans/2026-06-01-release-binaries-workflow.md
    - xtask/tests/commands_contract.rs
summary: Switched Windows Release Runner to Windows 2022
type: checkpoint
impact: Pins Windows release builds to a stable supported x64 runner label.
evidence:
  - cargo fmt --check passed
  - git diff --check passed
  - Ruby YAML parse of .github/workflows/release-binaries.yml passed
  - cargo test -p xtask workflow_commands_keep_release_binary_workflow_explicit
    passed
confidence: 5
---

## Switched Windows Release Runner to Windows 2022

- **WHAT:** Changed the release binary workflow Windows matrix row from `windows-2025` to `windows-2022`, with matching convention-test and plan updates.
- **WHY:** The `windows-2025` run still emitted GitHub's redirect notice for the upcoming `windows-2025-vs2026` image. The release package only needs a stable x64 MSVC runner, so `windows-2022` is the safer explicit label.
- **HOW:** Updated `.github/workflows/release-binaries.yml`, `xtask/tests/commands_contract.rs`, and `docs/plans/2026-06-01-release-binaries-workflow.md`, then reran static checks and the focused workflow convention test.
- **IMPACT:** Release binary production avoids the Windows 2025 alias transition warning while keeping the target artifact label `x86_64-pc-windows-msvc`.
