---
id: checkpoint_47808d19
timestamp: 2026-09-08T19:19:52.196Z
tags:
  - release
  - 2.42.0
  - store-import
git:
  branch: main
  commit: 8feb5d72
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extract-cli/tests/store_reader_mixed_version_contract.rs
    - crates/julie-extractors/Cargo.toml
    - crates/julie-extractors/README.md
    - docs/release-notes/v2.42.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepare julie-extract 2.42.0 (store throughput release)
type: checkpoint
---

## Prepare julie-extract 2.42.0 (store throughput release)

**WHAT:** Version bump 2.41.1 → 2.42.0 in the three shipped crates, lockfile, and the extractors README; `NEWER_FACTORY_VERSION` fixture 2.42.0 → 2.43.0 so the mixed-version reader test still has a newer factory; `docs/release-notes/v2.42.0.md` written. Preceded by 8feb5d72, which fixed the clippy lints that stable 1.98.1 added (is_multiple_of, let-chain collapsible if, type_complexity) and had failed CI Fast Gates on the last three main pushes.

**WHY:** User approved releasing the performance work (SQLite 3.53.2, busy reporting, connection/pool reuse, core-scaled workers, deep-wave prefetch + Codex review fixes) after the Codex review.

**HOW:** Minor bump because `--jobs 0` semantics and the deep chunk default changed (CLI contract text updated) and store commands gained the `busy` class. No extraction output change, so no entry in `docs/contracts/extraction-output-changes.md` (store-only releases never had one). Epoch stays 10.

**Gates so far:** `cargo xtask release preflight --version 2.42.0` → 4 targets, 35 inputs. `cargo fmt --check`, `git diff --check`, `cargo test -p xtask` pass. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.

**IMPACT / next:** Commit prep, run `xtask test default` + `contract` on Linux, sync + run default/contract on the Windows guest (threads + paths touched), push main, wait for CI, tag v2.42.0, push tag, verify the Release Binaries workflow, write `docs/release-evidence/2026-09-08-v2-42-0-release.md`, advance the pointer in docs/release.md, closeout with scripts/check-release-state.sh.
