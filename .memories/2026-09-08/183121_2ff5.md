---
id: checkpoint_2ff559ce
timestamp: 2026-09-08T18:31:21.142Z
tags:
  - design
  - store-import
  - prefetch
  - plan
git:
  branch: main
  commit: 4ac5cfd9
  files:
    - docs/plans/2026-09-08-deep-wave-prefetch-pipeline.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Deep-wave prefetch pipeline design
type: decision
decision: Overlap deep-wave extraction with writes via a single bounded prefetch
  owned by StoreRequestExecutor, keyed by (request_id, chunk_index,
  plan_fingerprint), consumed only when the per-file hashes still match; the
  coordinator, quantum, commit, and recovery contracts stay unchanged.
alternatives:
  - "Larger deep chunks: trades away interactive fairness, crash granularity,
    and memory"
  - "Pipeline inside the coordinator: would put extraction next to SQLite access
    and lease logic; rejected to keep SQLite on one thread"
---

## Deep-wave prefetch pipeline design

### WHAT
- `docs/plans/2026-09-08-deep-wave-prefetch-pipeline.md`: problem, goal, state shape, quantum flow, invalidation rules, failure/shutdown, memory bound (two chunks in flight), expected gain (deep wave bounded by the larger of ~17 s extraction and ~9 s write), measurement method, risks, and five tasks.

### WHY
- After connection reuse and core-scaled chunks the import is 35 s vs 17 s scan; the remaining gap is the serial extract-then-write quantum.

### IMPACT
- Not implemented. Next session can execute the five tasks and measure with the audit workload.
