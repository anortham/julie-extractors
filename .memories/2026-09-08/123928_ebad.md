---
id: checkpoint_ebad939c
timestamp: 2026-09-08T12:39:28.562Z
tags:
  - release
  - v2.41.1
  - blocked
  - github-auth
git:
  branch: main
  commit: da06f781
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: bump-extraction-identity-epoch-to-8
type: incident
context: Authorized publication of verified clean candidate da06f781 failed at
  the first external write.
evidence:
  - "git push origin main: fatal unable to get password from user"
  - "gh auth status: active anortham token invalid"
  - "SSH strict host verification: no trusted github.com ED25519 key"
  - origin/main5912bafe; candidate da06f781; no v2.41.1
next: User restores GitHub authentication with gh auth login -h github.com;
  resume publication from a clean reviewed commit.
---

## WHAT

Publication of julie-extract2.41.1 is blocked before any remote mutation. `git push origin main` failed because the configured HTTPS credential cannot authenticate. `gh auth status` reports the sole active anortham token invalid.

## WHY

The GitHub credential expired or was revoked after prior release work. No valid preconfigured fallback exists: SSH with BatchMode and strict host-key checking refuses because github.com has no trusted ED25519 host key in the existing known-hosts file.

## HOW

Checked only safe existing authentication paths without reading, printing, replacing, or reconfiguring secrets. Verified local main remains clean at da06f781, origin/main remains5912bafe, and v2.41.1 does not exist locally, remotely, or as a GitHub release.

## IMPACT

No external state changed. After the user runs `gh auth login -h github.com`, resume by rechecking clean main and live v2.41.1 absence, push main, create/push v2.41.1 at the reviewed commit, monitor Release Binaries, verify four immutable archives and checksums, record public evidence, then hand real hashes to Miller. Never use the local Linux binary hash as an archive pin.
