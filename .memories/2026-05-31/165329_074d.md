---
id: checkpoint_074d4e77
timestamp: 2026-05-31T16:53:29.769Z
tags:
  - docs
  - contracts
  - bootstrap
git:
  branch: main
  commit: d2a3a68
  files:
    - README.md
    - docs/contracts/cli.md
    - docs/contracts/jsonl-v1.md
    - docs/contracts/reports.md
    - docs/contracts/sqlite-schema-v1.md
summary: Drafted First Product Contract Docs
type: checkpoint
context: Continuing product bootstrap for the new standalone julie-extractors repo.
impact: Public contract surface is now concrete enough to guide migration tasks
  and future contract-tier tests.
evidence:
  - Verified all four contract files exist.
  - Executed SQL code blocks from sqlite-schema-v1.md through sqlite3 :memory:.
  - Parsed JSON code blocks from jsonl-v1.md and reports.md with jq.
  - Placeholder/deferred-language scan over docs/contracts and README returned
    no matches.
  - Subagent re-review confirmed prior blocking findings resolved.
next: Use these docs as the source for the first CLI/schema/report tests when
  code migration begins.
confidence: 5
---

## Drafted First Product Contract Docs

- **WHAT:** Added the first draft contract docs under `docs/contracts/`: CLI, SQLite schema v1, JSONL v1, and JSON reports. Linked them from `README.md`.
- **WHY:** The repo needs explicit public contracts before code migration starts, with `julie-extract` CLI and SQLite artifact as first-class product APIs.
- **HOW:** Read the new repo planning docs, inspected old Julie extraction docs/code/fixtures for evidence only, then designed clean product-neutral contracts instead of copying the old Julie schema. Used parallel subagents for old-Julie evidence and a follow-up contract review.
- **IMPACT:** Establishes draft downstream contracts for commands, statuses, exit codes, schema domains, JSONL envelope/payloads, report shapes, and error codes while keeping MCP/server/daemon/search/embedding/watcher/dashboard/editing behavior out of scope.

Review fixes applied:
- Made JSONL payload schemas explicit for every record kind.
- Added report `input.file_path` and `input.root_relative_path` for successful single-file commands.
- Aligned force rebuild naming as `operation: scan`, `mode: force`.
- Made report row-count keys exhaustive for SQLite schema v1 domains.
