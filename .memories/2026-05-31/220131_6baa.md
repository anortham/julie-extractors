---
id: checkpoint_6baa56d1
timestamp: 2026-05-31T22:01:31.139Z
tags:
  - julie-extractors
  - task-12
  - release
  - certification
  - xtask
git:
  branch: codex/julie-code-migration
  commit: 86ffa3f
  files:
    - docs/release-notes/README.md
    - docs/release.md
    - docs/testing-strategy.md
    - xtask/src/lib.rs
    - xtask/src/release.rs
    - xtask/src/test_tiers.rs
    - xtask/tests/release_contract.rs
    - xtask/tests/test_tiers.rs
summary: Implemented Task 12 certification and release scaffolding
type: checkpoint
impact: Task 12 implementation is ready for commit; plan ledger can be recorded
  after the implementation commit SHA exists.
evidence:
  - "RED: `cargo test -p xtask` failed because certification lacked
    capability-matrix commands."
  - "RED: `cargo test -p xtask --test release_contract` failed until
    `docs/release.md` was included in the manifest."
  - "GREEN: `cargo test -p xtask` passed."
  - "GREEN: `cargo xtask test default` passed."
  - "GREEN: `cargo xtask test contract` passed."
  - "GREEN: `cargo xtask test certification` passed."
  - "GREEN: `cargo xtask test real-world-smoke` passed."
  - "GREEN: `cargo xtask test real-world-release` passed."
  - "GREEN: `cargo xtask test changed crates/julie-extractors/Cargo.toml`
    passed."
  - "GREEN: `cargo fmt --check`, `cargo metadata --format-version 1 --no-deps`,
    and `git diff --check` passed."
next: Commit Task 12 implementation, then update the migration-plan acceptance
  checkboxes and verification ledger with the commit SHA.
---

## Implemented Task 12 certification and release scaffolding

- **WHAT:** Added xtask support for changed-path test planning, separate real-world smoke/release profiles, certification planning that runs capability-matrix and parser-upgrade gates, and a release package manifest for standalone `julie-extract` artifacts.
- **WHY:** Task 12 requires release/certification scaffolding without importing old Julie daemon/server packaging or letting slow gates leak into the default suite.
- **HOW:** Extended `xtask::test_tiers`, added `xtask::release`, added release contract tests, documented release commands, and added release-notes policy docs.
- **IMPACT:** The plan now has explicit commands for parser-dependency changes, real-world smoke vs release evidence, and package contents constrained to `julie-extract` binaries, checksums, docs, and release notes.
