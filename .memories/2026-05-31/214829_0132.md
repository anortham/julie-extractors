---
id: checkpoint_01325ca2
timestamp: 2026-05-31T21:48:29.119Z
tags:
  - task-11
  - cli
  - sqlite
  - jsonl
  - verification
git:
  branch: codex/julie-code-migration
  commit: aef0c3a
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/src/model.rs
    - crates/julie-extract-artifact/src/writer.rs
    - crates/julie-extract-artifact/tests/writer_contract.rs
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extract-cli/src/commands.rs
    - crates/julie-extract-cli/src/discovery.rs
    - crates/julie-extract-cli/src/extraction.rs
    - crates/julie-extract-cli/src/main.rs
    - crates/julie-extract-cli/tests/cli_contract.rs
    - crates/julie-extract-cli/tests/operations_contract.rs
    - xtask/src/test_tiers.rs
    - xtask/tests/test_tiers.rs
summary: Task 11 CLI Operations End-to-End Implemented
type: checkpoint
evidence:
  - "RED: `cargo test -p julie-extract-cli --test operations_contract` failed on
    supported-file scan stub before implementation."
  - "GREEN: `cargo test -p julie-extract-cli --test operations_contract` passed."
  - "GREEN: `cargo test -p julie-extract-artifact --test writer_contract`
    passed."
  - "GREEN: `cargo test -p julie-extract-cli --test cli_contract` passed."
  - "GREEN: `cargo test -p julie-extract-cli --test path_policy` passed."
  - "GREEN: `cargo test -p julie-extract-artifact --test jsonl_contract` passed."
  - "GREEN: `cargo test -p julie-extract-cli` passed."
  - "GREEN: `cargo test -p julie-extract-artifact` passed."
  - "GREEN: `cargo test -p xtask` passed."
  - "GREEN: `cargo xtask test default` passed."
  - "GREEN: `cargo xtask test contract` passed."
  - "GREEN: `cargo fmt --check` passed."
  - "GREEN: `cargo metadata --format-version 1 --no-deps` passed."
  - "GREEN: `git diff --check` passed."
next: Commit Task 11 code, then record the Task 11 ledger entry in the migration
  plan.
---

## Task 11 CLI Operations End-to-End Implemented

- **WHAT:** Wired `julie-extract` scan/update paths from source discovery through `julie_extractors::extract_canonical`, normalized artifact row mapping, batched SQLite writes, delete/info/export/languages reports, and JSONL export verification.
- **WHY:** Task 11 required the CLI to produce the standalone extraction product artifact end-to-end instead of returning the prior supported-file stub errors.
- **HOW:** Added a CLI-local extraction mapper, real BLAKE3 content hashes, snapshot scan deletion semantics in the writer, metadata updates only on actual writer mutations, operations contract tests, and contract-tier wiring.
- **IMPACT:** `scan`, `scan --force`, no-change scan, update, delete/not_found, read-only info, JSONL export, and languages capability output now pass through public CLI behavior against tiny fixture roots.
