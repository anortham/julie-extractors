---
id: checkpoint_c6fabb50
timestamp: 2026-08-07T15:54:55.308Z
tags:
  - row-level-scoping
  - resolution
  - v2.29.0
  - performance
  - shadow-mode
git:
  branch: main
  commit: "9529188"
summary: "Row-level resolution scoping: proven equivalent and measured — 2.29.0
  prep underway"
type: checkpoint
next: Task 5 worker commit → codex pre-merge review → user approval for release
  + pin bump. Reconcile this checkpoint file into the branch before push.
---

## Row-level resolution scoping: proven equivalent and measured — 2.29.0 prep underway

**WHAT:** Tasks 1–4 of the row-level scoping plan are committed on branch `row-level-scoping` (worktree .worktrees/row-level-scoping): hazard harness (bbd47bd), expansion accessors (f55e503), the resolve_delta swap (a25b677, with one oracle-forced amendment — the co-location repair for the delta's own demotion writes), and shadow mode (46401a0). Both lead-run release gates PASSED.

**WHY:** A one-file save re-derived 80–93% of identifier rows (16–18 s). The swap expands the touched-NAME set through the alias/receiver keying relations instead of expanding to whole files; the path-keyed module arm stays file-scoped.

**HOW (gates):** 40-save shadow dogfood (28 touches + 12 renames, julie Rust + Miller C# corpora, JULIE_RESOLUTION_SHADOW=1): 40× exit 0, zero mismatches. Save-shape A/B on the full Miller fixture vs the committed baselines: JulieExtractRunner.cs 15.98 s → 6.42 s wall (rows 339,869 → 24,353, 14×); SearchTool.cs 18.75 s → 10.84 s (rows 348,980 → 72,329, 4.8×); pending counts identical. Evidence: docs/release-evidence/v2.29.0/.

**IMPACT:** The save cost drops 1.7–2.4× wall today; the row cut (4.8–14×) proves the mechanism, and the remaining floor is the whole-workspace candidate-index load — the named follow-up lever. Task 5 worker is preparing the 2.29.0 release (sweep re-measurement, bump, notes with these numbers); then codex pre-merge review; release itself + Miller pin bump await user approval.
