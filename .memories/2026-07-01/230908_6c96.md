---
id: checkpoint_6c962a59
timestamp: 2026-07-01T23:09:08.715Z
tags:
  - julie-extractors
  - codex-review
  - plans
  - plan-fixes
git:
  branch: main
  commit: 1728dad
  files:
    - docs/plans/2026-07-01-engineering-health-pass.md
    - docs/plans/2026-07-01-http-boundary-facts.md
    - docs/plans/2026-07-01-structural-fact-pattern-registry.md
    - docs/plans/2026-07-01-web-structural-facts-module-split.md
summary: "Codex plan review: 6/6 findings verified and fixed in the four lane plans"
briefId: julie-extractors-post-v2-5-10-roadmap-http-boundar
type: checkpoint
next: User approval on the updated plans; then execute lane 1 (module split) via
  subagent-driven-development.
---

## Codex plan review: 6/6 findings verified and fixed in the four lane plans

Ran a read-only Codex review of the four 2026-07-01 lane plans; verified every finding against source before applying (all six held up):

1. **Module split / MarkupAttribute:** the struct is defined TWICE with different shapes — framework `{name, value, start_byte, end_byte}` (:936) vs web `{tag_name, name, value, span: NormalizedSpan}` (web_structural_facts.rs:3303, missed by the earlier symbol listing). Plan 1 Task 1 now specifies a six-field superset struct and which call sites consume which fields.
2. **HTTP boundary / ASP.NET naming:** aspnet.attribute_route.v1 keeps `route_template`/`effective_route_template` (matching minimal-API emission at framework:408 and jsonl-v3.md:511); the M6 `route_path` rule is now explicitly scoped to frontend families, with `effective_route_template` documented as the server-side join key for Miller.
3. **HTTP boundary / test wiring:** `crates/julie-extractors/src/tests/mod.rs` is an explicit module list — Task 1 now registers `pub mod http_client;` and worker verification requires "0 tests run = FAIL".
4. **Registry plan / gating conflict:** module registered ungated in tests/mod.rs; only golden-corpus conformance fns carry `#[cfg(feature = "test-golden")]`, so the default-suite JSON sync test can coexist.
5. **Registry plan / wrong citation:** `fn languages` handler is commands.rs:795 (dispatch :78) — fixed.
6. **Engineering health / writer premise wrong:** writer.rs has ZERO production unwraps (test module starts :1070; one documented expect at :947). Task 3 retargeted to the verified hotspots: sql/schemas.rs (18) + vue/helpers.rs (10), with writer.rs explicitly out of scope.

**State:** main @ 1728dad, four updated untracked plan files, nothing committed. Awaiting user approval to execute (lane 1 first).
