---
id: checkpoint_94f171d6
timestamp: 2026-07-01T15:17:56.875Z
tags:
  - julie-extractors
  - structural-facts
  - web
  - follow-up
git:
  branch: codex/residual-web-route-facts
  commit: 737227e
  files:
    - crates/julie-extractors/src/base/web_structural_facts.rs
    - crates/julie-extractors/src/tests/nuxt/structural_facts.rs
    - crates/julie-extractors/src/tests/react/structural_facts.rs
    - crates/julie-extractors/src/tests/vue/structural_facts.rs
summary: Fixed residual web route edge cases
briefId: julie-extractors-best-tree-sitter-implementation-f
type: checkpoint
context: Follow-up review flagged residual low-severity web route fact edge
  cases F/G/H after the prior fix was merged locally.
impact: Closes residual F/G/H follow-up findings locally; `.worktrees/` is now
  the local worktree convention after adding it to `.gitignore` on main.
evidence:
  - Initial cargo test -p julie-extractors structural_facts failed 3 new tests
    for F/G/H
  - "cargo test -p julie-extractors structural_facts -- --nocapture: 63 passed"
  - "cargo fmt --check: passed"
  - "cargo test -p julie-extractors --features test-golden --lib golden --
    --nocapture: 1 passed"
  - "cargo test -p julie-extractors --features test-capability-matrix --lib
    capability_matrix -- --nocapture: 36 passed"
  - "cargo test -p julie-extractors --features test-capability-matrix --lib
    pending_shape_contract -- --nocapture: 1 passed"
  - "cargo clippy --workspace --all-targets --all-features --no-deps -- -D
    warnings: passed"
  - "node scripts/language-data-quality-report.mjs --strict: exit 0,
    silent_cells 0, quality_bar_debts 0"
symbols:
  - nextjs_file_route_fact
  - nuxt_file_route_fact
  - has_nuxt_page_signal
  - vue_route_definition_ranges
  - create_router_routes_identifier
confidence: 5
unknowns:
  - Full framework disambiguation still cannot use project-level
    next.config/nuxt.config because the collector only receives a single file's
    path/content/tree.
---

## Fixed residual web route edge cases

- **WHAT:** Added regression coverage and fixes for three residual web route fact cases: `app/pages/**/page.tsx` no longer emits competing Next/Nuxt file routes, Nuxt file-route signals no longer come from comment/string text or generic `defineComponent`, and Vue route definitions now follow `createRouter({ routes: appRoutes })` to the referenced array.
- **WHY:** The residual review found low-severity false positives/false negatives that could still confuse framework route facts after the first pass.
- **HOW:** File-route fact generation now receives the parsed tree, Nuxt detection checks executable Nuxt-specific identifiers and static Nuxt import sources instead of raw `contains`, `app/pages` disambiguation suppresses Nuxt for Next app `page` files without a Nuxt signal, and Vue route range discovery adds array initializer ranges referenced by the `routes` property in `createRouter`.
- **IMPACT:** Web structural route facts are more precise without needing project-level config, and the new tests document the local tie-breaker behavior.
