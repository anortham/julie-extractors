---
id: checkpoint_c2584484
timestamp: 2026-08-26T19:24:15.382Z
tags:
  - release
  - v2.37.1
  - msbuild
  - xml
  - epoch-bump
git:
  branch: main
  commit: ee0da1a7
  files:
    - .github/workflows/release-binaries.yml
    - .github/workflows/specialist-gates.yml
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extract-cli/tests/store_equivalence.rs
    - crates/julie-extractors/Cargo.toml
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/tests/api_surface.rs
    - docs/contracts/extraction-output-changes.md
    - docs/contracts/store-v1.md
    - docs/release-notes/README.md
    - docs/release-notes/v2.37.1.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared v2.37.1 release candidate for the MSBuild XML extensions
type: checkpoint
---

## Prepared v2.37.1 release candidate for the MSBuild XML extensions

**WHAT**
- Fast-forwarded `main` to `feat/msbuild-xml-extensions` (ee0da1a7): xml spec now claims `csproj`, `props`, `targets`, `vbproj`, `fsproj`, `slnx`, `nuspec`, `resx`. `sln` stays unclaimed.
- Bumped `EXTRACTION_IDENTITY_EPOCH` 6 → 7 in `crates/julie-extractors/src/lib.rs`, with the pin in `tests/api_surface.rs` and the contract mirror in `docs/contracts/store-v1.md`.
- Parameterized a hardcoded epoch 6 in `crates/julie-extract-cli/tests/store_equivalence.rs` (`assert_v3_completion_and_manifest_bridge`) so the SQL check reads `EXTRACTION_IDENTITY_EPOCH`.
- Declared `## 2.37.1` (classification: compatible) in `docs/contracts/extraction-output-changes.md`.
- Version bump to 2.37.1: three crate Cargo.tomls, Cargo.lock, `release-binaries.yml` and `specialist-gates.yml` dispatch defaults.
- Wrote `docs/release-notes/v2.37.1.md` and indexed it in the release-notes README.

**WHY**
- The branch commit claimed the extensions but missed the release contract: store-v1.md requires a strictly newer epoch plus a classified ledger entry for any output difference. Files these extensions match were previously dropped as `unsupported`; they now publish rows, so output differs.

**HOW**
- Followed the QML precedent (v2.35.1, epoch 4 → 5) for a new-extensions release.
- Epoch bump forces family-store re-extraction because identity is `(path, content_hash, extraction_epoch)`.

**IMPACT / evidence**
- Gates green on this tree: data-quality strict (0 debts), `cargo fmt --check`, agent-doc sync, `cargo test -p xtask`, `xtask test default`, `xtask test contract`, `xtask test certification`, workspace clippy `-D warnings`, `release preflight --version 2.37.1` (4 targets, 35 inputs).
- Next: commit prep, push `main`, tag `v2.37.1`, watch Release Binaries workflow, then closeout (evidence doc, README/docs pointers, release.md advance).
