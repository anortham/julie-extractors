---
id: checkpoint_caf44846
timestamp: 2026-09-22T22:15:45.590Z
tags:
  - language-gaps
  - wave1
  - integration
  - review
git:
  branch: feat/language-gap-closure
  commit: 0ac8705c
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
next: Process Codex review findings; review the real-repo comparison; final report.
---

## WHAT
Wave 1 of the language gap closure is merged on `feat/language-gap-closure` at 0ac8705c (not pushed). All 13 group branches (`gaps/*`) merged; ~347 high gaps closed (1 bash sub-gap dropped per decision 2026-09-08 receiver-type-facts-wave-2).
Integration fixes by the lead:
- 16 recursive walkers lacked the traversal depth guard (traversal_guard_convention test) — guarded with `_at` depth variants.
- Reference-site owner conflicts (contract test reference_site_identity): C identifiers skip Field containers; F# Scope skips Field/EnumMember; Vue top-level calls take the innermost declaration as caller (decision doc updated).
- Rust `walk_tree` frame overflowed the 2 MiB test stack on Windows; out-of-line `push_symbol` halves stack need; the depth test now runs on a 1.5 MiB thread.
- Contract: version 3.4.0, `EXTRACTION_CONTRACT_VERSION` + `language-gap-closure-v1`, ledger `## 3.4.0`; epoch stays 10 (3.x precedent).

## WHY
User asked to close gaps in every language, keep agent count low, and run a Codex review of the whole branch before release.

## HOW
Gates at 0ac8705c all green on Linux (fmt, clippy, doc sync, strict quality 0/0, xtask, default, contract, compat-check NOTICE vs 3.3.1) and Windows default tier (4490 lib tests).

## IMPACT
Running now: Codex pre-merge review (general + security passes, budget 2/2, script scratchpad/review/run-review.sh) and a real-repo old/new scan comparison (scratchpad/compare). Next: verify every Codex finding with code-kb, fix/dismiss, local confirmation, report. Release/push/tag need explicit user approval. Worktrees `.worktrees/gaps-*` are merged and can be removed after approval.
