---
id: checkpoint_63f8231e
timestamp: 2026-09-22T22:31:12.602Z
tags:
  - language-gap-closure
  - wave-2
  - workflow
git:
  branch: feat/language-gap-closure
  commit: 13fb3273
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Wave 2 of language gap closure started
type: decision
decision: Wave 2 (medium and low gaps) runs on feat/language-gap-closure before
  release; 3.4.0 ships both waves together after a new Codex review.
alternatives:
  - "Release 3.4.0 first, then wave 2 on a new branch: the user chose option 2
    instead"
next: Wait for the workflow. Then merge the group branches, reconcile conflicts,
  and run the integration gates.
---

## Wave 2 of language gap closure started

**WHAT**
- The plan `docs/plans/2026-09-22-language-gap-closure.md` has a new "Wave 2" section (commit 13fb3273).
- Scope 1: the 685 medium and low audit gaps that wave 1 left open (476 medium, 209 low, after merging duplicate ids). Wave 1 closed or dropped 70.
- Scope 2: the capabilities.json `open_gaps` entries that one file can close without an owner decision.
- The workflow `wf_506f0671-0f7` (task we0ft9tzy) runs 13 group agents, at most 4 at a time, in the existing `.worktrees/gaps-<group>` worktrees. All 13 were fast-forwarded to 13fb3273.
- The gap evidence is in the scratchpad `wave2/<unit>.json`. It was built from `gaps/*.json`, minus the ids closed or dropped in the wave-1 journal.

**WHY**
- The user asked for wave 2 on this branch, with one release for both waves.
- The user also wants the agent count kept low, because of usage limits.

**HOW**
- The wave-1 prompt was reused. It adds the wave-1 integration lessons: depth-guarded walkers, no large values in the frames of recursive walkers (a Windows stack limit), one owner per reference site, and the html/vue goldens that embed JS/CSS output.
- Each agent returns per-language `output_changes` text for the lead's ledger.
- The 26 unverified units get no separate verifier run. The reproduce-first rule is the verification.

**IMPACT**
- Integration next: merge the 13 branches and regenerate the html/vue goldens.
- Extend the `## 3.4.0` ledger entry. The version and the v1 marker stay as they are, because 3.4.0 is unreleased.
- Then run the branch gates, the Windows default tier, the real-repo comparison, and a new two-pass Codex review.
