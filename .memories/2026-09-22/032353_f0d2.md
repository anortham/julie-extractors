---
id: checkpoint_f0d2dac7
timestamp: 2026-09-22T03:23:53.343Z
tags:
  - julie-extractors
  - wave9
  - release
  - version-bump
git:
  branch: feat/qt-cpp-macros
  commit: "13797156"
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Wave 9 task 22e step 1: version bump to 3.3.0"
type: checkpoint
---

## Wave 9 task 22e step 1: version bump to 3.3.0

Set `version = "3.3.0"` in the three release crates: `julie-extractors`,
`julie-extract-cli`, `julie-extract-artifact`. There are no internal version pins between
them — the workspace uses path dependencies — so only the three `[package]` lines and
`Cargo.lock` change, exactly as commit `cc43fb5a` did for 3.2.0.

`cargo check --workspace` passes and updated `Cargo.lock`.

Next: the eleven release gates on the final tree, then the two corpus re-measurements with
the 3.3.0 release binary.
