---
id: checkpoint_2aca87d4
timestamp: 2026-09-22T02:43:21.050Z
tags:
  - wave7
  - cpp
  - capabilities
  - task22b
git:
  branch: feat/qt-cpp-macros
  commit: 3d05f3d5
  files:
    - fixtures/extraction/capabilities.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Wave 7 task 22b: the cpp capability rows"
type: checkpoint
next: Build the release binary and measure both corpora for the wave-7 report.
---

## Wave 7 task 22b: the cpp capability rows

`fixtures/extraction/capabilities.json`, cpp `kind_coverage`:

- `symbols.supported` adds `event` and `property`, list kept sorted.
- `body_spans.not_applicable` adds `event` and `property`, because a declaration in a header has no body.
- `structural_facts.supported` adds `cpp.qt_property.v1`.

`docs/contracts/structural-fact-patterns.json` needed no change; wave 6 regenerated it with the pattern when the spec landed (`37d9d9db`). The `UPDATE_CONTRACT_JSON=1` run left it byte-identical.

This clears the wave-6 capability failure `capability_matrix_structural_fact_claims_match_registry`.

**Verification.** `cargo xtask test capability` 39 + 1 passed, `contract` 3 tiers all ok, `language cpp` 179 passed, `default` 30 suites ok, `golden` 8 passed, fmt and clippy clean.
