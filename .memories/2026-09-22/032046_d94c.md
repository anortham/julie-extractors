---
id: checkpoint_d94c6d3e
timestamp: 2026-09-22T03:20:46.502Z
tags:
  - julie-extractors
  - wave9
  - contracts
  - cpp
  - qt
git:
  branch: feat/qt-cpp-macros
  commit: 1650681a
  files:
    - crates/julie-extractors/src/lib.rs
    - docs/contracts/extraction-output-changes.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Wave 9 task 22c: the 3.3.0 contract declaration"
type: checkpoint
---

## Wave 9 task 22c: the 3.3.0 contract declaration

- **What:** `EXTRACTION_CONTRACT_VERSION` in `crates/julie-extractors/src/lib.rs` gains the
  `.qt-cpp-v1` suffix. `EXTRACTION_IDENTITY_EPOCH` stays 10.
- **What:** `docs/contracts/extraction-output-changes.md` gains a `## 3.3.0` entry,
  `classification: compatible`, one paragraph per table and row family, each ending in a
  "Consumer action for code-kb" sentence.
- **Covered:** new cpp `property` and `event` rows, the `qt_slot`/`qt_invokable`/`qml_*`/`qt_object`
  metadata keys, the rows that disappear (macro-named, empty-name, forward declarations, the
  duplicate constructor/destructor row, `Q_EMIT` variables), the signature changes (whole declared
  return type, trailing `override`/`final`, no leaked sibling modifier), the constructor and
  destructor visibility and terminator, `cpp.qt_property.v1`, the `parse_diagnostics` drop, the
  `language_capabilities` and `language_capability_fixtures` rows, the capability fingerprint, the
  golden fixture changes, and the contract-version suffix. Schema stays 7, report schema stays 3.
- **Gate:** `cargo xtask compat-check` against the 3.2.0 release binary lists exactly two tables:
  `language_capabilities` (the cpp row) and `language_capability_fixtures` (the new `cpp/qt_header`
  row). Both are declared. Exit 0.

The NOTICE still names `## 3.2.0`, because the crate version is not bumped until task 22e.
