---
id: checkpoint_2bc61e2f
timestamp: 2026-09-22T17:23:07.268Z
tags:
  - audit
  - language-gaps
  - workflow
git:
  branch: main
  commit: ea0bd76a
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
next: Wait for wf_3736beaf-bc7, triage verified gaps, then run the fix workflow.
---

## WHAT
Started a per-language extraction gap audit across all 40 languages (37 audit units). Workflow `language-gap-audit` (run wf_3736beaf-bc7) runs two finder lenses per unit (grammar coverage, real-world code) and one adversarial verifier per unit. Verifiers write `verified.json` under the session scratchpad `audit/<unit>/verify/`.

## WHY
User asked to examine each supported language for gaps that can be closed. Ledger baseline: `node scripts/language-data-quality-report.mjs --strict` shows silent_cells 0, quality_bar_debts 0, open_gap_backlog 47 (42 structural_facts, 3 relationships, 2 test_detection) plus 2 open XML capability gaps.

## HOW
- Pinned probe binary: release `julie-extract` built at HEAD ea0bd76a, copied to scratchpad `probe/julie-extract`; `probe/probe.sh <file-or-dir> [tables]` scans into a temp artifact and prints every row family via sqlite3.
- Audit agents are read-only and do not run cargo.
- Other worktrees `.worktrees/feat-qt-cpp-macros` and `.worktrees/feat-qt-first-class` are clean and already merged into main.

## IMPACT
Next: read verified gaps, plan fixes per language, run a fix workflow in isolated worktrees on a new branch, verify with `cargo xtask test language <x>`, golden, capability, strict quality report. No push without approval.
