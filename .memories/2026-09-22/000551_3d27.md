---
id: checkpoint_3d279253
timestamp: 2026-09-22T00:05:51.250Z
tags:
  - qt
  - qml
  - release-3.2.0
  - codex-review
  - fix-round
git:
  branch: feat/qt-first-class
  commit: 6068aabf
  files:
    - docs/contracts/extraction-output-changes.md
    - docs/languages/qml.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Qt first-class branch: Codex fix round reviewed, corpus numbers refreshed"
type: checkpoint
next: Commit the number refresh, run the default tier once more, then hand the
  release decision to the owner.
---

## Qt first-class branch: Codex fix round reviewed, corpus numbers refreshed

**WHAT**
- Wave 5 (commits 3d72bb96..6068aabf) closed eight Codex findings: grouped property blocks (`anchors { }`) emit no rows; inline component bodies emit `extends` from the inline class; qualified types never resolve to a same-file class; property targets honor an explicit id receiver; attached handlers name the attached type (`Keys`) as receiver; qualified `Connections` keeps handler semantics; directive lines tolerate a trailing `//` comment; ledger declares the remaining row changes. The ninth finding (owner matching by start line) is a documented limit in `docs/languages/qml.md`.
- Lead review of the diff: no findings. Omarchy re-scan confirms 0 `anchors` field rows, `returnPressed` receiver `Keys`, `SpeedDial extends Item`.
- plasma-framework at 0806864 re-measured with the final 3.2.0 build: QML symbols 3,784, resolved relationships 235, pending 1,682, field rows 1,551, pending extends 175 plus 6 resolved. `docs/languages/qml.md` and the ledger sentence now carry these numbers.

**Gates at 6068aabf (worker run)**: fmt, clippy, language qml/javascript, golden, capability, contract, default (4069 tests), strict report, release preflight 3.2.0, compat-check vs published 3.1.3 (all differences declared).

**Codex review archived** at `~/.code-kb/search-eval/reviews/2026-09-21-codex-julie-qt-branch-review.json`.

**Next**: the owner decides on merging `feat/qt-first-class` to main and releasing 3.2.0 (push, CI, tag, publish), then code-kb pins 3.2.0 with the six checksums.
