---
id: checkpoint_d0e0928d
timestamp: 2026-09-22T02:39:00.938Z
tags:
  - wave7
  - cpp
  - qt
  - golden
  - task22a
git:
  branch: feat/qt-cpp-macros
  commit: 9f256e54
  files:
    - crates/julie-extractors/src/tests/golden.rs
    - fixtures/extraction/capabilities.json
    - fixtures/extraction/cpp/qt_header/expected.json
    - fixtures/extraction/cpp/qt_header/source.h
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Wave 7 task 22a: the Kirigami-style Qt header golden"
type: checkpoint
next: "Task 22b: the cpp capability rows and the structural-fact pattern contract."
---

## Wave 7 task 22a: the Kirigami-style Qt header golden

`fixtures/extraction/cpp/qt_header/source.h` (60 lines) holds an SPDX comment, `#pragma once`, two includes, the forward declaration `class ColumnView;`, `ColumnViewAttached` with `Q_OBJECT`, `QML_ELEMENT`, `QML_ATTACHED`, three `Q_PROPERTY` lines (one plain, one pointer type, one multi-line `MEMBER ... CONSTANT FINAL`), an `enum class` with `Q_ENUM`, a constructor, a destructor, `Q_INVOKABLE`, `public Q_SLOTS:`, `Q_SIGNALS:`, a private member, then `ScrollIntentionEvent` and a plain `struct Geometry`.

`expected.json` has 21 symbols, 4 `cpp.qt_property.v1` facts and no parse diagnostics. Every acceptance item holds: four property rows with their types and metadata (`ColumnView *` keeps its star), two public event rows, `qt_slot` on `refresh`, `qt_invokable` on `reset`, `qml_element`/`qml_attached`/`qt_object` on the class, one constructor and one destructor row, no row for the forward declaration, the enum and its two members, and each fact's `containing_key` naming its class.

**One change outside the fixture.** `tests/golden.rs` detected the fixture's language from the path alone, with empty content, so every `.h` fixture resolved to `c` and the assertion "must route through its registry language" failed before the extraction ran. It now calls `detect_language_for_source(source_path, &source)`, the same content-aware detection the pipeline uses, so the assertion tests the real routing. The `.h` file was not renamed.

**Verification.** `cargo test -p julie-extractors --lib` 4132 passed, `cargo xtask test golden` 8 passed, fmt and clippy clean.
