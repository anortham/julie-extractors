---
id: checkpoint_2e182937
timestamp: 2026-08-20T02:59:09.443Z
tags:
  - release
  - v2.34.3
  - candidate
  - verification
  - compatibility
  - branch-gate
git:
  branch: fix/test-detection-precision
  commit: 1617d1c3
  files:
    - docs/plans/2026-08-19-julie-extract-2.34.3-release-prep.md
summary: Verified the local v2.34.3 release candidate
type: checkpoint
impact: Produces the final local candidate commit after the plan checkbox and
  this checkpoint are committed.
evidence:
  - Published v2.34.2 Linux asset SHA-256 verified before use.
  - "cargo xtask compat-check: 18 extraction tables byte-identical, exit 0."
  - "cargo fmt --check: exit 0."
  - "cargo test -p xtask: all tests passed."
  - "cargo xtask test default: exit 0."
  - "cargo xtask test contract: exit 0."
  - "cargo xtask release package-list: exit 0."
  - "cargo xtask release preflight --version 2.34.3: 4 targets, 35 inputs."
  - "scripts/check-agent-doc-sync.sh and git diff --check main..HEAD: exit 0."
symbols:
  - EXTRACTION_IDENTITY_EPOCH
  - COMPAT_BASELINE_EXTRACTION_IDENTITY_EPOCH
  - CURRENT_EXTRACTION_IDENTITY_EPOCH
next: Commit the final plan/checkpoint metadata, rerun metadata-sensitive
  release checks, reconcile all worktrees, then request explicit
  push/tag/publication approval.
confidence: 5
---

## Verified the local v2.34.3 release candidate

The test-detection precision branch is versioned and contract-aligned as Julie Extract v2.34.3.

- **WHAT:** Completed lead review, corrected durable release documentation, compared against the checksummed published v2.34.2 Linux binary, and passed the repository branch gate.
- **WHY:** Narrower Python decorator evidence and removal of Scala/Elixir path-only test detection change emitted `is_test` facts, requiring extraction identity epoch 3 and an explicit compatible ledger entry.
- **HOW:** v2.34.3 keeps schema 7, JSONL v5, store schema 2, and store format epoch 1; the compatibility baseline/current epoch pair is 2/3. The previous-binary fixture comparison was byte-identical across all 18 compared tables, while the broader default/contract tiers cover the changed language fixtures.
- **IMPACT:** The branch is a locally verified release candidate. Push, PR/merge, tag, and publication remain separate approval-bound actions.
