---
id: checkpoint_8289e0c4
timestamp: 2026-06-30T16:42:30.590Z
tags:
  - release
  - julie-extractors
  - v2.5.5
  - contract
  - golden-fixtures
  - vue
  - route-reference
git:
  branch: web-stack-structural-facts-bridge
  commit: 44acbea
  files:
    - docs/release-notes/v2.5.5.md
    - fixtures/extraction/vue/basic/expected.json
    - fixtures/extraction/vue/basic/source.vue
summary: Fixed v2.5.5 contract-tier failure before release
briefId: julie-extractors-best-tree-sitter-implementation-f
type: checkpoint
impact: Canonical fixture evidence now supports the vue.route_reference.v1
  capability claim.
evidence:
  - "Reproduced failure: vue claims structural fact pattern
    vue.route_reference.v1 but no fixture source emits it"
  - "UPDATE_GOLDEN=1 cargo xtask test golden: passed"
  - "cargo test -p julie-extractors --features test-capability-matrix --lib
    capability_matrix_structural_fact_claims_have_fixture_evidence --
    --nocapture: passed"
  - "cargo xtask test contract: passed"
  - "cargo fmt --check: passed"
  - "git diff --check: passed"
  - "cargo deny --manifest-path ./Cargo.toml --all-features check: passed with
    advisories ok"
  - "node scripts/language-data-quality-report.mjs --strict: passed with
    silent_cells=0 and quality_bar_debts=0"
  - "cargo xtask test default: passed"
  - "cargo xtask release preflight --version 2.5.5: passed"
next: Commit and push the fixture fix, wait for CI on the new commit, then
  publish v2.5.5.
---

## Fixed v2.5.5 contract-tier failure before release

The `2.5.5` prep CI cleared the RustSec dependency policy but failed in contract tier because the new Vue structural fact claim lacked canonical fixture evidence.

- **WHAT:** Added `<RouterLink to="/todos">` to `fixtures/extraction/vue/basic/source.vue` and regenerated `fixtures/extraction/vue/basic/expected.json` with `UPDATE_GOLDEN=1 cargo xtask test golden` so `vue.route_reference.v1` is emitted in golden fixture evidence.
- **WHY:** `capability_matrix_structural_fact_claims_have_fixture_evidence` requires every claimed structural fact pattern in `fixtures/extraction/capabilities.json` to be observed from canonical fixture source, not only unit-test inline sources.
- **HOW:** Reproduced the failing contract test locally, added the minimal fixture route literal, regenerated golden output, and reran the affected contract plus full local gates.
- **IMPACT:** The `v2.5.5` release prep can be committed/pushed again and should pass CI contract tier before publishing.
