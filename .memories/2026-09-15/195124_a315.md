---
id: checkpoint_a315d887
timestamp: 2026-09-15T19:51:24.691Z
tags:
  - release
  - 3.0.0
  - store-removal
  - jsonl-removal
git:
  branch: main
  commit: d75b9f8f
  worktree: /home/murphy/source/julie-extractors
  files:
    - docs/plans/2026-09-15-remove-store-and-jsonl.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared julie-extract 3.0.0 (store and JSONL removed)
evidence:
  - "cargo xtask release preflight --version 3.0.0: release preflight ok: 4
    targets, 27 inputs"
  - "cargo run -q -p julie-extract-cli -- --version: julie-extract 3.0.0"
  - "cargo test -p xtask: all targets ok"
---

## Prepared julie-extract 3.0.0 (store and JSONL removed)

**WHAT**
- Bumped `julie-extractors`, `julie-extract-artifact`, `julie-extract-cli` to `3.0.0`; `xtask` stays at 2.0.0; `Cargo.lock` refreshed.
- Added `docs/release-notes/v3.0.0.md` (classification: breaking; artifact output: compatible) and listed it in `docs/release-notes/README.md`.
- Left `docs/site/index.html` and `docs/release.md` for the publish step (the 2.43.0 prep commit did not touch them).

**WHY**
- Task 7 of `docs/plans/2026-09-15-remove-store-and-jsonl.md`. Tasks 2 to 6 removed the versioned family store, the JSONL export, and the Miller wording. Removing CLI commands is breaking, so the release is 3.0.0.

**HOW**
- Release note lists every removed surface from the Task 2 to 5 reports: `store` and its nested verbs, `export`, the `MILLER_*` env vars and store test hooks, the report fields and error codes, the deleted contract docs (recoverable at tag `v2.43.0`), the removed cargo features, and the dropped dependencies.
- Verified the store verb names against `v2.43.0:docs/contracts/cli.md` (`maintain cursor release`, not `maintain release`).
- Omitted the `## Default gate` section on lead instruction; the lead records timing separately.

**IMPACT**
- `cargo build --workspace`, `cargo test -p xtask`, `cargo xtask release preflight --version 3.0.0` (4 targets, 27 inputs), `package-list`, `cargo fmt --check`, `scripts/check-agent-doc-sync.sh` all pass; `julie-extract --version` prints `julie-extract 3.0.0`.
- Ready for the lead's branch gate, tag, and publish. Not tagged, not pushed.
