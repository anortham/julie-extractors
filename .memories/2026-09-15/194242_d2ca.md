---
id: checkpoint_d2caaa9d
timestamp: 2026-09-15T19:42:42.890Z
tags:
  - 3.0.0
  - store-removal
  - jsonl-removal
  - xtask
  - release-docs
git:
  branch: main
  commit: d75b9f8f
  worktree: /home/murphy/source/julie-extractors
  files:
    - docs/plans/2026-09-15-remove-store-and-jsonl.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 5: xtask and release-doc cleanup for the store and JSONL removal
  (3.0.0 step 5)"
evidence:
  - "cargo test -p xtask: all targets ok"
  - "cargo xtask release preflight --version 2.43.0: ok, 4 targets, 27 inputs"
  - "cargo clippy -p xtask --all-targets -- -D warnings: clean"
  - "cargo fmt --all -- --check: clean"
symbols:
  - release_package_items
  - release_package_list_is_exact_and_ordered
---

## Task 5: xtask and release-doc cleanup for the store and JSONL removal (3.0.0 step 5)

**WHAT**
- `xtask/src/release.rs` `release_package_items` and its exact-list test in `xtask/tests/release_contract.rs`: removed the 8 entries for deleted docs (`store-v1.md`, `sqlite-store-schema-v2.md`, `jsonl-v1..v5.md`, `versioned-index-store.md`). Manifest is now 27 inputs.
- `xtask/tests/compat_contract.rs`: renamed the synthetic `store_epochs` table in the diff test to `added`.
- `docs/testing-strategy.md`: dropped the two unreleased store test commands, the JSONL export shape bullet, the JSONL/store contract-tier sentences, the JSONL perf wording, JSONL in the dogfood gate text, and the whole `### Unreleased Ph2b store closeout` section.
- `docs/release.md` `## Evidence`: dropped JSONL from generated files and dogfood evidence. Historical entries and the current release pointer untouched.
- `docs/site/*.html`: no store or JSONL residue was present; no change.

**WHY**
Tasks 2 to 4 deleted the store and JSONL code and contracts; the release manifest still pointed at deleted files and the docs still described removed tiers.

**HOW**
Located residue with code-kb (`file_skeleton`, `get_symbol_body` on `release_package_items`, `perf_plan`, `help_text`) plus `rg -i "store|jsonl|export|MILLER"`. The xtask test tiers were already pruned; no empty tier remained.

**IMPACT**
`cargo test -p xtask`, `cargo xtask release package-list`, `cargo xtask release preflight --version 2.43.0` (4 targets, 27 inputs), `cargo fmt --all -- --check`, `cargo clippy -p xtask --all-targets -- -D warnings` all pass. Task 6 owns the Miller wording sweep; Task 7 bumps to 3.0.0.
