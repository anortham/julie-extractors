---
id: checkpoint_0ee3d4ff
timestamp: 2026-09-18T23:13:51.871Z
tags:
  - julie-extractors
  - qualified-call-chain
  - task-3
git:
  branch: qualified-call-chain
  commit: "7e540741"
  files:
    - crates/julie-extractors/src/cpp/relationships.rs
    - crates/julie-extractors/src/javascript/mod.rs
    - crates/julie-extractors/src/javascript/relationships.rs
    - crates/julie-extractors/src/tests/cpp/cross_file_pending.rs
    - crates/julie-extractors/src/tests/javascript/cross_file_pending.rs
    - crates/julie-extractors/src/tests/typescript/cross_file_pending.rs
    - crates/julie-extractors/src/tests/zig/cross_file_pending.rs
    - crates/julie-extractors/src/typescript/mod.rs
    - crates/julie-extractors/src/typescript/relationships.rs
    - crates/julie-extractors/src/zig/relationships.rs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 3: C++, Zig, JavaScript, TypeScript emit qualified call chains"
---

## Task 3: C++, Zig, JavaScript, TypeScript emit qualified call chains

**WHAT:** `cpp/relationships.rs` gains a `qualified_identifier` arm (`collect_scope_chain`, unwraps template names) and tries `from_qualified_text(text, [".", "->", "::"])` for field expressions; `zig/relationships.rs` walks nested `field_expression` with `collect_field_chain`; `javascript/relationships.rs` and `typescript/relationships.rs` gain `member_chain`, used by `extract_call_target` and by `build_unresolved_target` in both `mod.rs` files, which take `import_context` from the chain's root identifier. One new test per language in `tests/<lang>/cross_file_pending.rs`.

**WHY:** `Outer::Inner::chain()` (C++), `Outer.Inner.chain()` (Zig, JS, TS) emitted no pending row at all. Root cause in JS/TS: `should_emit_pending_call` needs `import_context`, and `member_receiver_import_context` returned `None` because the `object` node was a `member_expression`, not an `identifier`. Plan `docs/plans/2026-09-18-qualified-call-chain-receivers.md` in code-kb, Task 3.

**HOW:** a call result, `this`, or index in the chain returns `None` and the old path runs. `typescript/mod.rs` was outside the task's ownership but is the only place TS pending rows are built; the lead accepted it as a plan mismatch.

**IMPACT:** golden fixtures `cpp/cross_file` (`other_ns.do_thing` gains receiver), `cpp/basic` (`this->id` displays as `this.id`; new `std.make_unique` row), `zig/test_roles` (new `std.testing.expect` row) change; regeneration deferred to Task 6. Lead inline review clean; committed by the lead (parallel-lead-commit).
