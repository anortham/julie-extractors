---
id: checkpoint_8b5af8af
timestamp: 2026-08-11T17:34:54.458Z
tags:
  - task-1
  - scope-journal
  - manifest
  - verification
  - plan
git:
  branch: feature/store-incremental-resolution
  commit: 93a86b0
  files:
    - TODO.md
    - docs/plans/2026-08-11-store-incremental-resolution-brief.html
    - docs/plans/2026-08-11-store-incremental-resolution-brief.md
summary: WHAT
briefId: store-resolution-performance-repair
type: checkpoint
impact: Establishes the durable API and bounded writer seam all remaining
  incremental-resolution tasks consume; no open Task 1 findings remain.
evidence:
  - "Task 1 worker gate: 93 focused tests, fmt, artifact all-target/all-feature
    clippy"
  - "Lead affected-change gate at 93a86b0: 19 schema + 27 connection + 27
    manifest + 9 generation + 12 generation-equivalence + 29 CLI operations + 31
    CLI import tests passed"
  - Lead inline review fix rounds closed all six findings
symbols:
  - ResolutionScopeState
  - ResolutionScopeBatch
  - ResolutionScopeChange
  - ResolutionScopeChangeKind
  - capture_resolution_scope_transition
  - ManifestStore::publish_transaction
next: Commit plan/brief/TODO bookkeeping, then execute Task 2 predecessor
  rooting, GC, bypass handling, and crash recovery.
confidence: 5
---

## WHAT

Completed and lead-approved Task 1 of the store incremental-resolution plan. The artifact store now has the additive schema-v2 scope lifecycle, exact plan vocabulary, complete semantic transition capture, typed path kinds, canonical old/new touched names, bounded header-only fallback, and writer-side legacy upgrade.

The implementation landed in `34bae6b`, contract corrections in `3f26367`, and bounded fallback sequencing in `93a86b0`. The plan now records Task 1 acceptance and includes user-requested Task 9 for the Miller-blocking fresh-store recovery bug.

## WHY

Later scoped resolution requires a trustworthy transition chain keyed independently of reusable manifest generations. The journal must preserve exact predecessor identity, capture every semantic path change, and avoid corpus-scale symbol work when scope cannot be used.

## HOW

- `resolution_scope_state`, `resolution_scope_batches`, and `resolution_scope_journal` remain schema v2 behind feature metadata version 1.
- The private manifest publication seam atomically captures AUTOINCREMENT transitions before binding invalidation/head CAS.
- Child rows classify add/delete/replacement and store canonical names from both old and new immutable versions.
- Missing state or more than 512 changes writes a validated header-only fallback before any symbol-name hydration.
- Read-only legacy stores remain untouched; first mutating open installs the feature; promotion copies compatible catalog state and allocator high-water.
- Lead review rejected the initial version-only/wrong-vocabulary implementation and the first fix's eager fallback hydration; both were repaired with red/green regressions.

## IMPACT

Task 2 can now root predecessor base/delta tuples and make lifecycle bypasses crash-safe against a stable durable contract. The active goal also includes restoring Miller fresh-store recovery after Tasks 1-8.
