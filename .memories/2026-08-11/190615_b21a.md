---
id: checkpoint_b21a3598
timestamp: 2026-08-11T19:06:15.636Z
tags:
  - store-incremental-resolution
  - task-5
  - resolution-session
  - overlay-materialization
git:
  branch: feature/store-incremental-resolution
  commit: dd76557
  files:
    - crates/julie-extract-cli/src/store/mod.rs
    - crates/julie-extract-cli/src/store/resolution_session.rs
    - crates/julie-extract-cli/tests/resolution_session_contract.rs
    - crates/julie-extract-cli/tests/store_resolution_mechanism.rs
    - docs/plans/2026-08-11-store-incremental-resolution-brief.md
summary: WHAT
briefId: store-resolution-performance-repair
type: checkpoint
impact: Completes Task 5 and freezes exact scratch semantics for Task 6.
evidence:
  - "cargo test -q -p julie-extract-cli --features
    test-store-resolution-contract --test store_resolution_mechanism: 12 passed"
  - "cargo test -q -p julie-extract-cli --features
    test-store-resolution-contract --test resolution_session_contract: 6 passed"
  - "cargo clippy -p julie-extract-cli --all-targets --all-features -- -D
    warnings: passed"
  - "cargo fmt --all -- --check and git diff --check: passed"
  - Independent read-only review findings closed; lead re-review clean
symbols:
  - StoreScratchResolutionSession::open_resolution_pass
  - StoreScratchResolutionSession::materialize_prior_overlay
  - StoreScratchResolutionSession::next_phase_chunk
  - StoreScratchResolutionSession::finish_exact
next: Commit the four Task 5 files, plan acceptance, and this checkpoint; then
  execute Task 6 forced-full wiring, telemetry, and differential equivalence.
unknowns:
  - Miller remains unavailable because missing-store recovery publishes partial
    reference resolution; Task 9 owns the outage.
---

## WHAT

Implemented and lead-approved Task 5's scoped store resolution session and full exact overlay materialization in the CLI store path.

## WHY

The unchanged generic `resolve_delta` runner needs bounded store-backed worklists and predecessor hydration while exact publication still requires a complete canonical file where scratch results win and removed sources never return.

## HOW

The session now validates and pins the rooted predecessor overlay, honors forced-full requests before delta planning, freezes scoped phases, hydrates prior resolved rows through bounded name/file pages, consults scratch plus prior ownership, and materializes only current visible roots before unchanged target validation. Scratch touch tables preserve tombstones and precedence across phase pages. Review fixes added selected-version receiver coverage, current-root filtering, multi-page pending checks, materialized-relationship ownership, forced-full prior clearing, and unchanged public error variants.

## IMPACT

The store session can run the existing delta algorithm unchanged and emit a full-corpus exact resolution file with bounded reads, stale-target rejection, identifier totality, and no caller-facing API expansion. This unblocks Task 6 wiring and the forced-full differential oracle.
