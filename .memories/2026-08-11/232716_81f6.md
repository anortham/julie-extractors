---
id: checkpoint_81f68233
timestamp: 2026-08-11T23:27:16.042Z
tags:
  - release
  - v2.32.0
  - windows-ci
  - store-import
git:
  branch: main
  commit: 096886b
  files:
    - crates/julie-extract-cli/tests/store_import_contract.rs
summary: Exposed the Windows store-import failure payload
type: incident
context: The first v2.32.0 main CI run failed only in the Windows local-path
  store-import probe. The CLI child exited 1, stderr was empty, and the test
  discarded JSON failure output from stdout.
impact: Unblocks evidence-based diagnosis without weakening the Windows release gate.
evidence:
  - "CI run 31545868492: Extractor Compatibility passed; Windows Capacity Store
    Probe failed at local-path store import"
  - Focused Linux regression passed 1/1
  - cargo fmt --all -- --check passed
  - git diff --check passed
symbols:
  - public_store_import_reaches_the_production_executor
next: Commit and push the diagnostic, inspect the new Windows stdout payload,
  implement a failing regression and production fix, then rerun release gates.
unknowns:
  - The exact Windows CLI failure remains unknown until the rerun logs include
    stdout.
---

## Exposed the Windows store-import failure payload

### WHAT

Updated `public_store_import_reaches_the_production_executor` to include both child stdout and stderr when its exit-code assertion fails.

### WHY

`julie-extract --json` reports failures on stdout. The Windows CI assertion printed only stderr, hiding the error needed to diagnose the release-blocking failure.

### HOW

Kept production behavior unchanged and added boundary diagnostics only to the exact failing caller-facing test. The focused test, formatting, and diff checks pass locally.

### IMPACT

A new Windows CI run will reveal the actual structured error. The v2.32.0 tag remains intentionally withheld until the root cause is fixed and all CI gates pass.
