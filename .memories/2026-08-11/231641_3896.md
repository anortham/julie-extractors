---
id: checkpoint_38968299
timestamp: 2026-08-11T23:16:41.044Z
tags:
  - release
  - julie-extract
  - v2.32.0
  - main
  - incremental-resolution
git:
  branch: main
  commit: f8d3d0a
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
summary: Prepared julie-extract v2.32.0 on local main
type: checkpoint
impact: Unblocks committing release metadata, pushing main, tagging v2.32.0, and
  verifying published assets.
evidence:
  - "cargo xtask test default: exit 0"
  - "cargo xtask test contract: exit 0"
  - "cargo clippy --workspace --all-targets --all-features --no-deps -- -D
    warnings: exit 0"
  - "cargo test -p xtask: 99 passed"
  - "cargo xtask release preflight --version 2.32.0: 4 targets, 32 inputs"
  - "cargo deny --all-features check: advisories/bans/licenses/sources ok"
  - "gitleaks detect --no-banner --redact: 762 commits, no leaks"
  - Release binary reports julie-extract 2.32.0
  - "Staged Linux SHA-256 verification: OK"
symbols:
  - julie-extract 2.32.0
  - release preflight
  - Release Binaries
next: Commit the three manifests, Cargo.lock, and this checkpoint; push main;
  tag and push v2.32.0; monitor the Release Binaries workflow; verify assets;
  record publication evidence.
---

## Prepared julie-extract v2.32.0 on local main

### WHAT

- Fast-forwarded local `main` from `692ef4e` to the completed incremental-resolution feature at `f8d3d0a`.
- Bumped all three release crates and their lockfile package entries from `2.31.4` to `2.32.0`.
- Built and staged the Linux `julie-extract` release package.

### WHY

The scoped family-store resolution implementation is complete and the user approved local integration and publication as the next minor release.

### HOW

- Preserved the existing local capacity-fix commit and used a linear fast-forward; no history rewrite or conflict resolution.
- Reused the completed performance/Miller replay evidence because release prep changes only version metadata.
- Ran fresh merged-tree release gates for the declared `2.32.0` artifacts.

### IMPACT

`main` is ready for a release-preparation commit, push, and `v2.32.0` tag. The GitHub Release Binaries workflow will build four platform archives after the tag is pushed. Miller impact inspection remains unavailable because its bootstrap still rejects the workspace's partial legacy resolution metadata.
