---
id: checkpoint_5eb77bf3
timestamp: 2026-08-27T13:38:42.535Z
tags:
  - release
  - v2.37.2
git:
  branch: main
  commit: 50bf4ee5
  files:
    - .github/workflows/release-binaries.yml
    - .github/workflows/specialist-gates.yml
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
    - docs/release-notes/README.md
    - docs/release-notes/v2.37.2.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared v2.37.2 release candidate
---

## Prepared v2.37.2 release candidate

### WHAT
Advanced the three publishable crates, Cargo.lock, and both workflow version defaults to 2.37.2. Wrote `docs/release-notes/v2.37.2.md` (classification: compatible) and indexed it in `docs/release-notes/README.md`.

### WHY
User approved releasing the vanished-file delta fix (commit 50bf4ee5) so Miller can pick up a pinned julie-extract with the new store update semantics.

### HOW
Mirrored the v2.37.1 prep commit shape (1bb2956d). Gate plan mirrors the v2.37.1 evidence chain: fmt, xtask tests, default, contract, certification (Cargo.lock and julie-extractors/Cargo.toml are certification-trigger paths even for a version-only change), strict clippy, data-quality strict, package-list, doc-sync, git diff --check, Windows default suite on the win-test guest, preflight, gitleaks + cargo audit + cargo deny. Real-world tiers skipped: store request path fix, no extractor changes.

### IMPACT
Next: prep commit, run long gates at that SHA, push main, tag v2.37.2, verify the Release Binaries workflow, then closeout (evidence doc + pointer advances).
