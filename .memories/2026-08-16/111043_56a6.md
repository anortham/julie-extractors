---
id: checkpoint_56a69828
timestamp: 2026-08-16T11:10:43.263Z
tags:
  - performance-recovery
  - julie-extract
  - windows
  - linux
  - clippy
  - path-identity
git:
  branch: feature/miller-performance-recovery-producer
  commit: 37c81e5f
  files:
    - crates/julie-extract-artifact/tests/store_manifest_contract.rs
    - crates/julie-extract-cli/src/store/import.rs
summary: Closed Linux strict gates for Windows store-path hardening
type: checkpoint
evidence:
  - "cargo fmt --all -- --check: passed"
  - "strict artifact+CLI all-target/all-feature Clippy with -D warnings: passed"
  - "store_manifest_contract: 27/27"
  - "import capacity tests: 4/4"
  - "store_resolution_contract: 30/30"
  - "Miller exact-current Scale against rebuilt producer: 138 passed, 10 skipped"
symbols:
  - same_path_identity
  - root_scope_matches
next: Commit these two source/test corrections plus this checkpoint on
  feature/miller-performance-recovery-producer. Push only after renewed user
  approval for the changed verified state.
confidence: 5
---

## Closed Linux strict gates for Windows store-path hardening

### WHAT

Corrected two Linux-only strict-Clippy failures introduced by the accepted Windows path-identity changes.

### WHY

The Windows branch passed its native contracts, but exact-current Linux `-D warnings` found a Windows-only helper imported unconditionally and a collapsible nested canonical-path check.

### HOW

- Gated the `same_path_identity` test import with `cfg(windows)`.
- Collapsed the canonicalized path comparison to a behavior-identical Rust let-chain.

### IMPACT

The producer branch now retains Windows drive/UNC/verbatim-path behavior while passing Linux formatting, strict all-target/all-feature Clippy, manifest, import, and full resolution contracts. Miller Scale also passed against a binary rebuilt from this exact source tree.
