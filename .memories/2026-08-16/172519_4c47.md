---
id: checkpoint_4c47cc28
timestamp: 2026-08-16T17:25:19.853Z
tags:
  - checkpoint
  - plan
  - performance
  - store-resolution
  - delta-scope
  - rebase
  - worktree
  - tdd
git:
  branch: perf/accumulated-resolution-work-rebase
  commit: d4fad6cc
  files:
    - docs/plans/2026-08-16-accumulated-resolution-work-rebase-design.md
    - docs/plans/2026-08-16-accumulated-resolution-work-rebase-implementation.md
summary: Approved design moved into an isolated implementation worktree and planned
type: checkpoint
impact: Ready for explicit implementation-plan approval; execution will use
  serialized luna_worker tasks with lead inline review.
evidence:
  - "Baseline `cargo test -p julie-extract-cli --test
    store_delta_scope_contract`: 12 passed, 0 failed."
  - "`cargo check -p julie-extract-cli --features
    test-store-resolution-contract`: passed."
  - Plan/design placeholder scans returned no matches.
  - Plan/design whitespace checks returned no errors.
  - Luna read-only plan audit reconciled three findings and made no file changes.
  - Miller worktree registration itself blocked for over three minutes and was
    terminated; plan orientation used the fresh main index at the identical
    source commit.
symbols:
  - scope_crosses_over
  - validated_scope_changes
  - StoreDeltaScopeDecision
  - StoreResolutionDecisionTelemetry
  - resolve_claimed
  - exact_rebase_required_with_proof
  - store_resolution_performance_gate
next: Wait for the user to approve the implementation plan, then execute it
  immediately with razorback:subagent-driven-development and reviewer_choice
  none.
confidence: 5
---

## Approved design moved into an isolated implementation worktree and planned

### WHAT

Moved the approved accumulated-resolution rebase design and its prior checkpoint off dirty `main` into the clean managed worktree `/home/murphy/source/julie-extractors/.worktrees/fix-store-resolution-query-amplification`, created branch `perf/accumulated-resolution-work-rebase` from `main` at `d4fad6cc`, and added `docs/plans/2026-08-16-accumulated-resolution-work-rebase-implementation.md`.

### WHY

The implementation touches store scope, session, rebase publication, and performance harness code. It must remain isolated from four unrelated dirty source/test files on `main` and from two unmerged sibling branches.

### HOW

- Plan has three serialized TDD tasks: unique accumulated-scope policy and private telemetry; validated fold into existing rebase publication; deterministic 79-transition performance evidence.
- Existing inclusive 70% duplicated-read admission stays unchanged; new strict >25% unique-identifier rule requires more than one validated transition.
- Public Rust APIs, `ResolutionWorklists`, durable telemetry/reports, SQLite, Store Contract, CLI, and Miller remain unchanged.
- Corrected the approved design after source verification: durable payload omission is asserted because its manual decoder does not reject unknown keys.
- Luna plan audit found missing `test-store-resolution-contract` feature flags, a legacy-only test seam, and an incomplete performance-file range; all three were corrected.

### IMPACT

The isolated plan is ready for the final human plan-approval gate. No production code has changed and no commit, push, or release has occurred.
