---
id: checkpoint_73c62d0f
timestamp: 2026-08-16T21:47:47.767Z
tags:
  - julie-extract
  - test-fix
  - ci
  - resolve-waiter
git:
  branch: perf/accumulated-resolution-work-rebase
  commit: f6037134
  files:
    - crates/julie-extract-cli/tests/store_resolution_contract.rs
summary: Correct committed waiter timing observation
type: checkpoint
evidence:
  - "Five exact repetitions passed: cargo test -p julie-extract-cli --features
    test-store-resolution-contract --test store_resolution_contract
    committed_resolve_waiter_reports_durable_success_before_request_timeout --
    --exact."
  - "Full feature-enabled store_resolution_contract passed: 33 tests."
  - cargo fmt --check passed.
  - git diff --check and git diff --check origin/main passed before commit.
next: Commit the test-only correction locally, then verify origin/main diff
  whitespace and clean status.
confidence: 5
---

## Correct committed waiter timing observation

- **WHAT:** Updated `committed_resolve_waiter_reports_durable_success_before_request_timeout` to open `coord.db`, resume the first resolver, poll until the durable request row reaches `committed`, and only then start the one-second waiter latency timer. The waiter is checked for premature exit during the bounded observation loop.
- **WHY:** Windows CI measured the first resolver's remaining publication work plus waiter exit because the timer started before resuming the paused resolver. The one-second threshold itself remains unchanged.
- **HOW:** Followed the adjacent failed-state test's bounded SQLite observation pattern, with a 30-second deadline and 10ms polling interval; no production code or comments changed.
- **IMPACT:** The test now measures only the intended post-terminal-transition waiter response and avoids false failures from platform scheduling/publication variance.
