---
id: checkpoint_64879866
timestamp: 2026-06-02T20:03:26.064Z
tags:
  - julie-extractors
  - release
  - perf
  - ci
git:
  branch: main
  commit: 70444db
  files:
    - crates/julie-extract-artifact/src/writer.rs
    - crates/julie-extract-artifact/tests/writer_performance.rs
    - docs/release-evidence/2026-06-02-v2-0-1-release-candidate.md
    - docs/release-notes/v2.0.1.md
summary: Fixed v2.0.1 Fast Gates failure
briefId: julie-extractors-product-completion-focus
type: checkpoint
context: GitHub CI Fast Gates failed on the v2.0.1 release commit after the new
  writer performance test exceeded its wall-clock budget on the hosted runner.
impact: Unblocks pushing the fast-gate fix and re-running the v2.0.1 release
  workflow from a CI-clean commit.
evidence:
  - "cargo fmt --check: passed"
  - "cargo metadata --format-version 1: passed"
  - "cargo clippy --no-deps -p julie-extract-artifact -p julie-extract-cli -p
    xtask --lib --bins -- -D warnings: passed"
  - "cargo test -p julie-extract-artifact -- --nocapture: passed"
  - "cargo test -p xtask: passed"
  - "cargo xtask test default: passed"
  - "cargo xtask test contract: passed"
  - "git diff --check: passed"
  - "scripts/check-agent-doc-sync.sh: passed"
symbols:
  - ArtifactWriter::write_files
  - ArtifactWriter::write_scan_snapshot
  - ArtifactWriter::write_scan_spooled_snapshot
  - FileRowInserters
  - ChildRowInserters
next: Commit and push the fix, wait for GitHub CI Fast Gates, then rerun Release
  Binaries for v2.0.1 so assets are rebuilt from the fixed commit.
confidence: 5
---

## Fixed v2.0.1 Fast Gates failure

- **WHAT:** Reworked the artifact writer hot path to use transaction-scoped file-row and child-row prepared statement sets, added a deterministic unit test that verifies one prepare-set per scan transaction, and calibrated the large child-row timing tripwire for GitHub runner variance.
- **WHY:** The v2.0.1 release commit published successfully, but CI Fast Gates failed because the new writer performance test measured 1.619879239s on GitHub against a 1.25s threshold. The test was catching runner speed variance, while the code still had avoidable per-file statement-cache lookups.
- **HOW:** Introduced `FileRowInserters` and `ChildRowInserters` in `crates/julie-extract-artifact/src/writer.rs`, hoisted hot insert/update statements once per transaction, kept row-count behavior unchanged, added prepare-set counters under `cfg(test)`, and updated release evidence/release notes.
- **IMPACT:** Local exact CI fast-gate scope is green, the default and contract tiers pass, and the release can be re-run so v2.0.1 binaries match the fixed source commit.
