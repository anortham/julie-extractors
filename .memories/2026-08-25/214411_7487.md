---
id: checkpoint_7487458a
timestamp: 2026-08-25T21:44:11.339Z
tags:
  - ct
  - adversarial-review
  - test-detection
  - fix-round
git:
  branch: fix/adversarial-review-findings
  commit: "68972961"
  files:
    - crates/julie-extract-cli/src/discovery.rs
    - crates/julie-extractors/src/javascript/test_symbols.rs
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/test_detection.rs
    - crates/julie-extractors/src/tests/api_surface.rs
    - crates/julie-extractors/src/tests/javascript/test_detection.rs
    - crates/julie-extractors/src/tests/python/test_detection.rs
    - crates/julie-extractors/src/tests/test_detection.rs
    - docs/decisions/2026-08-20-test-role-contract-closure.md
    - docs/languages/javascript.md
    - docs/languages/python.md
    - fixtures/extraction/javascript/jest_vitest_roles/expected.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Adversarial review round: six confirmed findings fixed"
type: checkpoint
next: Run Windows default suite on the fix branch, merge locally, ask for push
  approval.
---

## Adversarial review round: six confirmed findings fixed

**WHAT:** A user-requested grok-4.6 adversarial review of the merged CT test-detection work (8d7f37c6..63a6b410) returned six findings. Lead verification confirmed all six. All are fixed on branch fix/adversarial-review-findings.

**WHY:** The original run had no external review (user chose lead-only inline review). The user asked for an adversarial pass before push.

**HOW / fixes:**
- Oversize cap now fires before language routing in `select_file`; unsupported-extension files are no longer read whole for hashing.
- Python lifecycle names (`setUp`, `setup_method`, ...) are gated on `is_test_path`; production hooks earn no role.
- `describe.each` reports `test_container`; only case words with `.each` report `parameterized_test`.
- Bare `integration/` removed from TEST_DIRECTORY_SEGMENTS; `cypress/` still covers the Cypress layout.
- `detect_js_ts` splits on PATH_SEPARATORS, not only `/`.
- `api_surface` locks the test-role marker; marker bumped `test-role-strings-v1` → `v2` (golden output changed).

**Evidence:** Linux branch gate green on the fix branch (FIX-BRANCH-LINUX-GATE-GREEN). Golden `javascript/jest_vitest_roles` regenerated. Docs corrected (closure decision doc, javascript.md, python.md).

**Incident:** the first grok invocation silently truncated the 694KB embedded diff (~24k input tokens, zero tool calls) and returned an empty findings shell. Recovery: resume the session and have grok read the diff from the repo itself. Lesson: never embed large diffs in grok prompts — point it at the repo range.

**IMPACT:** Windows suite pending, then local merge to main. Push still needs user approval. The SDD ledger was lost with the worktree removal (git-ignored scratch) and was recreated at .razorback/sdd/ with a recovery note.
