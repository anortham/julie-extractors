---
id: checkpoint_64ac3ea5
timestamp: 2026-06-09T21:02:50.759Z
tags:
  - julie-extractors
  - release
  - v2.2.1
  - aspnet
  - htmx
  - alpine
git:
  branch: codex/aspnet-htmx-alpine-structural-facts
  commit: f0954d4
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
    - crates/julie-extractors/src/base/framework_structural_facts.rs
    - docs/release-notes/README.md
    - docs/release-notes/v2.2.1.md
summary: Prepared julie-extractors v2.2.1 release commit
briefId: julie-extractors-product-completion-focus
type: checkpoint
impact: Unblocks committing and publishing `v2.2.1` from the verified
  release-prep tree.
evidence:
  - "cargo fmt --check: pass"
  - "cargo metadata --format-version 1 --no-deps: pass"
  - "scripts/check-agent-doc-sync.sh: pass"
  - "git diff --check: pass"
  - "cargo test -p xtask: pass"
  - "cargo test -p julie-extractors tests::structural_facts:: -- --nocapture:
    pass"
  - "cargo check --workspace --all-targets --all-features: pass"
  - "cargo clippy --workspace --all-targets --all-features --no-deps -- -D
    warnings: initially failed on collapsible_if, then passed after minimal
    syntax fix"
  - "cargo deny check: pass with existing duplicate/wildcard warnings"
  - "cargo xtask test default: pass on final source"
  - "cargo xtask test contract: pass on final source"
  - "cargo xtask test certification && cargo xtask test real-world-smoke &&
    cargo xtask test real-world-release: pass on final source"
  - "cargo xtask release package-list: pass"
  - "cargo xtask release preflight --version 2.2.1: pass"
  - "cargo build --release -p julie-extract-cli --bin julie-extract: pass"
  - "target/release/julie-extract --version: julie-extract 2.2.1"
  - "cargo xtask dogfood repo --root . --out-dir
    target/dogfood/julie-extractors-v2.2.1-pre-release --binary
    target/release/julie-extract: pass; schema/contract/jsonl v3, 1099 files,
    36878 symbols, 25 structural_facts"
symbols:
  - collect_markup_framework_attributes
  - framework_structural_facts.rs
next: Commit release prep, push to origin main, dispatch `release-binaries.yml`
  for version `2.2.1`, verify live release assets, then update README and
  release evidence from live GitHub facts.
---

## Prepared julie-extractors v2.2.1 release commit

### WHAT
- Bumped `julie-extractors`, `julie-extract-cli`, and `julie-extract-artifact` crate versions from `2.2.0` to `2.2.1`.
- Updated `Cargo.lock` workspace package versions to `2.2.1`.
- Added `docs/release-notes/v2.2.1.md` and listed it in `docs/release-notes/README.md`.
- Fixed two clippy-only nested-if warnings in `framework_structural_facts.rs` without changing structural fact behavior.

### WHY
- The user explicitly authorized a patch release for the ASP.NET minimal API, htmx, and Alpine structural-facts feature.
- The release workflow requires crate versions and release notes for the target version before publishing assets.

### HOW
- Kept the v3 extraction contract unchanged and documented the release as a patch release.
- Followed the repo release docs and prior julie-extractors release evidence pattern: live README/evidence updates are deferred until after GitHub publishes the release.
- Ran release gates on the isolated feature worktree at branch `codex/aspnet-htmx-alpine-structural-facts`.

### IMPACT
- The tree is ready for a release-prep commit, push to `main`, workflow dispatch for `v2.2.1`, then live release verification and evidence docs.
