---
id: checkpoint_0181ec2e
timestamp: 2026-08-15T05:42:04.335Z
tags:
  - performance-recovery
  - benchmark
  - store-resolution
  - coverage-batching
git:
  branch: feature/miller-performance-recovery-producer
  commit: 5f51809b
summary: WHAT
type: learning
impact: Proves coverage batching recovered most of the measured N+1 cost without
  semantic drift and narrows the next implementation to scoped exact
  finalization.
evidence:
  - "Evidence directory: /home/murphy/.miller/task8f-coverage-replay.rexnsc"
  - Public child exit 0; timed_out=false; hard_gate_passed=false.
  - "Phase timings: scope 563 ms, resolution 7,978 ms, diff 1,072 ms."
  - Peak Linux PSS 192,773,120 bytes, below 600 MiB gate.
  - All safe captured input hashes/SQLite quick-checks and staged pointer
    remained unchanged.
  - Worktree clean at 5f51809b after replay.
symbols:
  - ResolutionSession.propagation_is_covered_batch
  - StoreScratchResolutionSession.finish_exact
next: Design the deterministic RED for direct scoped delta finalization,
  preserve forced-full finish_exact as oracle, then prove scoped/full canonical
  parity.
unknowns:
  - The post-batching coverage sub-boundary was not re-profiled because the
    temporary aggregate probe was intentionally removed.
  - Peak PSS increased relative to the prior run but remains within the hard
    gate; re-evaluate after finalization and repeated strict replay.
---

## WHAT

Rebuilt Julie commit `5f51809b` in Release and ran the public production-volume `producer.resolve.one_file` harness against isolated captured inputs. The scoped exact run completed in 14,506 ms wall with a 7,978 ms resolution phase.

## WHY

The batching slice was intended to remove the measured 4.230-second scalar propagation-coverage owner before changing the independent full exact finalization path. A public replay was required to prove the optimization was causal and semantics remained exact.

## HOW

Used the Miller performance-recovery harness with fresh request identities, a temporary family clone, the exact Release producer (`julie-extract 2.33.2`, SHA-256 `d4658b17…37de4`), semantic work disabled, and guarded captured workspace/store inputs. No profiling probe was enabled. The harness changed one README file, imported without resolution, then invoked the public `store resolve` command.

## IMPACT

Resolution improved from 11,508 ms to 7,978 ms (−3,530 ms, −30.67%); wall improved from 16,462 ms to 14,506 ms (−1,956 ms, −11.88%). Mode remained scoped, state exact, scope one file, and `exact_at_matches=true`. The 5-second hard gate still fails, confirming the separate 5.562-second full exact finalization owner must be removed rather than tuning batching further.
