---
id: checkpoint_6a2a55d3
timestamp: 2026-08-15T22:44:33.444Z
tags:
  - performance-recovery
  - integration
  - producer
  - pre-commit
git:
  branch: feature/miller-performance-recovery-producer
  commit: 6ad9fdfc
  files:
    - crates/julie-extract-cli/src/store/resolution_session.rs
    - crates/julie-extract-cli/tests/store_maintenance_cli_contract.rs
summary: WHAT
type: milestone
---

## WHAT
Closed the exact producer Linux branch gate after the main recovery commit. Two integration corrections remain staged for a follow-up commit: the maintenance retention snapshot now reflects the two new SQLite index root pages, and four recovery-only instrumentation helpers are gated to `test-store-resolution-contract` so default builds are warning-free.

## WHY
The default tier correctly failed because the schema-v2 parent/name and type-fact indexes increased the deterministic empty store from 143 to 145 4KB pages. Production retention math was correct; only five snapshot values were stale. Plain release builds also exposed four helpers used only by feature-gated tests.

## HOW
Systematic debugging proved the +8192 bytes exactly equals two 4096-byte index roots; target/ceiling remain the intended 1.20/1.25 ratios. The focused snapshot was RED then GREEN. Test-helper cfg gates preserved permanent telemetry and passed plain check, focused reuse, strict all-feature Clippy, format, and diff checks. The full producer branch gate then passed: xtask 120/120, default 3,245+ package tests, 412 contract tests, resolution supplements 20/9/17, and official three-run full/scoped performance.

## IMPACT
Official medians are 29,522 ms full and 11,002 ms scoped; all semantic/applied/row differences are zero and all G1–G6 gates pass. The two-file correction is ready to commit before rebuilding the exact final binary for Miller Linux integration.
