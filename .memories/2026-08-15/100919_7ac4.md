---
id: checkpoint_7ac42ec9
timestamp: 2026-08-15T10:09:19.050Z
tags:
  - bug-fix
  - test
  - rust
  - cargo
  - feature-gating
  - compile-gate
  - prior-overlay
git:
  branch: feature/miller-performance-recovery-producer
  commit: 692a3adc
  files:
    - crates/julie-extract-cli/tests/store_prior_overlay_contract.rs
summary: Fixed default-profile compilation for the prior-overlay contract
type: checkpoint
context: Default `cargo test -p julie-extract-cli --test
  store_prior_overlay_contract -- --test-threads=1` failed with E0599 because
  the test called `find_ready_with_proof_for_test` without the feature that
  exposes it.
decision: Gate only the test and helper that require the test-store-resolution
  contract, preserving the private production hook and all other default tests.
alternatives:
  - Make the proof producer public unconditionally — rejected because it weakens
    the production API boundary.
  - Gate the entire test file — rejected because the other eight default-profile
    tests must remain covered.
impact: Unblocks the default CLI test compilation while retaining
  feature-specific coverage for the strict-reader fallback contract.
evidence:
  - "Default focused test: 8 passed, 0 failed."
  - "Feature-enabled exact mismatch test: 1 passed, 0 failed."
  - "cargo clippy -p julie-extract-cli --all-targets --all-features -- -D
    warnings: passed."
  - "cargo fmt --all -- --check: passed."
  - "git diff --check: passed."
symbols:
  - validated_base_catalog_mismatch_falls_back_through_strict_reader
  - Fixture::validated_base
  - find_ready_with_proof_for_test
next: Lead agent should review the two-file worktree state, then decide whether
  and how to commit; no commit was made in this packet.
---

## Fixed default-profile compilation for the prior-overlay contract

- **WHAT:** Feature-gated `validated_base_catalog_mismatch_falls_back_through_strict_reader` and its `Fixture::validated_base` helper under `test-store-resolution-contract`; removed imports that were unused in the default profile and qualified the catalog/factory inside the gated helper.
- **WHY:** The default CLI integration target compiled a call to the artifact-only `find_ready_with_proof_for_test` hook, which is exposed only by `julie-extract-artifact/test-store-resolution`.
- **HOW:** Kept the production proof producer private and changed only `crates/julie-extract-cli/tests/store_prior_overlay_contract.rs`.
- **IMPACT:** Default compilation/tests and the feature-enabled strict-reader mismatch regression can run without weakening the production visibility boundary.
