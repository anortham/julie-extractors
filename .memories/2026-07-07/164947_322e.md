---
id: checkpoint_322e742d
timestamp: 2026-07-07T16:49:47.953Z
tags:
  - dead-code
  - variable-ref
  - plan-review
  - julie-extractors
git:
  branch: main
  commit: d930685
  files:
    - docs/plans/2026-07-07-variable-ref-identifier-emission-plan.md
summary: Lead self-review of the variable_ref emission plan — 6 findings, plan
  amended to rev 2
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: decision
decision: Amend the plan in place (rev 2) rather than execute as-written or
  rewrite from scratch — the architecture held up under review; only contract
  edges, coverage, and gate honesty needed fixes.
next: "User approval to execute the amended plan (Task 1: C# reference
  implementation)."
---

## Lead self-review of the variable_ref emission plan — 6 findings, plan amended to rev 2

Reviewed `/Users/murphy/source/julie-extractors/docs/plans/2026-07-07-variable-ref-identifier-emission-plan.md` against real code before execution (user asked for a careful pre-flight after the dead-code gate failure burned a full build cycle).

**Verified accurate:** r/qml template arms exist; `ReferenceKind::from_identifier_kind` maps only call/type_usage/member_access (variable_ref stays non-resolvable); CLI at 2.9.0; C# helper filters exist; Miller's `DeadCodeCandidateReader` name-count has NO kind filter (new rows consumed with zero Miller changes), BINARY/case-sensitive, backed by `idx_identifiers_name_kind`.

**Findings folded into the plan:**
1. **Prior art wrong** — powershell already emits variable_ref (`$var` via variables.rs) and yaml emits it for aliases; bash emits `$var` expansions as member_access (name-visible already). Batch E/F notes added.
2. **Coverage gap** — bash/json/toml/markdown were silently absent; added to Batch F as assessments.
3. **Evidence framing** — the 328→~28 result was measured with Miller-side rescue signals (pending_relationships receiver + same-file lexical scan), NOT a variable_ref prototype; equivalence is inference until the Downstream gate re-run. Divergence-diagnosis step added.
4. **Contract gap (biggest)** — rule 4's write-target exclusion would falsely kill object-initializer members (`new Foo { Bar = 5 }`), attribute named args, and compound assignments; rule 1/4 amended to emit for those; C# fixture cases added; write-only-member residual + comment-only-mention residual codified as gate classification rules (true finds, not gate failures).
5. **Perf gate had no teeth** — `xtask performance` is baseline-only, no identifier budget. Task 8 now records before/after vs the 2.9.0 Miller baseline (94,721 identifier rows: 52,498 call / 26,271 member_access / 15,952 type_usage; 237 MB artifact) with explicit escalation thresholds (>5× rows or >2× scan time).
6. **Recall cost documented** — local-read flooding masks same-named internal symbols (worst in snake_case languages); accepted precision-first, noted so a short candidate list isn't read as exhaustive.

**Impact:** plan is now grounded and execution-ready; awaiting user approval to dispatch Task 1.
