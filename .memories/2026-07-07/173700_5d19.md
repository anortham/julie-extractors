---
id: checkpoint_5d19fba9
timestamp: 2026-07-07T17:37:00.402Z
tags:
  - variable-ref
  - perf-regression
  - dead-code
  - sqlite
git:
  branch: feat/variable-ref-emission
  commit: "3941274"
summary: "Phase boundary: Task 1 approved + scan regression root-caused, fix in flight"
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: checkpoint
next: Review perf-fix worker output; then unpark batches 2–7.
---

## Phase boundary: Task 1 approved + scan regression root-caused, fix in flight

**Task 1 (C# variable_ref reference impl): APPROVED & committed** (178e6d0 + plan-tick 3941274 on feat/variable-ref-emission). csharp 137/0, golden clean, RED-first. Locked contract doc-comment + fixture-regen procedure (`UPDATE_GOLDEN=1 cargo test -p julie-extractors --features test-golden --lib golden_fixtures_match_canonical_extraction`) recorded in .razorback/sdd/task-1-report.md. Accepted deviation: out/ref arg slots emit as reads (safe direction; locals never candidates) — plan rule 4 aligned.

**v2.9.0 scan regression ROOT-CAUSED** (user-directed priority; batches 2–7 parked until fixed):
- 6.5s (v2.8.0) → 425.9s (v2.9.0) full scan, same repo; v2.9.0 release evidence dropped the scan-duration line so it shipped unseen.
- `sample`: 99.9% of CPU in SQLite `memjrnlTruncate`. Cause: `SAVEPOINT resolution_hook` (writer.rs ~1270) held open across ~125k per-row statements (62,378 upserts + per-row denormalizing UPDATEs + pending writes) — each statement end truncates the in-memory sub-journal, each truncate walks the growing chunk list → quadratic.
- Experiment (savepoint removed, same binary/repo): **10.3s, full row parity** — hypothesis confirmed.
- Why the lane's perf harness missed it: it times resolve_workspace directly, bypassing the savepoint seam.
- Fix dispatched (branch fix/resolution-savepoint-perf, worktree): collect-then-flush batched writes inside the kept savepoint; must map mid-pass read-after-write deps (demotion→fill); adds a RED-first perf test THROUGH the hook seam; hard gate scan ≤20s with row parity.

**Follow-ups queued:** Task 8 must restore the Scan-duration line to release evidence + use the honest ~6.5s baseline (not the regressed ~4min I originally wrote into the plan).
