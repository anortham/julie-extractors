---
id: checkpoint_ec34d637
timestamp: 2026-07-07T12:36:41.250Z
tags:
  - julie-extractors
  - reference-resolution
  - pre-release-review
  - jsonl-contract
  - dead-code
git:
  branch: main
  commit: 7fe9844
  files:
    - crates/julie-extract-artifact/src/jsonl.rs
    - crates/julie-extract-artifact/tests/jsonl_contract.rs
    - crates/julie-extract-cli/tests/cli_contract.rs
    - docs/contracts/jsonl-v3.md
    - docs/contracts/reports.md
    - docs/contracts/sqlite-schema-v4.md
    - docs/release-notes/v2.9.0.md
summary: Pre-release gap review of v2.9.0 — findings fixed
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: checkpoint
evidence:
  - "cargo test -p julie-extract-artifact --test jsonl_contract: 9 passed"
  - "cargo test -p julie-extract-cli --test cli_contract: 10 passed"
  - "cargo test -p julie-extract-artifact + -p julie-extract-cli full: passed"
  - "cargo clippy -p julie-extract-artifact -p julie-extract-cli --all-targets
    -D warnings: clean"
  - "cargo fmt --all -- --check: clean"
  - "node scripts/language-data-quality-report.mjs --strict: silent_cells 0,
    quality_bar_debts 0"
next: Commit the gap fixes, then user decides push + release-prep (version bump
  to 2.9.0) + Miller pin-bump slice.
confidence: 5
---

## Pre-release gap review of v2.9.0 — findings fixed

### WHAT
Ran a pre-push gap review of the 23 unpushed reference-resolution commits (the dead-code-enablement lane) and fixed all fillable findings.

### Review results
- Gap-reviewer subagent verdict: all 11 design acceptance criteria evidenced, dead-code consumer query fully supported by the artifact, all release-notes claims traced to code. Nothing blocks the push.
- My own sweep found two contract-doc/export gaps the earlier Codex review missed.

### Fixes applied
1. **Strict-schema v3 regression test** (reviewer finding A1): `strict_schema_rejects_older_v3_artifact_with_migration_required` in `cli_contract.rs` — v3 artifact + `--strict-schema` → exit 3 `schema_migration_required`; non-strict read not rejected.
2. **JSONL resolution-status export** (my finding): JSONL `artifact` record now carries additive `reference_resolution_status/version/last_full_revision` fields (`null` when absent) via `export_artifact` + `optional_metadata_i64`; TDD test `artifact_record_exports_reference_resolution_metadata`; exact-keys guard updated. Without this, JSONL consumers saw resolver-populated `target_symbol_id` with no way to apply the mandated status-gating rule.
3. **Contract docs**: `jsonl-v3.md` (artifact fields + identifier `target_symbol_id` semantics + detection rule), `reports.md` (new Reference Resolution Section documenting the `languages.reference_resolution` report shape), `sqlite-schema-v4.md` (`no_context` covers unsupported identifier kinds — reviewer B2; alias-shaped dead-code suppression caution — reviewer B1), `v2.9.0.md` contract bullet updated.

### Deferred (named)
- A2: end-to-end backfill test over a genuine v3 artifact file (piecewise evidence exists).
- B1 note must also land in Miller's P3 dead-code design (Miller repo).
- Crate version bump 2.8.1→2.9.0 happens in the `release: prepare` commit per convention.

### IMPACT
v2.9.0 lane is gap-reviewed and release-ready pending the user's push/release approval; JSONL consumers can now trust-gate resolution data identically to SQLite consumers.
