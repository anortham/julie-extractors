---
id: checkpoint_aabfd2e7
timestamp: 2026-07-07T03:30:27.319Z
tags:
  - reference-resolution
  - batch-b-done
  - task-3
  - task-4
  - delegation
git:
  branch: feature/workspace-reference-resolution
  commit: 39b584e
summary: Workspace reference resolution — Batch B done (Tasks 3+4), 4 of 8 tasks
  committed
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: checkpoint
evidence:
  - commits 356a3fe + 39b584e
  - "cargo xtask test default green: extractors 2790/0 artifact 95/0 cli 63/0"
  - writer_contract 36/0
  - resolution 28/0
next: Dispatch Task 5 (serial integration); review + commit; then Batch C (6+7),
  then Task 8.
---

## Workspace reference resolution — Batch B done (Tasks 3+4), 4 of 8 tasks committed

Branch `feature/workspace-reference-resolution`. Executing the 8-task plan via razorback:subagent-driven-development, delegation now reliable (subagent-cwd-fix in every prompt: cd repo + absolute paths + Miller workspace_id).

### Committed so far
- Task 1 (901c68c): schema v4 overlay tables + resolution_store.rs primitives.
- Task 2 (376f211): pending spans (PendingSpan=NormalizedSpan) + occurrence-distinct IDs.
- Task 3 (356a3fe): ResolutionHook writer seam — non-escaping HRTB FnMut in all 6 mutating paths, SAVEPOINT makes non-fatal contract truthful, old-name collection before delete_file_rows, row-domain handoff (SQLITE_ROW_DOMAINS/RowDomainCounts/expected_tables/ReportCode::ResolutionFailed). Lead fixed artifact_access.rs table_totals literal (Task 3's RowDomainCounts +2 fields broke that CLI caller).
- Task 4 (39b584e): pure tier chain resolve_one (tiers 2-4), UnresolvedEdge::from_pending/from_identifier, WorkspaceCandidateIndex::build. 28 unit tests.

### Gates
Batch A + Batch B affected-change gates green (cargo xtask test default: extractors 2790/0, artifact 95/0, cli main 63/0 incl 28 resolution, operations_contract 35/0). Tree clean at 39b584e.

### Task 5 IN FLIGHT (serial, the integration keystone)
resolve_workspace + hook closures at every mutating CLI flow; build candidate index (CandidateSymbol via SymbolKind::try_from_string, TypeFact, ImportRecord from kind='import' symbols); Full vs Delta (demotion sweep + fill sweep); span propagation to identifiers (byte-span join + line-fallback-when-unique) — NOTE Task 2 concern: pending span is call-expression node, WIDER than callee identifier, so byte-exact may miss (use containment/start_byte + line fallback); v3 backfill; reference_resolution_status/version/last_full_revision metadata; per-language/tier report + capability rows; xtask dogfood ResolutionFailed gate (dogfood.rs:321). Task 5 MUST clear Task 4's dead_code warnings by wiring resolve_one + use-or-drop CONFIDENCE_TIER1.

### Then
Batch C (Task 6 fixtures + Task 7 perf), serial Task 8 (contracts/capabilities/release notes). Branch gate after Task 6 lands: cargo xtask test default+contract+capability + dogfood + strict data-quality report.
