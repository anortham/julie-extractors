---
id: checkpoint_b0688271
timestamp: 2026-07-07T17:01:05.673Z
tags:
  - dead-code
  - variable-ref
  - codex-review
  - plan-review
git:
  branch: main
  commit: d930685
  files:
    - docs/plans/2026-07-07-variable-ref-identifier-emission-plan.md
summary: Codex adversarial review of variable_ref plan — 4 findings, all
  verified real, all fixed (rev 3)
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: learning
impact: Prevented a Batch E worker from skipping PowerShell coverage, a late
  release-preflight failure, and another argued-after-the-fact gate outcome.
next: "User approval to execute the plan (Task 1: C# reference implementation)."
---

## Codex adversarial review of variable_ref plan — 4 findings, all verified real, all fixed (rev 3)

Codex reviewed the rev-2 plan (`docs/plans/2026-07-07-variable-ref-identifier-emission-plan.md`) adversarially with code access. Verdict: needs-attention, 4 findings. I verified each against live code before accepting — **all four were real**, including one that corrected MY rev-2 amendment:

1. **PowerShell prior-art claim was FALSE (mine, from rev 2).** `powershell/variables.rs::extract_variable_reference` creates *Symbols* (`SymbolKind::Variable`, automatic/env vars only) — my grep had matched the substring `variable_ref` inside the function *name*, not `IdentifierKind::VariableRef`. Plan now treats powershell as greenfield. **Lesson: a filename-level grep hit is not evidence of emission — read the function.**
2. **Downstream gate inconsistency**: step 1 still said "proven prototype: 328→~28" and framework overrides were both "accepted residual" and subject to a zero-live hard gate. Now: step 1 is the explicit proof-of-equivalence step with a stop-on-divergence rule; overrides must be suppressed Miller-side BEFORE the scored gate run.
3. **Bare `cargo xtask dogfood`/`performance` commands exit with usage errors.** Replaced everywhere with exact invocations (`dogfood repo --root . --out-dir …`, `performance baseline --root <miller> --out-dir … --binary … --runs 3`).
4. **Version bump path incomplete**: release preflight (`RELEASE_CRATE_MANIFESTS`) checks all THREE crate manifests (artifact/cli/extractors), not just the CLI. Task 8 now bumps all three + Cargo.lock and runs `cargo xtask release preflight --version 2.10.0` (syntax verified in xtask before writing it).

**Impact:** plan is at rev 3, externally reviewed, all commands verified runnable. Awaiting user approval to execute.
