---
id: checkpoint_b5e13815
timestamp: 2026-08-31T18:30:16.532Z
tags:
  - epoch
  - store
  - csharp
  - release-candidate
  - bre-16
git:
  branch: main
  commit: 7712691f
  files:
    - TODO.md
actor:
  harness: grok-shell-goldfish
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Bumped extraction identity epoch to 8
briefId: bump-extraction-identity-epoch-to-8
type: checkpoint
impact: Unchanged C# content allocates a new file_versions row at epoch 8.
  Miller still needs a published 2.38.2 pin.
symbols:
  - EXTRACTION_IDENTITY_EPOCH
next: Do not push. Remaining work is ship v2.38.2 and pin miller after approval.
confidence: 5
---

## Bumped extraction identity epoch to 8

Family stores could not rewrite C# `internal` after BRE-16 because identity stayed at epoch 7.

- **WHAT:** `EXTRACTION_IDENTITY_EPOCH` 7 → 8. Candidate crate version 2.38.2. Compatible ledger `## 2.38.2`. Store-v1 and architecture docs name epoch 8 as current. Capability snapshot stays keyed by the written epoch, so new rows land at 8.
- **WHY:** v2.38.0 already emits `visibility=internal` for explicit C# `internal`. Store import reuses `(path, content_hash, extraction_epoch)`, so miller `workspace full` kept epoch-7 `private` rows.
- **HOW:** TDD on `tests::api_surface::test_public_contract_version_marks_current_fact_families` (red: left 7 / right 8). Then the constant, contracts, 2.38.2 candidate metadata, and TODO remaining-release note. Did not add `record_struct_declaration`. Did not delete `file_versions`. Did not pin miller.
- **IMPACT:** Unchanged C# content allocates a new `file_versions` row at epoch 8 on the next import. Epoch 7 rows stay immutable. Miller still needs a published 2.38.2 pin.

### Verification

- RED: api_surface epoch assertion failed left 7 / right 8
- GREEN: same test passed at 8
- `extraction_epoch_change_creates_a_new_version_for_unchanged_content` passed
- `the_shipped_ledger_entry_for_the_current_version_parses_when_present` passed
- `cargo xtask release preflight --version 2.38.2` passed (4 targets, 35 inputs)
- `cargo xtask test default` passed
- store_equivalence oracle passed (`test-store-contract`)
- `store_layout_creation_publishes_a_reopenable_generation` passed
- `cargo fmt --all -- --check`, `scripts/check-agent-doc-sync.sh`, `git diff --check` passed
