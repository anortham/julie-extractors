---
id: checkpoint_ba603260
timestamp: 2026-08-31T03:40:55.258Z
tags:
  - BRE-42
  - fsharp
  - marker
  - golden
  - correction
git:
  branch: codex/extractor-gap-closure-plans
  commit: 003e458c
  files:
    - fixtures/extraction/fsharp/script/expected.json
    - fixtures/extraction/fsharp/script/source.fsx
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
context: Correction round 1 after removing F# marker inventory claim exposed
  capability evidence mismatch while retaining the script golden marker row.
decision: Use ordinary F# script comment text and regenerate its golden,
  preserving source-region evidence and all marker registry/matrix contracts.
alternatives:
  - Restore the F# marker capability claim; rejected because it violates the
    shared per-language inventory convention.
  - Change the capability test or marker registry behavior; rejected because the
    fixture alignment resolves the mismatch within the approved ownership.
impact: The F# marker cleanup now satisfies the capability evidence contract and
  all assigned focused gates.
evidence:
  - cargo xtask test language fsharp — passed (17 F# tests and filtered golden)
  - cargo xtask test golden — passed (6 tests)
  - cargo xtask test capability — passed (39 capability tests and pending shape
    contract)
  - cargo test -p julie-extractors --features test-capability-matrix --lib
    structural_fact_registry — passed (16 tests)
  - cargo test -p julie-extractors
    tests::marker_structural_facts::marker_language_matrix_covers_every_supported_comment_language
    -- --exact — passed (1 test)
  - node scripts/language-data-quality-report.mjs --strict — passed;
    silent_cells=0; quality_bar_debts=0
  - cargo fmt --all -- --check — passed
  - git diff --check — passed
symbols:
  - golden_fixtures_match_canonical_extraction
  - marker_language_matrix_covers_every_supported_comment_language
next: Commit the two fixture files, correction report, and this checkpoint;
  return the commit SHA and clean worktree state.
confidence: 5
---

## WHAT

Closed the F# marker capability evidence mismatch. Changed `fixtures/extraction/fsharp/script/source.fsx` from `// TODO: script marker` to the ordinary `// script comment`, then regenerated `fixtures/extraction/fsharp/script/expected.json`. The script golden now has no `code.marker.v1` structural fact while retaining its F# comment source-region evidence with updated offsets.

## WHY

The F# capability inventory intentionally contains only native `fsharp.attribute.v1`; the unchanged marker-like script source made the capability evidence gate require an invalid cross-language marker claim. Aligning the fixture with other marker-capable languages preserves the registry and marker matrix contract without adding a per-language inventory exception.

## HOW

Used Miller to confirm the registry language and matrix remain unchanged, then ran the selected-language golden updater. Reviewed the resulting two-file fixture diff: only the comment text, structural-fact removal, and dependent byte/column spans changed. Ran the complete assigned focused gates.

## IMPACT

The cleanup is now internally consistent: F# marker registration and unit coverage remain, the capability row remains native-only, and the full capability suite passes. Changes are ready to commit with the prior boundary report updated for this correction round.
