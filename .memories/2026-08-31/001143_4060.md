---
id: checkpoint_4060e788
timestamp: 2026-08-31T00:11:43.824Z
tags:
  - go
  - subtests
  - bre-43
  - review-fix
  - scope-shadowing
git:
  branch: codex/extractor-gap-closure-plans
  commit: 4ec3733e
  files:
    - crates/julie-extractors/src/go/test_calls.rs
    - crates/julie-extractors/src/tests/go/test_detection.rs
    - docs/plans/2026-08-30-bre-16-csharp-internal-visibility.md
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: BRE-43 review round 2 ready to commit
type: checkpoint
impact: Commit only the two owned round-2 fix files and this checkpoint; leave
  the lead-owned C# plan modification unstaged. Append the round-2 evidence and
  commit SHA to the task report.
evidence:
  - RED scope-introducer test failed against 4ec3733e (2026-08-31T00:09:24Z)
  - RED switch-initializer test failed against 4ec3733e (2026-08-31T00:09:35Z)
  - GREEN scope-introducer test passed (2026-08-31T00:10:54Z)
  - GREEN switch-initializer test passed (2026-08-31T00:11:05Z)
  - "focused Go detection: 20 passed (2026-08-31T00:11:09Z)"
  - "cargo xtask test language go: 89 Go tests plus filtered golden passed
    (2026-08-31T00:11:09Z)"
  - post-edit Miller impact mapped Go test_calls and Go extractor dispatch
    dependents
symbols:
  - scope_introduces_local_binding
  - for_statement_introduces_binding
  - switch_case_contains_call
  - active_testing_t_receiver
next: Commit the explicit round-2 files plus this checkpoint, append the round-2
  report, and hand the commit to the lead.
---

## BRE-43 review round 2 ready to commit

### WHAT
Extended Go `testing.T.Run` receiver binding validation to scope-introducing ancestors. Added TDD coverage for `if` initializers, `for` initializers, range-clause bindings, expression-switch initializers, and type-switch aliases. The new detector now rejects calls whose receiver name is introduced by those nearer scopes before consulting an outer `*testing.T` parameter.

### WHY
The previous review fix only scanned preceding direct statements in block bodies. Valid Go calls inside `if t := ...`, `for t := ...`, `for _, t := range ...`, and switch case bodies remained false positives because their bindings live on scope-introducing ancestor nodes.

### HOW
Miller re-inspected both owned files and the changed collector/test helpers before edits, then traced collector and declaration references. The locked tree-sitter-go grammar was checked directly: `if_statement` and expression-switch nodes expose `initializer`; `for_statement` carries `for_clause`/`range_clause` plus `body`; `for_clause` exposes `initializer`; `range_clause` exposes `left`; type-switch headers expose `initializer` and `alias`; switch cases carry case statement lists. TDD tests were written first and failed against 4ec3733e; minimal ancestor checks now cover these exact fields and only treat range aliases as bindings in loop bodies and type-switch aliases as bindings in case bodies.

### IMPACT
The bounded Go lexical shadow detector now covers direct block declarations plus requested scope-introducing forms without a general resolver. Focused Go detection and `cargo xtask test language go` pass. Lead-level broader gates remain.
