---
id: checkpoint_79ecc22c
timestamp: 2026-08-31T13:49:22.926Z
tags:
  - windows
  - fsharp
  - release-2.38.0
  - verification
git:
  branch: main
  commit: c16a0d90
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Windows suite passed for the fsharp grammar (pre-tag gap closed)
type: checkpoint
evidence:
  - "win-test run log: 4363 passed / 0 failed, exit 0 (cargo xtask test default
    at 2dd9a463)"
  - fsharp tests all ok, e.g.
    tests::fsharp::semantic_facts::fsharp_attributes_emit_registered_structural_facts
next: Commit the 2.38.0 work in .worktrees/extractor-gap-closure-plans, then
  merge and tag v2.38.0 (needs user approval).
---

## Windows suite passed for the fsharp grammar (pre-tag gap closed)

**WHAT:** Ran `cargo xtask test default` on the win-test guest at commit `2dd9a463` (branch `codex/extractor-gap-closure-plans`). Result: 4363 passed, 0 failed, exit 0. The new tree-sitter-fsharp grammar compiled under MSVC and all fsharp tests passed.

**WHY:** Every release ships a Windows binary. The v2.38.0 candidate added the tree-sitter-fsharp grammar with no Windows run on record. This was the last verification gap flagged before a tag.

**HOW:**
- The uncommitted 2.38.0 work (docs, versions, rustdoc fence change) stayed on the host; the synced commit `2dd9a463` already contains all fsharp code, so the run answers the fsharp question.
- First run failed: guest C: was 100% full (os error 112). `C:\work\julie-extractors\target` held 44.5 GB of stale debug builds. `cargo clean` freed it; the rerun passed.
- Committed the untracked `.memories/2026-08-31/` files on main as `c16a0d90` because `win-test sync` refuses a dirty host tree.

**Tooling notes:**
- `win-test sync` requires a FULL 40-char SHA; a short SHA trips its HEAD-match check against itself.
- Host zsh mangles `$_` and nested quotes in `win-test run ... powershell -Command` lines; single-quote or avoid `$`.
- Watch guest C: free space; the target dir regrows ~45 GB per full debug build cycle.

**IMPACT:** The Windows blocker named in the v2.38.0 handoff is closed. Remaining before tag: commit the 16 uncommitted files in `.worktrees/extractor-gap-closure-plans`, then merge/tag/push (user approval). Deferred work items unchanged: rust block-comment doc tests (rust.doc_test_block_comments gap), registry.rs type_info widening (9 languages), hardcoded rust pattern id, attach_fsharp_attribute_symbols fallback.
