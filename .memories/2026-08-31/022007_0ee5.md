---
id: checkpoint_0ee5b254
timestamp: 2026-08-31T02:20:07.216Z
tags:
  - bre-42
  - fsharp
  - task-4
  - fixtures
  - evidence
git:
  branch: codex/extractor-gap-closure-plans
  commit: 48c8feb3
  files:
    - crates/julie-extractors/README.md
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/tests/api_surface.rs
    - crates/julie-extractors/src/tests/capability_snapshot_test.rs
    - docs/languages/fsharp.md
    - fixtures/extraction/capabilities.json
    - fixtures/extraction/fsharp/basic/expected.json
    - fixtures/extraction/fsharp/basic/source.fs
    - fixtures/extraction/fsharp/negative/expected.json
    - fixtures/extraction/fsharp/negative/source.fs
    - fixtures/extraction/fsharp/script/expected.json
    - fixtures/extraction/fsharp/script/source.fsx
    - fixtures/extraction/fsharp/signature/expected.json
    - fixtures/extraction/fsharp/signature/source.fsi
    - fixtures/extraction/fsharp/test_roles/expected.json
    - fixtures/extraction/fsharp/test_roles/source.fs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: F# Task 4 evidence and contract publication
type: checkpoint
evidence:
  - "cargo xtask test language fsharp: 15 F# tests and filtered golden passed"
  - "JULIE_GOLDEN_LANGUAGE=fsharp cargo xtask test golden: 6 tests passed"
  - "cargo xtask test capability: 39 tests plus pending-shape contract passed"
  - "node --test scripts/grammar-freshness-report.test.mjs: 11 tests passed"
  - cargo deny --all-features check and cargo audit exited 0
  - cargo clippy -p julie-extractors --lib --tests --all-features --no-deps --
    -D warnings exited 0
  - "node scripts/language-data-quality-report.mjs --strict: silent_cells=0,
    quality_bar_debts=2"
  - "cargo xtask test contract: blocked by operations_contract.rs expecting Rust
    structural facts without rust.doc_test.v1"
next: Lead should reconcile the Rust contract expectation and decide whether the
  strict quality-bar policy needs a scoped closure or a documented product
  decision before final branch gates.
---

## F# Task 4 evidence and contract publication

### WHAT

Prepared the approved F# publication packet on branch `codex/extractor-gap-closure-plans`: five deterministic fixture families (basic, script, signature, negative, and xUnit test roles), the F# capability row, language documentation, README parser inventory visibility, the `fsharp-v1` extraction contract marker, and the capability snapshot count update.

### WHY

Task 4 needs reviewed golden evidence and honest capability claims after the F# extractor behavior tasks. The packet also needs real-world evidence from Expecto at the pinned commit, without claiming unsupported F# domains.

### HOW

Generated the four requested goldens plus the required negative-control golden through the repository golden workflow, after observing missing-expected RED failures. Reviewed symbols, relationships, pending relationships, identifiers, types, type-argument usages, literals, complexity metrics, annotations, and diagnostics in every F# golden. Cloned Expecto at `cec2c63c8d77c6c21bf7e35d903020f74ddc1cea` and ran the exact scan CLI into ignored `target/corpora` output. Capability tests pass with explicit gaps for top-level script imports, F# structural facts, source regions, and test containers/lifecycle. The strict data-quality report has `silent_cells=0` but still reports two quality-bar debts for the honest structural-facts and source-regions open gaps, so those are recorded as a blocker rather than relabeled not-applicable.

### IMPACT

F# golden, capability, language, API, grammar, dependency, formatting, and package-Clippy checks are green. The broader contract tier exposes a separate Rust golden contract expectation that omits `rust.doc_test.v1`; the lead must reconcile that outside this packet. The final commit will contain only owned Task 4 files plus this memory artifact.
