---
id: checkpoint_0559d5b5
timestamp: 2026-08-31T01:16:46.662Z
tags:
  - BRE-42
  - fsharp
  - task-1
  - correction
git:
  branch: codex/extractor-gap-closure-plans
  commit: a768d3cd
  files:
    - crates/julie-extractors/src/factory.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
impact: Removes the known 39-vs-40 integration failure without touching Task 4
  capability evidence.
evidence:
  - cargo test -p julie-extractors
    factory_consistency_tests::test_all_languages_in_factory --lib --
    --nocapture (1 passed)
  - cargo test -p julie-extractors tests::fsharp --lib -- --nocapture (6 passed)
  - cargo check -p julie-extractors (passed)
  - cargo fmt --all -- --check && git diff --check (passed)
symbols:
  - test_all_languages_in_factory
next: Commit only factory.rs and this checkpoint artifact; lead performs final
  review.
unknowns:
  - The capability snapshot count remains 39 until Task 4 publishes F# evidence.
---

## WHAT

Completed BRE-42 Task 1 correction round 1. Updated the factory consistency test in `crates/julie-extractors/src/factory.rs` from a hard-coded 39-language expectation/message to 40, matching the newly registered F# language.

## WHY

The required F# registry entry made the existing factory count stale. The lead's inline review identified this as an Important integration defect, and ownership was explicitly expanded to this one file for correction. The capability snapshot count remains intentionally deferred to Task 4.

## HOW

Used Miller search, inspect, and impact before editing; previewed both literal changes with Miller edit, applied the narrow patch, refreshed and re-inspected the test, then ran the exact failing factory test plus the assigned F# and build/format checks.

## IMPACT

The factory inventory gate now passes for all 40 registered languages. F# behavior remains green and the crate remains buildable. This correction is ready as a separate serial worker commit for the lead to review.
