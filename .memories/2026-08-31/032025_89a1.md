---
id: checkpoint_89a1dddb
timestamp: 2026-08-31T03:20:25.649Z
tags:
  - bre-53
  - traversal-guard
  - serial-worker-commit
git:
  branch: codex/extractor-gap-closure-plans
  commit: ed1ab948
  files:
    - crates/julie-extractors/src/base/rust_doc_test_facts.rs
    - crates/julie-extractors/src/fsharp/declarations.rs
    - crates/julie-extractors/src/tests/marker_structural_facts.rs
    - crates/julie-extractors/src/tests/source_regions.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
context: Default-gate correction after the lead found collect_doc_comment_lines
  as an unguarded recursive tree walker.
decision: Guard the Rustdoc recursive comment walk with the standard tree
  traversal budget.
alternatives:
  - Refactor the walker to an iterative traversal; rejected because the approved
    correction only requires the standard guard and must preserve behavior.
  - Modify the F# walker reported after this correction; rejected because it is
    outside this packet and lead-owned.
impact: Lead can apply the remaining F# traversal/format correction separately
  and rerun the branch gates.
evidence:
  - "cargo test -p julie-extractors
    tests::traversal_guard_convention::production_tree_walkers_bound_themselves\
    _with_the_traversal_budget: Rustdoc violation cleared; remaining failure is
    F# first_identifier_text."
  - "cargo test -p julie-extractors tests::rust::doc_tests -- --nocapture: 6
    passed."
  - "cargo xtask test language rust: 108 Rust tests and Rust golden passed."
  - "cargo clippy -p julie-extractors --lib --tests --all-features --no-deps --
    -D warnings: passed."
  - "rustfmt --edition 2024 --check
    crates/julie-extractors/src/base/rust_doc_test_facts.rs: passed."
  - "git diff --check: passed."
next: Stage only rust_doc_test_facts.rs and this checkpoint; append the ignored
  worker report with the final commit SHA.
unknowns:
  - The full cargo fmt check remains blocked by the lead-owned F# formatting
    diff.
  - The exact traversal convention remains blocked by the lead-owned F#
    self-recursive walker.
---

## WHAT

Added the shared traversal-budget guard to `collect_doc_comment_lines` in `crates/julie-extractors/src/base/rust_doc_test_facts.rs`, including a `depth: u32` parameter, `should_visit_tree_depth` entry guard, `child_tree_depth` descent guard, and recursive propagation.

## WHY

The default traversal convention requires every production recursive tree walk to stop at the crate-wide depth budget. The BRE-53 collector was the only Rustdoc walker still missing that guard.

## HOW

Miller search/inspect/trace confirmed the failing symbol and the existing guarded `collect_node` pattern in `base/structural_facts.rs` and `base/source_regions.rs`. The collector behavior and fact output were left unchanged; only traversal control was added. Miller edit preview could not apply because its index reported the committed file as missing on disk, so the equivalent scoped `apply_patch` was used after the failed preview.

## IMPACT

The Rustdoc collector now satisfies the traversal safety convention. Focused Rustdoc tests, the Rust language gate, clippy for `julie-extractors`, owned-file rustfmt, and diff checks pass. The convention and workspace fmt gates still report one unrelated lead-owned F# walker/formatting issue.
