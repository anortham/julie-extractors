---
id: checkpoint_70dd6fcc
timestamp: 2026-08-31T03:22:26.132Z
tags:
  - bug-fix
  - test
  - fsharp
  - source-regions
  - structural-facts
  - coverage-matrix
git:
  branch: codex/extractor-gap-closure-plans
  commit: b9e62f8c
  files:
    - crates/julie-extractors/src/tests/marker_structural_facts.rs
    - crates/julie-extractors/src/tests/source_regions.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Closed F# shared-coverage matrix gaps
type: checkpoint
impact: Restores default-gate coverage for F# without broadening production behavior.
evidence:
  - "cargo test -p julie-extractors --lib
    tests::marker_structural_facts::marker_language_matrix_covers_every_support\
    ed_comment_language -- --exact --nocapture: PASS"
  - "cargo test -p julie-extractors --lib
    tests::source_regions::supported_languages_with_source_region_syntax_emit_r\
    egions -- --exact --nocapture: PASS"
  - "cargo test -p julie-extractors --lib tests::marker_structural_facts --
    --nocapture: 4 passed"
  - "cargo test -p julie-extractors --lib tests::source_regions -- --nocapture:
    7 passed"
  - "cargo fmt --all -- --check: PASS"
  - "git diff --check: PASS"
symbols:
  - marker_language_matrix_covers_every_supported_comment_language
  - supported_languages_with_source_region_syntax_emit_regions
next: Commit only the two owned test files and this checkpoint, then append the
  correction report with the commit and clean-state evidence.
confidence: 5
---

## Closed F# shared-coverage matrix gaps

The default extractor gates failed because the language-wide marker and source-region test matrices did not account for the newly supported F# language.

- **WHAT:** Added a real F# source-region fixture containing `//`, `///`, and a string literal; classified F# as non-applicable in the marker registry-alignment matrix because `code.marker.v1` has no F# registry entry.
- **WHY:** The source-region matrix now proves all three F# region kinds, while the marker matrix covers every supported language without claiming an unregistered marker pattern.
- **HOW:** Kept the production registry unchanged and updated only `marker_structural_facts.rs` and `source_regions.rs`.
- **IMPACT:** Both exact reviewer failures and their focused test modules pass; no concurrent traversal-guard files were touched.

Verification: exact marker and source-region tests, focused marker module (4 passed), focused source-region module (7 passed), `cargo fmt --all -- --check`, `git diff --check`, and Miller impact all pass.
