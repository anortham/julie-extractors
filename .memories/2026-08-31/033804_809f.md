---
id: checkpoint_809fbc2d
timestamp: 2026-08-31T03:38:04.720Z
tags:
  - BRE-42
  - fsharp
  - marker
  - cleanup
git:
  branch: codex/extractor-gap-closure-plans
  commit: "85010439"
  files:
    - crates/julie-extractors/src/base/structural_facts.rs
    - fixtures/extraction/capabilities.json
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
context: Fresh bounded cleanup after BRE-42 Task 4 review found an F#-only
  cross-language marker inventory exception.
decision: Keep F# marker emission and registry coverage, but remove the
  test-only per-language inventory exception and capability claim.
alternatives:
  - Keep the F# capability claim to satisfy the existing fixture-evidence test;
    rejected because it violates the shared inventory convention.
  - Remove the F# golden marker row; rejected because the packet explicitly
    requires preserving it.
  - Modify capability_matrix.rs to ignore the cross-language marker baseline;
    outside this packet's owned paths.
impact: Two-file cleanup is recorded before commit; lead must reconcile the
  capability-test boundary.
evidence:
  - cargo xtask test language fsharp — passed
  - cargo xtask test golden — passed (6 tests)
  - cargo test -p julie-extractors --features test-capability-matrix --lib
    structural_fact_registry — passed (16 tests)
  - cargo test -p julie-extractors
    marker_language_matrix_covers_every_supported_comment_language — passed
  - node scripts/language-data-quality-report.mjs --strict — passed;
    silent_cells=0; quality_bar_debts=0
  - cargo fmt --all -- --check — passed
  - git diff --check — passed
  - cargo xtask test capability — fails only at
    capability_matrix_structural_fact_claims_have_fixture_evidence for retained
    F# code.marker.v1 golden output
symbols:
  - structural_fact_pattern_ids_for_language
  - marker_language_matrix_covers_every_supported_comment_language
next: Commit only the owned source, capability row, report, and this checkpoint;
  then hand the capability-test mismatch to the lead.
confidence: 5
---

## WHAT

Removed the F#-only `code.marker.v1` insertion from `structural_fact_pattern_ids_for_language` and removed `code.marker.v1` from the F# `kind_coverage.structural_facts.supported` list. Preserved the registered marker language, marker matrix fixture, and F# script golden marker row.

## WHY

The per-language inventory must follow the established convention: the cross-language marker baseline is accounted for by the registry test rather than claimed in each language's inventory. F# already has native `fsharp.attribute.v1` evidence.

## HOW

Miller inspection confirmed the F# special case was the only inventory exception and that the registry test explicitly inserts the marker pattern for registered languages. The cleanup was restricted to the two requested files. Focused F#, golden, registry, marker-matrix, strict-quality, formatting, and diff checks were run. The full capability suite still reports the known contract mismatch caused by retaining the F# golden marker row while removing its capability claim; resolving that requires an out-of-scope capability-test change or golden-fixture change.

## IMPACT

The F# inventory now matches the shared registry-accounting convention without changing emitted marker facts or golden evidence. The two-file cleanup is ready for commit, with the capability-suite mismatch recorded as a boundary for the lead.
