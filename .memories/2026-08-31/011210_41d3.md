---
id: checkpoint_41d300bb
timestamp: 2026-08-31T01:12:10.278Z
tags:
  - BRE-42
  - fsharp
  - task-1
  - tdd
git:
  branch: codex/extractor-gap-closure-plans
  commit: db6777bb
  files:
    - Cargo.lock
    - crates/julie-extractors/Cargo.toml
    - crates/julie-extractors/src/fsharp/declarations.rs
    - crates/julie-extractors/src/fsharp/mod.rs
    - crates/julie-extractors/src/language_spec/mod.rs
    - crates/julie-extractors/src/language_spec/specs.rs
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/pipeline.rs
    - crates/julie-extractors/src/registry.rs
    - crates/julie-extractors/src/tests/fsharp/mod.rs
    - crates/julie-extractors/src/tests/mod.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
impact: Unblocks BRE-42 Task 2 semantic facts and later evidence publication; no
  capability fixture is published in Task 1.
evidence:
  - cargo test -p julie-extractors tests::fsharp --lib -- --nocapture (6 passed)
  - cargo check -p julie-extractors (passed)
  - cargo clippy -p julie-extractors --lib --tests --all-features --no-deps --
    -D warnings (passed)
  - cargo fmt --all -- --check && git diff --check (passed)
symbols:
  - get_tree_sitter_language_for_path
  - configured_parser_for_language_at
  - FSharpExtractor
  - declarations::visit_node
  - extract_canonical_with_parse
next: Lead reviews the owned diff and routes the out-of-scope 39-language
  assertion correction before branch-gate verification.
---

## WHAT

Completed BRE-42 Task 1 in the extractor-gap-closure-plans worktree. Added the exact tree-sitter-fsharp 0.3.0 dependency and lock entry, registered stable `fsharp` metadata for `.fs`, `.fsx`, and `.fsi`, added path-aware implementation/signature parser selection, and added the foundational F# declaration extractor.

## WHY

This establishes the parser and extractor interfaces required by later F# semantic-fact and test-role tasks while preserving the existing public one-argument parser lookup and one artifact language identity.

## HOW

TDD was followed: six focused F# tests were written and observed failing because F# was unsupported, then the smallest parser/spec/registry/extractor implementation was added. The grammar was proven from the installed binding and node-types: namespaces/modules, type-definition wrappers, record/union fields and cases, members, declaration/value nodes, XML docs, and attributes. Symbols use BaseExtractor IDs/spans/body hashes where its shared body contract applies, textual `///` doc extraction, and normalized F# `[<...>]` annotations.

## IMPACT

Focused F# tests (6), crate check, dependency tree, formatting, diff checks, and focused API/pipeline regressions pass. The next F# task can consume the stable extractor and declaration IDs. Two existing hard-coded counts remain outside this packet: `factory.rs` expects 39 languages (actual 40) and the capability snapshot count will need Task 4 evidence updates.
