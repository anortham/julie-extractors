---
id: checkpoint_404aa99f
timestamp: 2026-08-31T03:31:39.293Z
tags:
  - bug-fix
  - contract
  - fsharp
  - marker
  - golden
  - capability
git:
  branch: codex/extractor-gap-closure-plans
  commit: bc628444
  files:
    - crates/julie-extractors/src/base/structural_fact_registry/marker.rs
    - crates/julie-extractors/src/base/structural_facts.rs
    - crates/julie-extractors/src/tests/marker_structural_facts.rs
    - docs/contracts/structural-fact-patterns.json
    - fixtures/extraction/capabilities.json
    - fixtures/extraction/fsharp/script/expected.json
    - fixtures/extraction/fsharp/script/source.fsx
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Restored F# marker contract
type: checkpoint
impact: Makes the F# structural-fact capability contract honest and fixture-proven.
evidence:
  - marker matrix RED before registry enrollment, GREEN after
  - capability structural-claim test RED before F# helper entry, GREEN after
  - "cargo xtask test language fsharp: PASS (17 F# tests plus golden)"
  - "cargo xtask test capability: PASS (39 capability tests plus pending-shape)"
  - "cargo test -p julie-extractors --features test-capability-matrix --lib
    structural_fact_registry -- --nocapture: PASS (16 tests)"
  - "node scripts/language-data-quality-report.mjs --strict: PASS
    (silent_cells=0, quality_bar_debts=0)"
  - "cargo fmt --all -- --check and git diff --check: PASS"
symbols:
  - LANGUAGES
  - marker_language_matrix_covers_every_supported_comment_language
  - structural_fact_pattern_ids_for_language
  - capability_matrix_structural_fact_claims_match_registry
next: Commit only the owned registry/helper/test, F# script, capability,
  structural-contract, and checkpoint paths; append the SDD report with final
  state.
confidence: 5
---

## Restored F# marker contract

Round 3 corrected the F# marker classification after corpus evidence showed generic marker facts are emitted from F# source regions.

- **WHAT:** Registered `fsharp` for `code.marker.v1`, moved it into the marker fixture matrix with a valid `// TODO: marker` source, converted the existing F# script comment to `// TODO: script marker`, regenerated the script golden and structural-fact contract, and claimed the marker in F# capability coverage.
- **WHY:** Round 2's `not_applicable` classification contradicted five real F# marker facts in the pinned Expecto artifact.
- **HOW:** Miller traced the registry and test-only `structural_fact_pattern_ids_for_language` authority. The capability gate exposed that helper's F# omission; its only callers are the capability and registry tests, so the minimal F# entry was added under the existing test-only feature gate.
- **IMPACT:** Registry, golden, capability, and strict data-quality contracts now agree with actual generic marker emission. No traversal-guard files were touched.

TDD evidence: marker matrix failed before registry enrollment and passed after; capability structural-claim test failed before the helper entry and passed after. Verification: `cargo xtask test language fsharp`, F# golden, `cargo xtask test capability`, structural registry tests, strict quality (`silent_cells=0`, `quality_bar_debts=0`), exact marker matrix, fmt, and diff checks all pass.
