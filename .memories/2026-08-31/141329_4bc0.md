---
id: checkpoint_4bc00687
timestamp: 2026-08-31T14:13:29.948Z
tags:
  - release-2.38.0
  - rust
  - fsharp
  - doc-tests
  - registry
git:
  branch: main
  commit: c16a0d90
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Closed all four deferred items for v2.38.0
type: checkpoint
evidence:
  - "cargo xtask test default: 4397 passed, 0 failed"
  - "gates chain contract/capability/golden/certification: 431 passed, 0 failed"
  - "release preflight ok: 4 targets, 35 inputs"
  - "strict report: silent_cells 0, quality_bar_debts 0, open_gap_backlog 47"
next: Commit the worktree, run the Windows suite at the new SHA, then ask the
  user to approve merge, tag, and push for v2.38.0.
---

## Closed all four deferred items for v2.38.0

**WHAT:** All four pre-release work items are implemented and verified in `.worktrees/extractor-gap-closure-plans`:
1. `rust.doc_test.v1` now reads `/** ... */` and `/*! ... */` block doc comments (`collect_block_doc_comment_lines` + `block_doc_star_column` in `base/rust_doc_test_facts.rs`), mirroring rustdoc's horizontal trim; 5 new unit tests plus two new golden fixture fences (`double_flag`, `halve_flag`). The `rust.doc_test_block_comments` open gap is removed; backlog 48 → 47.
2. `attach_fsharp_attribute_symbols` lost its global fallback. An attribute with no in-scope annotated target keeps its lexical container. New test: `fsharp_assembly_attributes_do_not_claim_unrelated_annotated_symbols` proved the `[<assembly: ...>]` mis-attachment before the fix.
3. `registry.rs` gained `types_with_base_info` (unit-tested): all nine other type-inferring arms (python, cpp, ruby, razor, regex, html, sql, erlang, vue) now merge `base.type_info` like the full-language macro.
4. The hardcoded `"rust.doc_test.v1"` in `structural_facts.rs` now references `rust_doc_test_facts::PATTERN_ID` (made pub(crate)).

**WHY:** The user asked to finish these deferred items before the v2.38.0 release.

**HOW:** TDD throughout: each behavior change started from a watched failing test. Golden regeneration needs `--features test-golden` and `UPDATE_GOLDEN=1` with `JULIE_GOLDEN_LANGUAGE=<lang>`; a bare `cargo test --lib golden` silently matches a different test.

**IMPACT / verification:** fmt clean, clippy 0 warnings, `xtask test default` 4397 passed 0 failed, contract+capability+golden+certification 431 passed 0 failed, strict report silent_cells 0 quality_bar_debts 0 backlog 47, doc sync clean, `release preflight --version 2.38.0` ok (4 targets, 35 inputs). capabilities.json returned to its committed state (prior session had only added the now-closed gap row). Docs updated: `docs/languages/rust.md`, `docs/languages/fsharp.md`, `docs/release-notes/v2.38.0.md`. Next: commit worktree, rerun Windows suite at the new SHA, then merge/tag/push on user approval.
