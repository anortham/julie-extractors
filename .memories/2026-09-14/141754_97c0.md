---
id: checkpoint_97c0e04b
timestamp: 2026-09-14T14:17:54.842Z
tags:
  - maintainability-cleanup
  - artifact-writer
  - refactor
git:
  branch: main
  commit: 460fd1a1
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
evidence:
  - cargo fmt --check
  - cargo test -p julie-extract-artifact --lib writer::tests
  - cargo test -p julie-extract-artifact --test writer_batching_contract
  - cargo test -p julie-extract-artifact --test writer_contract
symbols:
  - ArtifactWriter.write_scan_with_bulk_load
next: Lead should run its planned branch checks and focused Windows writer
  contract after the shared worktree settles.
---

## WHAT

Shared the duplicated bulk-load wrappers for in-memory and spooled scan writes through `ArtifactWriter::write_scan_with_bulk_load` in the maintainability-cleanup worktree.

## WHY

Both entry points had identical poison, eligibility, timing, restoration, and committed-error policy.

## HOW

The private helper accepts the differing write operation and keeps the prior wrapper sequence unchanged. Existing contract coverage exercises both regular and spooled bulk paths, including restoration errors.

## IMPACT

Removes duplicated control flow without changing SQLite/JSON contracts or public APIs. Focused writer tests pass; Windows should run the existing writer contract target.
