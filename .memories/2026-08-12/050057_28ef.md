---
id: checkpoint_28ef2e5e
timestamp: 2026-08-12T05:00:57.481Z
tags:
  - incident
  - performance
  - sqlite
  - query-amplification
  - store-resolution
  - locator
  - pending-phase
  - telemetry
  - disproven-hypothesis
git:
  branch: perf/store-resolution-query-amplification
  commit: 0f2011e5
  files:
    - crates/julie-extract-cli/tests/store_resolution_performance.rs
summary: Proved the real 10,804-call locator phase after two disproven optimizations
type: incident
context: A faithful replay of bounded resolved-pending hydration completed in
  49.77s with `ResolvedPendingCoLocationHydration=0` and
  `LocateIdentifier=10,804`, contradicting the corrected Task 3 attribution just
  as the prior relationship-coverage replay had contradicted the first
  attribution.
impact: Stops speculative work on inactive resolver paths and selects
  `resolve_pending_items` as the measured target with direct phase evidence.
evidence:
  - Focused resolved-pending RED failed exactly at Store LocateIdentifier 8 vs 0
    after semantic/scoped assertions.
  - Focused resolved-pending GREEN had LocateIdentifier=0 and bounded hydration
    3 executions/8 rows, but faithful replay emitted the new family 0 times.
  - Phase telemetry exact test passed with expected fixed buckets 1/2/3/5.
  - "Single caller-attribution replay: Pending=10,804; ResolvedPending=0;
    Relationships=0; Other/Unset=0."
  - "Live snapshot at 1,047ms: Pending=8,109 and all other buckets zero."
  - Replay exact digest
    b8833220fd1e24b78586e2ddd1c0f8d17b77c37a1fb58c9df175617c5d46c28b with
    392,526 identifier and 10,804 pending rows.
symbols:
  - SnapshottingSession
  - ResolutionPhase
  - ResolutionSession::locate_identifier
  - resolve_pending_items
  - recheck_resolved_pending_items
  - materialized_relationship_covers
next: Lead should review/commit the test-only phase telemetry, then define a
  fresh TDD slice for bounded pending-page co-location hydration without
  touching `finish_exact`.
confidence: 5
unknowns:
  - The exact best bounded interface for eliminating Pending-phase locator calls
    is not yet designed or tested.
  - "`finish_exact` still accounts for approximately half of total process wall
    and remains a separate task."
---

## Proved the real 10,804-call locator phase after two disproven optimizations

Two focused optimizations were valid locally but absent from the faithful Miller-scale path: materialized relationship coverage and resolved-pending co-location hydration both emitted zero executions in production replay.

- **WHAT:** Removed the entire uncommitted resolved-pending production/test slice and retained only test-only fixed-phase locator telemetry in `SnapshottingSession`.
- **WHY:** `LocateIdentifier=10,804` remained unchanged after both attempted slices, so another optimization without caller proof would repeat the same failure.
- **HOW:** Added fixed `resolved_pending`, `pending`, `relationships`, and `other_or_unset` buckets to final/live diagnostic JSON; an exact wrapper test proved phase attribution, then one 60-second-capped replay ran without retry.
- **IMPACT:** Final replay directly proves all 10,804 locator statements occur in `Pending`; the other three buckets are zero. The next performance slice can target `resolve_pending_items` with evidence. Neither disproven slice should be reintroduced.
