---
id: checkpoint_60338d7c
timestamp: 2026-07-27T01:27:14.247Z
tags:
  - julie-extractors
  - schema5
  - jsonl
  - reference-sites
  - markers
  - claude-review
git:
  branch: codex/cross-repo-dogfood-repair
  commit: f397421e
  files:
    - crates/julie-extract-artifact/src/jsonl.rs
    - crates/julie-extract-artifact/src/writer/rows.rs
    - crates/julie-extract-artifact/tests/jsonl_contract.rs
    - crates/julie-extract-artifact/tests/writer_contract.rs
    - crates/julie-extractors/src/base/marker_structural_facts.rs
    - crates/julie-extractors/src/base/results_normalization.rs
    - crates/julie-extractors/src/tests/marker_structural_facts.rs
summary: Closed Claude producer review findings
type: decision
decision: Fix the four confirmed producer defects while retaining the schema-5
  reference-site identity guard because scope disagreement is invalid producer
  data.
alternatives:
  - Drop containing_symbol_id from reference-site identity and silently merge
    disagreements; rejected because the v2.18 contract requires conflicting
    canonical rows to fail and the live corpus has zero disagreements.
  - Treat Eros compatibility as a completion gate; rejected because Eros is
    downstream and not shipping.
impact: Miller now has a producer whose schema-5 reference evidence, JSONL
  output, marker facts, and row counts are internally consistent.
evidence:
  - "RED: nullable caller_scope exported as InvalidColumnType(Null)."
  - "RED: skipped child rows left 2 reference_sites."
  - "RED: shared site with different scope raised reference_site identity
    conflict, confirmed as intended contract behavior by live corpus counts of
    zero disagreements."
  - "RED: malformed owner emitted zero marker facts."
  - "GREEN: jsonl_contract 10/10, schema_contract 16/16, writer_contract 39/39."
  - "GREEN: malformed marker and normalization source-guard tests pass."
  - cargo fmt --all --check passed.
symbols:
  - export_pending_relationships
  - insert_reference_sites
  - relationship_is_insertable
  - pending_relationship_is_insertable
  - marker_prefix
next: Commit the Julie producer review fixes, run the full Julie workspace
  gates, rebuild the pinned producer into Miller, then run Miller fast/scale/MCP
  verification.
confidence: 5
---

## Closed Claude producer review findings

### WHAT

Fixed nullable pending-relationship JSONL export, prevented orphan reference-site rows for skipped relationships, retained malformed marker facts, and completed the normalization source guard. Added focused regressions for each behavior.

### WHY

Claude found producer defects after the schema-5/index repair. These could fail JSONL export, inflate reference-site counts, or silently lose marker evidence.

### HOW

- Read `caller_scope_symbol_id` as `Option<String>` in JSONL export.
- Share insertability predicates between reference-site and relationship writers.
- Treat an unclosed marker owner as description text instead of suppressing the marker.
- Check both forbidden normalization names in both source files.
- Kept the reference-site identity trigger: a live full artifact showed zero identifier, relationship, or pending scope disagreements, so disagreement is invalid producer data and must roll back.

### IMPACT

Focused artifact contracts pass 65 tests, marker and normalization regressions pass, schema catalog fingerprint remains unchanged, and skipped child rows no longer leave orphan sites.
