---
id: checkpoint_d2bd576d
timestamp: 2026-07-27T18:00:36.791Z
tags:
  - release
  - v2.19.0
  - merge
  - worktree-cleanup
  - dogfood
  - regression
git:
  branch: main
  commit: cb4f9457
summary: Merged to main, cleaned all worktrees/branches, prepped v2.19.0 — found
  dogfood gate broken since 2.18.0
type: checkpoint
evidence:
  - git cherry main codex/cross-repo-marker => '-' (already upstream)
  - "cargo xtask test default: pass"
  - "cargo xtask test contract: pass"
  - "cargo xtask test certification: pass"
  - "cargo xtask test real-world-smoke: pass"
  - "cargo xtask release preflight --version 2.19.0: ok, 4 targets, 27 inputs"
  - "cargo xtask dogfood repo: pass after fix, 1689 files"
  - "cargo fmt --check + clippy: clean"
symbols:
  - JSONL_RECORD_KINDS
  - marker_prefix
  - check-release-state.sh
next: Await user decision on pushing main + tagging v2.19.0. Nothing pushed.
---

## Merged to main, cleaned all worktrees/branches, prepped v2.19.0 — found dogfood gate broken since 2.18.0

### The "missed" marker worktree was not missed
`codex/cross-repo-marker` looked like unmerged work (1 commit not in main). It is **already in main** as `f1e0c956` — `git cherry main codex/cross-repo-marker` returns `-` (patch already upstream). The attempted merge hit an add/add conflict, and comparing the two versions showed **main's is strictly better**: the branch's `marker_prefix` uses `split_once(')')?`, so a malformed `// TODO(alice: no closing paren` returns None and **suppresses the marker fact entirely**; main uses `.and_then(...)` so the fact still emits with `owner = None`, and main has a test `malformed_owner_does_not_suppress_marker_fact` pinning it. The branch was also 2,942 lines behind main (pre-schema-v5). Merging would have regressed main. Aborted the merge and deleted the branch instead. Preserved its gitignored task report to `.razorback/sdd/marker-facts-task-2-report.md`.

### Merged and cleaned
- `7b94810f` feat commit + `c4dd8c8f` merge → main. All gates green post-merge.
- Removed all 3 worktrees and the `.worktrees/` dir; deleted 18 branches total (4 worktree branches + 14 stale), each verified with `git cherry` at 0 unapplied commits. Only `main` remains.

### v2.19.0 prepared (`cb4f9457`)
Version 2.18.0 → 2.19.0 in the 3 crate manifests + Cargo.lock, `docs/release-notes/v2.19.0.md`, README index. Chose minor: schema v5 / extract v4 / JSONL v4 all unchanged, resolution contract 2 → 3, but C# artifact *content* changes (655 new namespace symbols, re-parented types).

### Real find: dogfood gate broken since 2.18.0
`cargo xtask dogfood repo` aborted with "JSONL record 196621 has unsupported kind `reference_site`". Root cause: `reference_site` shipped in 2.18.0 (`cc501177`) and the artifact crate exports `pub const JSONL_RECORD_KINDS` including it, but `xtask/src/dogfood.rs` kept a **private duplicate** of that allowlist which was never updated. Every dogfood run since 2.18.0 has failed. Fixed structurally — xtask now imports `julie_extract_artifact::jsonl::JSONL_RECORD_KINDS` rather than duplicating it, so drift is impossible. Verified the only other occurrence (`jsonl_contract.rs:419`) is a per-record field assertion, not a second allowlist.

### Gates run on the release candidate
fmt, clippy, `cargo test -p xtask`, `xtask test default`, `xtask test contract`, `xtask test certification` (triggered by the Cargo.lock/Cargo.toml change), `xtask test real-world-smoke`, `xtask release preflight --version 2.19.0` ("ok: 4 targets, 27 inputs"), and `xtask dogfood repo` (1,689 files, 1.07 GB SQLite, clean rescan). All pass.

### State
main = `cb4f9457`, 4 commits ahead of origin/main, **not pushed** — user said "we need to make sure we're ready to push a new release first". `scripts/check-release-state.sh` still reports UNPUSHED MAIN by design; it will also want tag `v2.19.0` pushed once main is.
