---
id: checkpoint_7e6c5f9e
timestamp: 2026-07-27T03:47:30.815Z
tags:
  - release
  - julie-extract
  - 2.18.0
  - packaging
  - contracts
git:
  branch: main
  commit: 500416af
  files:
    - xtask/src/release.rs
    - xtask/tests/release_contract.rs
summary: Packaged the missing schema-v5 catalog authority before publishing 2.18.0
type: checkpoint
next: Commit, push main, tag v2.18.0, verify the four archives + sha256 sidecars
  publish, then fill Miller's julie-pins.json hashes.
---

## Packaged the missing schema-v5 catalog authority before publishing 2.18.0

**WHAT:** Added `docs/contracts/sqlite-schema-v5.catalog.sha256` to the release package manifest (`xtask/src/release.rs`) and the enforcing list in `xtask/tests/release_contract.rs`.

**WHY:** `sqlite-schema-v5.md` and `jsonl-v4.md` both name that file as the authoritative catalog fingerprint ("the complete catalog authority is the normalized sqlite_master SHA-256 in ..."), but the archive shipped the docs without the file. A consumer unpacking the release could not verify their artifact's schema against the declared authority.

**HOW:** One manifest entry plus the matching contract-test entry. Verified with `cargo xtask release package-list` (catalog file now listed) and `cargo xtask release preflight --version 2.18.0` → "ok: 4 targets, 27 inputs".

**IMPACT:** Unblocks publishing julie-extract v2.18.0, which is the hard precondition for Miller v1.14.0 (Miller pins 2.18.0 with empty sha256).

**Gates green at this commit:** default tier 3021 passed / 0 failed; contract tier all green / 0 failed; certification tier 42 passed incl. `parser_upgrade_gate_covers_full_language_inventory`.

**Pre-flight verified:** main clean and 8 ahead; all worktrees clean except one untracked agent report; the 8 commits touch no `.github/workflows` (no tag-push 403 risk); `codex/cross-repo-marker`'s extra commit has an identical patch-id to `f1e0c956` already in main, so no stranded work.
