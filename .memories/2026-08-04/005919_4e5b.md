---
id: checkpoint_4e5b40eb
timestamp: 2026-08-04T00:59:19.882Z
tags:
  - ci
  - test-tiers
  - budget
git:
  branch: main
  commit: 5d8a4b7
  files:
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extract-cli/tests/deep_recursion_contract.rs
    - crates/julie-extract-cli/tests/perf_gate_convention.rs
    - crates/julie-extract-cli/tests/reference_site_identity.rs
    - xtask/src/test_tiers.rs
    - xtask/tests/test_tiers.rs
summary: "Restored the default-tier budget: heavy contract suites moved behind
  test-heavy-contracts"
type: checkpoint
next: Commit + push to julie main, watch Fast Gates go green.
---

## Restored the default-tier budget: heavy contract suites moved behind test-heavy-contracts

julie-extractors CI (Fast Gates) has been red on every main push since 13:18 2026-08-03 — all tests passed but the default tier tripped its 90s wall-clock budget (162s hosted, 85s local M2).

- **Root cause:** `deep_recursion_contract.rs` (48s hosted) and `reference_site_identity.rs` (30s hosted), added 2026-08-02 by the traversal-budget and reference-site-identity fixes, landed as ungated CLI-crate integration tests, doubling the default tier.
- **Fix (house convention, mirrors test-perf/test-real-world):** new `test-heavy-contracts` feature; both suites gated with `#![cfg(feature = ...)]`; a third guard test in `perf_gate_convention.rs` so un-gating fails loudly; `contract_plan()` in xtask now runs both with the feature (contract tier is already a Fast Gates CI step, so the suites still run on every push); xtask plan-expectation test extended.
- **Verified:** fmt clean; `cargo test -p xtask` green; default tier rc=0 in 17s locally (was 85s) with both binaries compiling to 0 tests; contract tier rc=0 with deep_recursion 1/1 (39s) and reference_site_identity 3/3 (15s).
- **Impact:** default tier back under budget with real headroom; no coverage lost — the suites moved tiers, they didn't disappear.
