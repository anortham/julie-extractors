---
id: checkpoint_93320481
timestamp: 2026-09-23T12:33:32.574Z
tags:
  - release
  - 3.5.0
  - erlang
  - real-world
  - incident
git:
  branch: fix/erlang-corpus-baseline
  commit: 743751f1
  files:
    - crates/julie-extract-cli/tests/erlang_corpus.rs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Stale Erlang corpus baseline found before tagging 3.5.0
type: incident
context: Nightly Specialist Gates on the 3.4.0 publication commit failed; found
  while preparing the 3.5.0 tag.
evidence:
  - Specialist Gates run 35841361929 Real-World Release failure
  - local cargo xtask test real-world-release exit 0 after baseline update
  - "3.3.1 vs 3.5.0 symbol diff: +variable CP 422, +variable R1 422"
---

## Stale Erlang corpus baseline found before tagging 3.5.0

- **WHAT:** the nightly Specialist Gates run 35841361929 on a8918d82 failed `Real-World Release`: `erlang_corpus_scans_every_file_against_the_committed_baseline` expected 206 symbols for `unicode_util_compat.erl`, got 208 (same on 3.4.0 and 3.5.0).
- **WHY it changed (intended):** 3.4.0 Erlang work stopped a comment between clauses from splitting a function's clause run, so the `gc_1` clauses after `%% Handle control` add parameters `CP` and `R1` (rule: parameter symbols for every clause head). Verified by diffing 3.3.1 vs 3.5.0 symbols for that file: only `variable CP 422`, `variable R1 422` added.
- **FIX:** baseline 206 -> 208 with a note in crates/julie-extract-cli/tests/erlang_corpus.rs (branch fix/erlang-corpus-baseline).
- **Process gap:** real-world-release was not run before the 3.4.0 tag (it is a nightly specialist tier, not per-commit CI). For 3.5.0 ran locally: real-world-release pass (erlang corpus last command), real-world-smoke pass, dogfood pass, certification 42 pass.
- **IMPACT:** 3.5.0 tag goes on the commit with this fix; 3.4.0 shipped with this nightly gate red (baseline only, no product defect).
