---
id: checkpoint_ada9fb30
timestamp: 2026-09-23T08:01:58.354Z
tags:
  - test-roles
  - specflow
  - behat
  - json5
  - 3.5.0
git:
  branch: main
  commit: a8918d82
  worktree: /home/murphy/source/julie-extractors
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: step_definition TestRole + JSON5/regex decisions (feat/gap-followups)
type: decision
decision: A step definition carries test_role=step_definition in metadata only
  and sets no is_test/test_container/test_lifecycle column.
alternatives:
  - "Set is_test=1 for steps: rejected, consumers would count steps as test
    cases"
  - "Add a step_definition capability unit: rejected, changes the fixed
    vocabulary for all 40 languages"
next: Merge gaps/go-module (gomod language) when the agent finishes, add
  go-module-manifest-v1 marker + ledger paragraph, run branch gate + Windows
  default.
---

## step_definition TestRole + JSON5/regex decisions (feat/gap-followups)

- **WHAT:** `TestRole::StepDefinition` ("step_definition"). SpecFlow/Reqnroll: `[Given]/[When]/[Then]/[StepDefinition]` on a method of a `[Binding]` class (all .NET languages via `mark_dotnet_test_containers`, new `binding_ids` set). Behat: new `PhpSuiteFramework::Behat` for classes implementing Context/SnippetAcceptingContext/CustomSnippetAcceptingContext/TranslatableContext -> test_container; `behat_member_role` maps Given/When/Then attrs or docblock tags -> step_definition, Before*/After* (suite/feature/scenario/step) -> fixture hooks.
- **WHY:** closes open gaps `specflow.step_definition_role` and `behat.step_definition_roles` per owner decision 2026-09-23.
- **Key decision:** step_definition sets NO role column (`is_test` stays 0) because the schema contract defines is_test=1 as case-or-hook and consumers count cases as is_test=1 AND test_lifecycle=0. Metadata-only evidence; test_detection capability units unchanged.
- **Also:** JSON5 out-of-scope decision (tree-sitter-json5 0.1.0 needs tree-sitter 0.20), `json5_unquoted_keys` gap removed; `regex_conditional_patterns` reason/closure updated (tree-sitter-regex 0.25.0 latest, no conditional rule). Version 3.5.0, contract marker `step-definition-role-v1`, ledger `## 3.5.0`, plan docs/plans/2026-09-23-gap-followups.md.
- **Evidence:** goldens csharp:step_definitions, php:behat_context (controls get no role), csharp:language_idioms diff (+step_definition). Default tier 5445 passed / 83s; contract 187; xtask 119; clippy 1.97 + 1.98 clean; strict report silent 0, debts 0, backlog 22.
