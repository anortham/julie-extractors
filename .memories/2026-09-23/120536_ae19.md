---
id: checkpoint_ae19456b
timestamp: 2026-09-23T12:05:36.350Z
tags:
  - review
  - codex
  - 3.5.0
  - behat
  - gomod
  - security
git:
  branch: main
  commit: a8918d82
  worktree: /home/murphy/source/julie-extractors
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Codex review of 3.5.0 branch: 4 medium findings fixed"
type: checkpoint
next: Commit fixes, Windows default on final commit, release notes v3.5.0, then
  merge/push/tag/publish.
---

## Codex review of 3.5.0 branch: 4 medium findings fixed

Two-pass Codex review (general + security) of feat/gap-followups at 225e1ed2; 2/2 invocations; no external-model policy declared (diff sent to openai).

- **G1 Behat step roles overwritten** (real): PHPUnit member pass turned a `#[Given] testConnection` into test_case; path rule left is_test on a step. Fix: `apply_test_role(StepDefinition)` clears is_test/test_lifecycle/test_container; the PHPUnit member pass skips Behat containers (`phpunit_container_ids`).
- **G2 generic `Context`** (real): `php_base_types` strips namespaces, so `App\Context` became Behat. Fix: `implements_behat_context` requires the base type to resolve into `Behat\` (qualified, or via `php_behat_imports` from `use Behat\...` import symbols, alias aware). Fixture controls: `AppContext extends App\Context`, `settings.php` with `use App\Support\Context`.
- **G3 gomod unquote** (real): only \n \t \r \\ \" decoded. Fix: `decode_interpreted` implements Go escapes (\a \b \f \n \r \t \v \\ \" \ooo \xhh \uhhhh \Uhhhhhhhh); invalid escape / non-UTF-8 keeps the token as written.
- **S1 quadratic comment lookahead** (real, 1 MiB file ~6e10 steps): replaced per-comment `comment_documents_following_directive` with `gomod::doc_comment_starts` (one bottom-up pass) used by `promote_gomod_doc_comments` post-pass in source_regions.rs.
- **Lead-found related:** retract block rationale copied into every fact (quadratic output). Fix: `retract_rationale` memoizes per block and caps at 500 bytes with `rationale_truncated` (registry key added, patterns JSON regenerated).
- Docs: php.md, gomod.md, decision doc (+ Rust enum consequence), ledger 3.5.0 (Behat evidence rule, rationale cap, Rust API note).
- **Gates:** fmt; clippy 1.97 + 1.98; default 5474 passed; contract 187; xtask; golden 8; capability 40; strict report clean (backlog 21); compat-check vs 3.4.0 accepts 3.5.0.
