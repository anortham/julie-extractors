---
id: checkpoint_f5c75ff2
timestamp: 2026-09-23T07:11:21.589Z
tags:
  - release-3.4.0
  - language-gap-closure
  - decision
git:
  branch: feat/language-gap-closure
  commit: 25c5e093
  files:
    - README.md
    - docs/contracts/extraction-output-changes.md
    - docs/release-notes/v3.4.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: 3.4.0 release approved; release notes written
type: decision
decision: Release 3.4.0 now; do the step-definition role and go.mod language
  after the release; JSON5 out of scope; regex conditionals stay a grammar
  limit.
---

## 3.4.0 release approved; release notes written

**WHAT:** The user approved the 3.4.0 release and my recommendations for the four deferred wave-2 gaps. The gaps worktrees (13) and their merged branches are removed. `docs/release-notes/v3.4.0.md` is written, the README XML line is corrected (XML now publishes relationships and pending relationships), and the 3.4.0 ledger entry no longer says "unreleased".

**Decisions (user, 2026-09-23):**
- Add a `step_definition` TestRole for SpecFlow/Reqnroll and Behat steps: do after the release on a new branch.
- Add a Go module-manifest language for exact basenames `go.mod` / `go.sum`: do after the release on a new branch.
- JSON5 is out of scope: record a decision and retire the json open gap.
- Regex conditionals stay a recorded grammar limit.
- The `feat-qt-*` worktrees are old merged work (in v3.3.0); user did not ask to remove them.

**Release checks on the release-notes commit tree:** compat-check vs 3.3.1 accepts the 3.4.0 compatible entry; `cargo xtask release preflight --version 3.4.0` ok (6 targets, 27 inputs); contract tier, doc sync, `git diff --check`, package-list pass. Earlier on 25c5e093: full Linux gate set and Windows default tier (5064 passed).

**Next:** fast-forward main, push main, wait for CI, annotated tag `v3.4.0` ("julie-extract 3.4.0"), push tag, wait for Release Binaries, verify assets, then the evidence-only publication commit (release evidence doc, `docs/release.md` pointer, release-notes README, site current-release line).
