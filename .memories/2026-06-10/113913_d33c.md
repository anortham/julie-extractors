---
id: checkpoint_d33cd0b7
timestamp: 2026-06-10T11:39:13.595Z
tags:
  - julie-extractors
  - language-quality
  - phase-0
  - capability-matrix
git:
  branch: feature/extraction-data-quality
  commit: 6b29116
  files:
    - crates/julie-extractors/src/tests/capability_matrix.rs
    - crates/julie-extractors/src/tests/capability_snapshot_test.rs
    - docs/findings/2026-06-09-language-coverage-review.md
    - fixtures/extraction/capabilities.json
    - scripts/language-data-quality-report.mjs
summary: Phase 0 language-quality gate implemented
briefId: julie-extractors-best-tree-sitter-implementation-f
type: checkpoint
evidence:
  - "RED: cargo nextest run -p julie-extractors --features
    test-capability-matrix -E
    'test(capability_matrix_has_no_silent_kind_coverage_cells)' failed with 116
    silent cell messages."
  - "GREEN: cargo nextest run -p julie-extractors --features
    test-capability-matrix -E
    'test(capability_matrix_has_no_silent_kind_coverage_cells)' passed, 1/1."
  - "Capability matrix suite: cargo nextest run -p julie-extractors --features
    test-capability-matrix capability_matrix passed, 36/36."
  - "Snapshot test: cargo nextest run -p julie-extractors -E
    'test(test_capability_snapshot_deserializes_mixed_legacy_and_kind_coverage_\
    rows)' passed, 1/1."
  - "Scorecard: node scripts/language-data-quality-report.mjs --strict reported
    silent_cells: 0 and quality_bar_debts: 98."
  - git diff --check passed.
next: Commit Phase 0. Then start the first extractor-depth slice by closing
  literal/doc-comment/annotation gaps for a small C-family or scripting-language
  batch.
confidence: 5
---

## Phase 0 language-quality gate implemented

### WHAT
- Added `scripts/language-data-quality-report.mjs` to compute fixture-proven domain counts, silent cells, and quality-bar debt from `capabilities.json` plus golden fixtures.
- Added `capability_matrix_has_no_silent_kind_coverage_cells` to fail when any `kind_coverage` domain has no `supported`, `not_applicable`, or `open_gaps` entry.
- Filled 116 formerly silent `kind_coverage` cells in `fixtures/extraction/capabilities.json` with explicit `open_gaps` closure tasks rather than false support or false non-applicability.
- Updated capability snapshot regression coverage for deserializing `open_gaps` rows.
- Recorded the Phase 0 scorecard in `docs/findings/2026-06-09-language-coverage-review.md`.

### WHY
- The high-bar plan needs gaps to be visible as debt before extractor work can close them.
- This does not lower the bar: `silent_cells` is now 0, but `quality_bar_debts` remains 98 and is tracked explicitly.

### HOW
- TDD red/green: added the silent-cell capability test, verified it failed on 116 cells, then updated the matrix and verified it passed.
- The scorecard script exits nonzero in `--strict` mode when silent cells exist.

### IMPACT
- Future language-quality work cannot silently leave a domain blank.
- The next slice can close concrete open gaps and watch `quality_bar_debts` decrease.
