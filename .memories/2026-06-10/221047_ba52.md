---
id: checkpoint_ba526ccd
timestamp: 2026-06-10T22:10:47.032Z
tags:
  - julie-extractors
  - data-quality
  - applicability
  - cursor-agent
  - audit
git:
  branch: feature/extraction-data-quality
  commit: 001ca76
  files:
    - docs/findings/2026-06-09-language-coverage-review.md
    - scripts/language-data-quality-report.mjs
summary: Completed raw-gap applicability audit
briefId: julie-extractors-best-tree-sitter-implementation-f
type: checkpoint
context: After scorecard v2 landed, the next plan item was a raw-gap
  applicability audit for relationships, identifiers, body_spans,
  source_regions, pending_relationships, and types. Cursor Agent performed the
  audit and the lead corrected classifications to preserve the high quality bar.
impact: Enables focused implementation work on true debt instead of broad
  raw-gap chasing.
evidence:
  - "node scripts/language-data-quality-report.mjs --strict: pass; silent_cells
    0; quality_bar_debts 0; relationships native_debt ruby; types native_debt
    qml, convention_only lua/r"
  - "git diff --check: pass"
  - "cargo fmt --check: pass"
  - "git diff Phase 17 non-ASCII check: no new non-ASCII output"
next: Commit the audit slice, then delegate or implement the native debt closure
  for Ruby relationships and QML types.
---

## Completed raw-gap applicability audit

- **WHAT:** Classified all scorecard-v2 unclassified gaps for `relationships`, `identifiers`, `body_spans`, `source_regions`, `pending_relationships`, and `types` in script-local applicability metadata and documented Phase 17 in the findings doc.
- **WHY:** Raw missing rows needed to be separated into true non-applicability, convention-only product decisions, and real implementation debt before assigning larger implementation batches.
- **HOW:** Used Cursor Agent for the audit, then corrected weak classifications during lead review: `types.qml` is `native_debt` because QML has typed properties/functions and existing type inference but no `TypeInfo` rows; `types.lua` and `types.r` are `convention_only` rather than `not_applicable` because they have type-like runtime/documentation conventions.
- **IMPACT:** The six audited domains now have `unclassified_gaps: 0`. Real native debt is narrowed to Ruby `relationships` and QML `types`; Lua/R types are product-decision follow-ups, not native extractor debt.
