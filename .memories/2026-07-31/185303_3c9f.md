---
id: checkpoint_3c9f0d0b
timestamp: 2026-07-31T18:53:03.021Z
tags:
  - erlang
  - xml
  - julie-extractors
  - language-support
  - issue-8
  - plan
git:
  branch: main
  commit: 4bee2fe2
summary: "Erlang + XML language support: design + plan ready for approval"
type: checkpoint
next: Await plan approval, then execute via
  razorback:subagent-driven-development in the worktree
---

## Erlang + XML language support: design + plan ready for approval

**WHAT:** Investigated Miller issue #8, designed and planned Erlang + XML language support for julie-extractors. Design doc and implementation plan committed on branch `erlang-xml-language-support` in worktree `/Users/murphy/source/julie-extractors/.worktrees/erlang-xml-language-support` (commits ac62c55f design, e3d5dc67 plan).

**WHY:** Issue #8 reported "Erlang/Xml extractor" parse failures on Miller v1.14.1 + julie-extract 2.20.0. Repro proved the error strings are fabricated (no such extractors exist; `.erl`/`.xsd` are recorded as unsupported, not failed). Real gap: zero index coverage for Erlang deps in BEAM workspaces and XML/XSD. User chose to add both languages.

**HOW (key decisions):**
- Erlang: `.erl`/`.hrl`, FULL_CAPABILITIES at elixir parity, tree-sitter-erlang 0.20.0 (crates.io, verified live — Codex's 0.19.0 claim was stale GitHub data)
- XML: `.xml`/`.xsd`/`.wsdl`, DATA_ONLY (name-promoted symbols + QName identifiers), hybrid yaml parent-chain + html element filtering
- 1MB MAX_SOURCE_FILE_BYTES stands — big XML is generated codegen input (AXL XSDs duplicate already-indexed generated Java); no per-language override
- Codex adversarial review (SHIP WITH CHANGES) surfaced: stale-row bug when an indexed file grows past 1MB (`no_change` path in commands.rs:1445 — plan Task 5 fixes it), Miller auto source corpus also 1MB-capped so manual `content import` (25MB) is the real escape hatch, 36-language hardcoded assertions at registry.rs:676/factory.rs:60/capability_snapshot_test.rs:8
- Toolchain gotcha: global `stable` rustup is 1.94.0 but repo needs ≥1.95 — use RUSTUP_TOOLCHAIN=1.97.1

**IMPACT:** 10-task plan (grammar spike risk-first → Erlang lane T2/4/6/7/8, XML lane T3/9, oversized fix T5, branch gates T10) awaiting user approval, then subagent-driven-development execution. Baseline verified green (3,415 passed). Post-merge chain: julie-extract 2.21.0 release → Miller pin bump → 36→38 docs → issue #8 reply, each gated on user approval.
