---
id: checkpoint_a32340bc
timestamp: 2026-07-31T14:56:05.519Z
tags:
  - docs
  - readme
  - cleanup
git:
  branch: main
  commit: 44e32a9e
  files:
    - README.md
summary: README cleanup and humanizer pass
---

## README cleanup and humanizer pass

Rewrote `README.md` from 415 lines down to ~215, per user request to cut useless info and make it user friendly.

- **Removed:** Current Release section (version, publish date, commit, workflow run, SHA-256 asset table — all shown on the GitHub release page), ~70 historical release-notes/evidence links in the Documentation Map, v2.3.0-era Data Quality narrative with stale scorecard/dogfood numbers, the 21-item JSONL record-order list, the SQLite index enumeration, the v1.22.0 lineage note, the local `/Users/murphy/source/julie` path.
- **Fixed drift:** prose said SQLite schema v4 / JSONL v3 / extracted-data-v3 while contract docs are v5 / v4 / v4. Verified against `docs/contracts/sqlite-schema-v5.md`, `jsonl-v4.md`, `reports.md` (report_schema_version stays 3).
- **Kept:** Install (now pointing at releases/latest), Quickstart, CLI table, trimmed artifact contract, reports/status values, supported languages, quality-bar summary with strict gate command, grammar-fork pointer, non-goals, dev gates, trimmed docs map. Release notes now referenced via the `docs/release-notes/` directory instead of per-version links.
- **Style:** humanizer skill applied — sentence-case headings, no em dashes, no promotional filler.
- **Verified:** every remaining relative link resolves to a real file.

Uncommitted; user has not asked for a commit yet.
