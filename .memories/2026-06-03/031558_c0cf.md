---
id: checkpoint_c0cfc797
timestamp: 2026-06-03T03:15:58.765Z
tags:
  - julie-extractors
  - ci-fix
  - clippy
  - v2.0.3
git:
  branch: main
  commit: 7e08b63
  files:
    - xtask/src/test_tiers.rs
summary: Fixed v2.0.3 fast-gates clippy failure
briefId: julie-extractors-product-completion-focus
type: checkpoint
evidence:
  - gh run view 26861338302 --log-failed showed clippy::manual_contains at
    xtask/src/test_tiers.rs:360
  - gh run cancel 26861340930 submitted; gh run view later showed
    conclusion=cancelled
  - git ls-remote --tags origin 'v2.0.3' returned no tag
  - gh release view v2.0.3 returned no release
  - cargo clippy --no-deps -p julie-extract-artifact -p julie-extract-cli -p
    xtask --lib --bins -- -D warnings passed locally
  - cargo test -p xtask --test test_tiers
    test_changed_expected_golden_output_requires_extractor_contract_review
    passed
  - git diff --check passed
next: Commit and push the clippy fix, wait for CI fast gates to pass, then rerun
  the Release Binaries workflow for version 2.0.3.
confidence: 5
---

## Fixed v2.0.3 fast-gates clippy failure

- **WHAT:** Replaced an `iter().any()` equality check in `xtask/src/test_tiers.rs` with `changed_paths.contains(&"crates/julie-extractors/src/lib.rs")`.
- **WHY:** GitHub CI fast gates failed on `cargo clippy --no-deps -p julie-extract-artifact -p julie-extract-cli -p xtask --lib --bins -- -D warnings` due to `clippy::manual_contains` in the v2.0.3 release-prep commit.
- **HOW:** Read failed CI logs for run `26861338302`, identified the exact lint at `xtask/src/test_tiers.rs:360`, canceled the in-flight release workflow `26861340930` before publish, applied the clippy-suggested contains form, and reran the failing command locally.
- **IMPACT:** Fast-gates clippy failure is fixed locally. No v2.0.3 tag or GitHub Release exists from the canceled workflow, so the release can be rerun from the fixed commit after pushing.
