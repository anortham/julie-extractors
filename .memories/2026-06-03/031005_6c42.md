---
id: checkpoint_6c42c793
timestamp: 2026-06-03T03:10:05.127Z
tags:
  - julie-extractors
  - release
  - v2.0.3
  - contract-version
git:
  branch: main
  commit: acc9388
  files:
    - Cargo.lock
    - crates/julie-extract-artifact/Cargo.toml
    - crates/julie-extract-cli/Cargo.toml
    - crates/julie-extractors/Cargo.toml
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/tests/downstream_smoke.rs
    - docs/release-notes/README.md
    - docs/release-notes/v2.0.3.md
    - xtask/src/test_tiers.rs
    - xtask/tests/test_tiers.rs
summary: Prepared v2.0.3 contract-version patch release
briefId: julie-extractors-product-completion-focus
type: checkpoint
evidence:
  - cargo fmt --check passed
  - cargo test -p xtask passed
  - cargo xtask test changed Cargo.lock crates/julie-extract-artifact/Cargo.toml
    crates/julie-extract-cli/Cargo.toml crates/julie-extractors/Cargo.toml
    crates/julie-extractors/src/lib.rs
    crates/julie-extractors/tests/downstream_smoke.rs
    docs/release-notes/README.md docs/release-notes/v2.0.3.md
    xtask/src/test_tiers.rs xtask/tests/test_tiers.rs passed
  - cargo xtask test contract passed
  - cargo test -p julie-extractors --features test-downstream-smoke --test
    downstream_smoke
    julie_extractors_works_as_path_dependency_in_downstream_crate -- --exact
    passed
  - cargo build --release -p julie-extract-cli --bin julie-extract passed
  - cargo xtask release package --version 2.0.3 --target aarch64-apple-darwin
    --out-dir target/release-package/v2.0.3-aarch64-apple-darwin --binary
    target/release/julie-extract passed
  - shasum -a 256 -c dist/aarch64-apple-darwin/julie-extract.sha256 passed
  - target/release/julie-extract --version returned julie-extract 2.0.3
  - git diff --check passed
next: Commit, push main, run the Release Binaries workflow for version 2.0.3,
  then verify GitHub release assets and update README/release evidence with
  published hashes.
confidence: 5
---

## Prepared v2.0.3 contract-version patch release

- **WHAT:** Bumped all `julie-extractors` workspace crate versions and `Cargo.lock` to `2.0.3`, updated `EXTRACTION_CONTRACT_VERSION` to `2026-06-03.ecmascript-swift-shape-v3`, added `docs/release-notes/v2.0.3.md`, and added changed-path guards for golden expected-output fixture review plus xtask self-test routing.
- **WHY:** v2.0.2 was already published with a stale Rust crate extraction contract string after Swift and ECMAScript extraction-shape changes; downstream Rust consumers that compose the string into their own index version need a visible invalidation signal.
- **HOW:** Kept the artifact integer `extract_contract_version = 1` unchanged, because SQLite/JSONL/CLI artifact contracts did not change. Guarded future golden `expected.json` changes through `cargo xtask test changed` by requiring `crates/julie-extractors/src/lib.rs` in the changed path set.
- **IMPACT:** Release prep is ready to commit and publish as v2.0.3. Local verification passed: `cargo fmt --check`, `cargo test -p xtask`, full changed-path gate with certification, `cargo xtask test contract`, explicit downstream smoke, release build, local aarch64 package staging, checksum verification, and `git diff --check`.
