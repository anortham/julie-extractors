---
id: checkpoint_cb309e16
timestamp: 2026-06-03T01:00:57.646Z
tags:
  - performance
  - sqlite
  - write-path
  - merge
  - main
  - housekeeping
  - todo
  - branch-cleanup
  - milestone
git:
  branch: main
  commit: 1315c69
summary: Merged write-path + parallel-extraction to main; repo housekeeping
briefId: julie-extractors-product-completion-focus
type: checkpoint
impact: Performance work is on main and fully verified; openclaw cold scan
  38.0s→24.5s, hermes 13.7s→9.3s, byte-identical output. Open follow-ups are now
  tracked in a verified TODO.md instead of a stale review doc.
next: Decide whether to push main (11 commits) to origin/main. Then optionally
  tackle TODO.md item 1 (export --out - stdout-discipline fix, commands.rs:792)
  — one-line fix + test.
confidence: 5
---

## Merged write-path + parallel-extraction to main; repo housekeeping

Integrated the completed performance work into `main` and cleaned up tracking docs. Branch `main` is now **11 commits ahead of `origin/main`, not yet pushed** (push decision still open).

### Merges (both `--no-ff`, verified)
- `26fbd03` Merge `perf/parallel-extraction` (rayon scan extraction + malformed-file panic hardening)
- `2bbe665` Merge `perf/sqlite-write-path` (5 commits; final step `8b316d0` fold symbol parent landed just before merge)
- The two branches were stacked linearly (write-path ⊇ parallel-extraction); `--no-ff` gives two revertable merge bubbles.
- **Merged main tree is byte-identical to the A/B-verified `8b316d0` tip.**

### Post-merge verification on main (all green)
- Tests: 2282 core + all suites pass, 0 failed, 7 ignored (slow gates correctly excluded)
- `cargo fmt --all --check` clean; **exact CI clippy** (`-p julie-extract-artifact -p julie-extract-cli -p xtask --lib --bins -D warnings`) exit 0
- `scripts/check-agent-doc-sync.sh` synced
- The 484 `julie-extractors` clippy warnings are pre-existing and **out of CI scope** (CI only gates the 3 product crates)

### Authoritative clean A/B (interleaved, cold force scan)
- openclaw artifact_write 31,942 → 18,728ms (**−41%**); full scan 38.0s → 24.5s
- hermes artifact_write 10,956 → 6,639ms (**−39%**); full scan 13.7s → 9.3s
- Integrity IDENTICAL both corpora (parent=298,576, containing=1,087,006, target=0)

### Housekeeping
- Deleted merged branches `perf/parallel-extraction` + `perf/sqlite-write-path` (commits preserved in main)
- `9721390` Added root `TODO.md` (verified open items: export `--out -` stdout bug at commands.rs:792, cargo-deny gate, md5→md-5 dep, legacy clippy residual) and removed stale v0.1.0 `docs/findings/CC_REVIEW.md`
- `1315c69` checkpoint memory
- Caught + fixed a staging bug: first docs commit omitted TODO.md (git add aborted on already-deleted pathspec); soft-reset and redid so the commit honestly contains both files
- Left dated 2026-06-01 plans/memory referencing CC_REVIEW intact as historical record
