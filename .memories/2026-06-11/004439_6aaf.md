---
id: checkpoint_6aaff67e
timestamp: 2026-06-11T00:44:39.440Z
tags:
  - code-review
  - release-prep
  - vue
  - complexity-metrics
  - cli
git:
  branch: feature/extraction-data-quality
  commit: 5dee5cd
  files:
    - crates/julie-extract-cli/src/commands.rs
    - crates/julie-extractors/src/base/complexity_metrics.rs
    - crates/julie-extractors/src/regex/complexity_metrics.rs
    - crates/julie-extractors/src/sql/complexity_metrics.rs
    - crates/julie-extractors/src/vue/mod.rs
    - fixtures/extraction/vue/basic/expected.json
    - fixtures/extraction/vue/cross_file/expected.json
summary: Pre-release review fixes on feature/extraction-data-quality
briefId: julie-extractors-best-tree-sitter-implementation-f
type: checkpoint
evidence:
  - "cargo nextest run --workspace: 2625 passed, 7 skipped"
  - "cargo clippy --workspace --all-targets: clean"
  - "vue fixtures: containing_key attachment identical before/after"
next: User to review diff and decide on committing + cutting the release.
---

## Pre-release review fixes on feature/extraction-data-quality

Multi-agent code review of the branch (7 finder angles, adversarial verify) found 3 issues; all fixed.

### WHAT
1. **Vue component span convention** (`vue/mod.rs`): component symbol reported `end_line = lines().count() + 1` (line 42 of a 41-line file). Now ends on the real last line with `end_column = last_line.len() + 1`, so `end_byte` still covers the file content and byte-based containment of section facts is unchanged. Regenerated vue basic + cross_file golden fixtures (`UPDATE_GOLDEN=1 cargo nextest run -p julie-extractors --features test-golden golden`); verified all structural facts / source regions / literals still attach to the component symbol.
2. **Triplicated span helpers**: `overlaps`, `contains`, `symbol_span`, `body_covers_meaningful_share` were byte-identical in base/, sql/, regex/ complexity_metrics modules. Now `pub(crate)` in `base::complexity_metrics`, imported by sql + regex.
3. **CLI duplicate kind_coverage blocks** (`julie-extract-cli/src/commands.rs`): two identical 10-domain JSON blocks (languages cmd + artifact snapshot) consolidated into shared `kind_coverage_json(&CapabilityKindCoverage)` helper.

### WHY
Pre-release review: the N+1 span was a bandaid for fact containment (widened span instead of correct end position) and put a nonexistent line number in the artifact contract; the duplications were silent-divergence hazards.

### IMPACT
- Full default suite: 2625 passed. Clippy clean, fmt clean.
- Review also REFUTED 10 other candidates (no panic paths in new base/*_structural_facts collectors; capability snapshot stored as JSON blob so serde defaults keep old artifacts readable; structural-fact collectors gate by language before walking).
- Known leftover: repo still has mixed whole-file span conventions (bash/regex use N+1 from tree-sitter root nodes; elixir/vbnet/vue now use N) — pre-existing, not blocking.
- Minor gap noted: xtask commands_contract doesn't assert the 4 new kind_coverage JSON keys.
