---
id: checkpoint_ebb1040c
timestamp: 2026-07-02T03:41:10.833Z
tags:
  - julie-extractors
  - lane-3
  - pattern-registry
  - structural-facts
  - sdd
git:
  branch: feature/structural-fact-pattern-registry
  commit: 978aef4
  files:
    - docs/plans/2026-07-01-structural-fact-pattern-registry.md
summary: Lane 3 (pattern registry) — Tasks 1 & 2 complete on
  feature/structural-fact-pattern-registry
briefId: julie-extractors-post-v2-5-10-roadmap-http-boundar
type: checkpoint
next: Review Task 3 (JSON export + sync test), then Task 4 (languages --json),
  then branch gate + finishing. Lane 4 (engineering health) follows on its own
  branch.
---

## Lane 3 (pattern registry) — Tasks 1 & 2 complete on feature/structural-fact-pattern-registry

**WHAT:** The structural-fact pattern registry now exists and is conformance-tested. Task 1 (commit 8c0bc47): `base/structural_fact_registry.rs` with 144 typed specs (10 built-in aggregator patterns + 67 code + 26 data + 12 sql + 8 framework + 21 web) — the plan's "63" was a stale undercount. Task 2 (commits c8f1628 + 978aef4): `MetadataValueType` extended with `ObjectArray` (D1 adjudication) and a golden-corpus conformance test (`structural_facts_conform_to_registry`, gated `test-golden`) proving emission↔declaration agreement over all 125 fixture sources.

**WHY:** Lane 3 of the post-v2.5.10 roadmap — machine-readable pattern registry to end the M4-style doc-drift class.

**HOW / key decisions:**
- Set-equality invariant compares against the aggregator `structural_fact_pattern_ids_for_language` and is gated `test-capability-matrix` (all collector pattern-id fns are gated there); zero collector edits needed.
- D1: razor `route_parameters` (array of objects, shipped v2.5.x payload) → extend registry vocabulary with `ObjectArray`, do NOT change emission. Registry enum additions are lead-adjudicated contract decisions.
- D4 watch list (9 gating-derived `Always` keys) all confirmed by corpus — 0 flips.
- Conformance test teeth verified via 4 deliberate reverted breaks.

**IMPACT:** Tasks 3 (checked-in JSON export, in progress via l3-impl-3) and 4 (languages --json section) consume `structural_fact_pattern_specs()`. Lead gates green @978aef4: conformance 8/8, golden byte-compare 1/1, strict report 0/0. Baseline was 2732/0 @3e3d5f8.
