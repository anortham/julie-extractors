---
id: checkpoint_a3e08d2a
timestamp: 2026-07-02T13:27:05.194Z
tags:
  - julie-extractors
  - binding-fix
  - containing-symbol
  - contract-change
  - miller-bridge
  - complete
git:
  branch: feature/containing-symbol-binding-fix
  commit: f04e660
  files:
    - docs/plans/2026-07-02-containing-symbol-binding-fix.md
summary: Containing-symbol binding fix COMPLETE on feature/containing-symbol-binding-fix
briefId: julie-extractors-post-v2-5-10-roadmap-http-boundar
type: checkpoint
next: "Ask user: merge feature/containing-symbol-binding-fix to main; decide
  push (Lane 4 + fix) and whether to cut v2.6.1 so Miller can pin the fixed
  bindings."
---

## Containing-symbol binding fix COMPLETE on feature/containing-symbol-binding-fix

**WHAT:** Both Miller-reported binding defects fixed, contract-swept, and gate-verified. Commits: 8f55ba7 (plan), 41b8964 (shared binder `base/containing_symbol.rs` — six collector copies consolidated to one, RED-first repro tests), 62c3ba5 (denylist shrunk to `{Variable, Constant, EnumMember, Import}` per lead adjudication), 66b8e51 (41 goldens regenerated across 23 languages + `EXTRACTION_CONTRACT_VERSION` marker `.containing-symbol-binding-v2` + api_surface), f04e660 (contract docs semantics prose).

**Fixed behavior (e2e-verified with the rebuilt CLI):**
- `export const POST = async () =>` route handlers: containing_symbol_id NULL → binds the `POST` function symbol (line-containment fallback).
- `const res = await fetch(...)`: bound `res` variable → binds the enclosing function (kind filter).

**Key adjudications:** Field/Property KEPT as containment candidates (first-class members, accessor bodies are real scopes, class recoverable via parentage); module-scope value-holder facts now NULL by design (container semantics, not owner semantics — e.g. Zig `const std = @import`).

**Golden impact (hard-gated, parse-based multiset check):** 86 facts — 56 rebound / 2 newly-bound / 28 unbound; all other arrays byte-identical; no rows added/removed.

**Branch gate @f04e660:** workspace 2755/0, fmt/clippy clean, xtask default 5/0 + contract 32/0, strict 0/0, registry conformance 9/9, capability-matrix 9/9.

**IMPACT:** contract change → next release (2.6.1) carries the new binding semantics; Miller's bridge can drop any variable-hop workaround at the pin bump. Local `main` still holds unpushed Lane 4 (5 commits); this branch adds the fix on top — both await integration/push approval.
