---
id: checkpoint_040aef7d
timestamp: 2026-08-24T16:15:19.004Z
tags:
  - qml
  - task-5
  - testing
  - real-world
git:
  branch: feat/qml-first-class-extraction
  commit: 3239f08d
  files:
    - crates/julie-extractors/src/tests/golden.rs
    - docs/languages/qml.md
    - docs/plans/2026-08-24-qml-first-class-extraction-implementation-plan.md
    - docs/testing-strategy.md
    - xtask/src/test_tiers.rs
    - xtask/tests/test_tiers.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Task 5 complete before commit
briefId: qml-first-class-extraction-and-testing
type: checkpoint
impact: Unblocks the QML per-language certification path and documents
  reproducible real-world evidence.
evidence:
  - cargo test -p xtask --test test_tiers
  - cargo xtask test language qml
  - cargo xtask test language qmldir
  - cargo build --locked --bin julie-extract
symbols:
  - CommandSpec
  - run_plan
  - language_plan
  - golden_fixtures_match_canonical_extraction
next: Lead runs normal golden, certification/parser-upgrade, default/contract,
  Windows, security, and Grok review gates.
---

## Task 5 complete before commit

### WHAT

Added deterministic per-command environment entries to xtask test plans. Per-language targets now run the unit filter and an exact golden command scoped by `JULIE_GOLDEN_LANGUAGE`; the golden harness filters the capability matrix to that language and rejects unknown filters. Added QML testing documentation and recorded a KDE Plasma Framework evidence scan.

### WHY

QML and basename-only qmldir need repeatable continuous targets without making the default, certification, or real-world plans accidentally run a language-specific golden corpus.

### HOW

`CommandSpec::with_env` stores sorted owned key/value pairs, `run_plan` applies them through `Command::env`, and display output includes the environment deterministically. The authorized shallow checkout used KDE plasma-framework commit `0806864a1e7c200ee8872074a4c16be7e1ce3358`; no third-party code was executed. The temporary checkout and SQLite artifact were removed after collecting counts and row evidence.

### IMPACT

The focused xtask suite has 22 passing tests. `cargo xtask test language qml` passed 127 unit tests plus 5 filtered goldens, and `cargo xtask test language qmldir` passed 4 unit tests plus 5 filtered goldens. Lead still owns branch-wide contract/default/Windows gates and final Grok review.
