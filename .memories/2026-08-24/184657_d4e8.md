---
id: checkpoint_d4e8c6b2
timestamp: 2026-08-24T18:46:57.838Z
tags:
  - release
  - v2.35.1
  - verification
  - clippy
  - windows
git:
  branch: main
  commit: 6d4eb127
  files:
    - crates/julie-extract-artifact/tests/store_maintenance_contract.rs
    - crates/julie-extractors/src/base/code_structural_facts.rs
    - crates/julie-extractors/src/qml/typeinfo.rs
    - crates/julie-extractors/src/qmldir/mod.rs
    - xtask/src/test_tiers.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: julie-extractors-v2-35-1-qml-release
type: incident
context: "Exact-candidate release gates on 6d4eb127 exposed four hard-gate
  failures: Clippy warnings in QML/xtask code and a Windows full-suite heartbeat
  handoff timeout."
evidence:
  - "RED: cargo clippy --workspace --all-targets --all-features -- -D warnings
    failed at test_tiers.rs, code_structural_facts.rs, qml/typeinfo.rs,
    qmldir/mod.rs"
  - "RED: Windows cargo xtask test default failed
    maintenance_heartbeat_fails_closed_after_lease_takeover after 5-second
    handoff timeout"
  - "Focused Windows reproduction: failing test passed 3/3; sibling heartbeat
    tests passed"
  - "GREEN: cargo test -p xtask"
  - "GREEN: cargo xtask test language qml"
  - "GREEN: cargo xtask test language qmldir"
  - "GREEN: focused maintenance heartbeat test"
  - "GREEN: cargo clippy --workspace --all-targets --all-features -- -D warnings"
  - "GREEN: git diff --check"
next: Commit the bounded corrections, rerun exact-SHA
  Linux/Windows/security/release gates, then publish only if all pass.
---

## WHAT

Resolved all release-gate findings before publication: simplified deterministic environment sorting, collapsed two semantics-preserving QML nested-let shapes, replaced the over-parameterized `qmldir` symbol helper with `TypeSymbolMode`, and increased three test-only heartbeat handoff scheduling bounds from 5 to 15 seconds.

## WHY

`cargo clippy --workspace --all-targets --all-features -- -D warnings` failed on four code shapes. The Windows default suite also timed out waiting for a test thread to enter its controlled takeover window under suite load, while the same test passed 3/3 in isolation and both sibling heartbeat tests passed.

## HOW

Systematic debugging established each root cause before correction. Three non-overlapping Luna packets made bounded edits; lead review used Miller impact/full inspection and found one missed timeout occurrence, which was corrected. The timeout change preserves every fail-closed assertion and only widens the scheduler handoff budget.

## IMPACT

The candidate is Clippy-clean, QML and `qmldir` outputs remain golden-identical, xtask ordering remains deterministic, and Windows full-suite verification can be rerun on a clean correction commit.
