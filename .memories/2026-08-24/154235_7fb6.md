---
id: checkpoint_7fb6f148
timestamp: 2026-08-24T15:42:35.657Z
tags:
  - qml
  - task-4
  - recovery
  - qmldir
  - contract
git:
  branch: feat/qml-first-class-extraction
  commit: 1bc5b2bf
  files:
    - crates/julie-extract-cli/tests/operations_contract.rs
    - crates/julie-extractors/src/base/code_structural_facts.rs
    - crates/julie-extractors/src/base/structural_fact_registry/builtins/extra.rs
    - crates/julie-extractors/src/base/structural_fact_registry/tests.rs
    - crates/julie-extractors/src/tests/capability_matrix.rs
    - crates/julie-extractors/src/tests/golden.rs
    - crates/julie-extractors/src/tests/qml/structural_facts.rs
    - crates/julie-extractors/src/tests/structural_fact_registry.rs
    - docs/plans/2026-08-24-qml-first-class-extraction-implementation-plan.md
    - fixtures/extraction/capabilities.json
    - fixtures/extraction/qml/basic/expected.json
    - fixtures/extraction/qml/cross_file/LocalCard.qml
    - fixtures/extraction/qml/cross_file/Module.qmltypes
    - fixtures/extraction/qml/cross_file/expected.json
    - fixtures/extraction/qml/cross_file/source.qml
    - fixtures/extraction/qml/test_roles/expected.json
    - fixtures/extraction/qml/typeinfo/expected.json
    - fixtures/extraction/qml/typeinfo/source.qmltypes
    - fixtures/extraction/qmldir/basic/expected.json
    - fixtures/extraction/qmldir/basic/qmldir
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 4 recovery: QML/QMLDIR facts and goldens"
briefId: qml-first-class-extraction-and-testing
type: incident
context: The recovery brief explicitly required honest basename-only `qmldir`
  support and all assigned gates; the original worker stopped because the golden
  harness lacked multi-file support, which is now implemented in the current
  diff.
impact: The extractor-side QML/QMLDIR evidence is green; the remaining blocker
  is a shared CLI store-fixture helper contract that assumes extension-bearing
  source files.
evidence:
  - cargo xtask test language qml — 127 passed
  - cargo xtask test golden — 5 passed
  - cargo xtask test capability — 39 + 1 passed
  - node scripts/language-data-quality-report.mjs --strict — silent_cells=0,
    quality_bar_debts=0
  - cargo test -p julie-extract-cli --test operations_contract
    languages_json_emits_capability_snapshot_data — passed
  - cargo xtask test contract — blocked by store_equivalence qmldir fixture
    source assumption
  - cargo fmt --all -- --check and git diff --check — passed
symbols:
  - collect_code_structural_facts
  - structural_fact_pattern_specs
  - golden_fixtures_match_canonical_extraction
  - capability_matrix_matches_registry_entries
  - languages_json_emits_capability_snapshot_data
next: Lead should decide whether to expand Task 4 ownership for the
  store-equivalence fixture helper/test path or defer the QMLDIR matrix row to
  its dedicated Task 5.
confidence: 4
unknowns:
  - The checked-in structural-fact JSON contract remains outside this packet and
    was not regenerated; focused registry contract-sync test is still a separate
    failure if run with test-capability-matrix because the artifact is stale for
    the new specs.
---

## Task 4 recovery: QML/QMLDIR facts and goldens

### WHAT
Recovered the stalled Task 4 packet on `feat/qml-first-class-extraction`. The current diff now contains QML domain-fact registration, normalized import metadata, QML object/typeinfo facts, complete multi-source golden extraction, QMLDIR manifest facts/golden evidence, capability-matrix evidence, and basename-only `qmldir` validation.

### WHY
Task 4 must publish durable QML/QMLDIR structural-fact contracts and authoritative artifact evidence without inventing a `.qmldir` extension or using cross-file resolution in the golden harness.

### HOW
Miller workspace `f882d90413a5` was refreshed; changed source files and central symbols were inspected, registry/golden seams were traced, and impact was checked. The language-spec line for `qmldir` was already restored to `&[]`; the capability harness and matrix now enforce basename-only `qmldir`. A registry invariant omission for `code.marker.v1` was corrected within approved registry-test ownership. The CLI `languages --json` contract test was updated to require empty extensions only for `qmldir` after the first contract failure.

### IMPACT
Focused QML, golden, capability, strict-quality, formatting, and the corrected CLI contract test pass. The full contract gate still reaches two store-equivalence tests that call `write_all_language_fixture`, which currently requires every language fixture to contain a `source.*` file and fails on the intentional basename-only `qmldir` fixture. That helper/test path is outside the current ownership packet and needs a bounded follow-up or ownership expansion before serial-worker commit.
