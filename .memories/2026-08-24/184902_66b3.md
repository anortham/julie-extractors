---
id: checkpoint_66b392b3
timestamp: 2026-08-24T18:49:02.836Z
tags:
  - release
  - v2.35.1
  - formatting
git:
  branch: main
  commit: 5a325c89
  files:
    - crates/julie-extractors/src/qmldir/mod.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: julie-extractors-v2-35-1-qml-release
type: checkpoint
evidence:
  - "RED: cargo fmt --check failed on 5a325c89"
  - "GREEN: cargo fmt --check"
  - "GREEN: cargo xtask test language qmldir"
  - "GREEN: git diff --check"
next: Commit formatting/checkpoint and restart every exact-SHA gate.
---

## WHAT

Applied the formatter-required layout to the new `qmldir` `TypeSymbolMode` call sites after the exact-SHA release gate caught the mismatch.

## WHY

`cargo fmt --check` is a hard release gate and failed on commit `5a325c89`.

## HOW

A bounded Luna packet ran `cargo fmt --all`, confirmed only `crates/julie-extractors/src/qmldir/mod.rs` changed, and reran formatting, `qmldir` language/golden tests, and whitespace checks.

## IMPACT

The candidate needs a new exact SHA and a complete release-matrix restart; no runtime behavior changed.
