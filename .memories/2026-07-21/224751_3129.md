---
id: checkpoint_31292902
timestamp: 2026-07-21T22:47:51.866Z
tags:
  - discovery
  - scan-scope
  - dotnet-artifacts
git:
  branch: main
  commit: 1cf7291a
summary: Hard-excluded .NET build artifacts from discovery (obj/, TestResults/)
type: checkpoint
---

## Hard-excluded .NET build artifacts from discovery (obj/, TestResults/)

**WHAT:** Added `obj` and `TestResults` to `HARD_EXCLUDE_DIRS` in `crates/julie-extract-cli/src/discovery.rs` with two directory-exclusion tests (`.cs` files inside each dir, proving dir exclusion rather than extension rejection). Commit 1cf7291a on local main, NOT pushed.

**WHY:** Miller dogfood night found churning `TestResults/*.trx` rows wedging Miller's content-corpus/vector pipeline. Miller-side root cause (revision-blind corpus freshness) is fixed in Miller; this is belt-and-braces so non-git workspaces and repos whose .gitignore misses these dirs never index .NET build artifacts at all.

**HOW:** TDD red-first; full workspace default suite green (31 test-result blocks). `bin/` deliberately NOT hard-excluded — many ecosystems keep real tracked source in bin/ and a hard exclude cannot be whitelisted back, unlike ignore files.

**IMPACT:** Rides the next julie-extract release train — no version bump or Miller pin change tonight (Miller's own .gitignore already covers these dirs on the dogfood machines).
