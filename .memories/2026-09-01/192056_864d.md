---
id: checkpoint_864d8394
timestamp: 2026-09-01T19:20:56.776Z
tags:
  - receiver-type-facts
  - wave-2
  - plan
  - review
git:
  branch: main
  commit: 54ad36bd
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Wave-2 plan revised after external review
briefId: bump-extraction-identity-epoch-to-8
type: checkpoint
next: Get plan approval, then execute in
  .claude/worktrees/receiver-type-facts-wave-2.
---

## Wave-2 plan revised after external review

**WHAT**
- Revised `docs/plans/2026-09-08-receiver-type-facts-wave-2.md` in the wave-2 worktree to address all six points from the GPT review (checkpoint 191014_60da).

**WHY**
- Review found that identifier-only edits cannot put `receiver_type` on pending rows, that the front-and-generic stripper cannot reduce trailing `*`/`&` or F# postfix generics, and that closeout allowed debt to roll forward.

**HOW**
- Task 1 now adds one base helper `record_declared_type_fact_with_declared(symbol_id, base_text, declared_text, rules, is_inferred)`; every language reduces the type node structurally (rust `base_type_name_node` pattern) and passes base and declared text separately. Task 3 migrates rust `record_type_node` to it.
- Every language task lists its pending-relationship module (kotlin/swift/gdscript/scala/cpp/zig/vbnet/powershell/fsharp/qml/r/ruby relationships.rs, dart pending_calls.rs, php call_relationships.rs, lua relationships.rs + core.rs). Razor is identifier-only by recorded exception.
- Primary-constructor rule: kotlin and scala `class_parameter` are `property` under the class, never parameter symbols. Python id wording now names `generate_id_for_node(name, function_definition_node)`. Java task lists classes.rs.
- Task 28 must close all 19 debts; a shortfall is a plan mismatch during the language task.
- Every same-file constructor task carries negative tests (unknown, imported/qualified, non-constructor call).

**IMPACT**
- Architecture risk drops to low for facts once Task 1 lands. Plan awaits approval; then razorback:subagent-driven-development in the wave-2 worktree.
