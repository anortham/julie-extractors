---
id: checkpoint_71523a83
timestamp: 2026-09-01T21:10:02.584Z
tags:
  - php
  - wave2
  - type-facts
git:
  branch: main
  commit: 54ad36bd
  worktree: /home/murphy/source/julie-extractors
  files:
    - crates/julie-extractors/src/erlang/definition_forms.rs
    - crates/julie-extractors/src/erlang/mod.rs
    - crates/julie-extractors/src/tests/erlang/mod.rs
actor:
  harness: omp-coding-agent
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: PHP receiver type facts (wave 2 task 19)
briefId: bump-extraction-identity-epoch-to-8
type: checkpoint
decision: Record inferred facts from syntax-stated new Type, including qualified
  unknown names, instead of the same-file constructor-only rule.
alternatives:
  - Same-file constructor only (global wave-2 rule). Rejected because the PHP
    task brief required Vendor\\Unknown from new \\Vendor\\Unknown().
evidence:
  - cargo xtask test language php passed
  - cargo test -p julie-extractors --lib tests::php::type_facts passed
symbols:
  - extract_parameter_symbols
  - extract_assignment
  - record_declared_type
  - php_call_receiver_type
confidence: 5
---

## PHP receiver type facts (wave 2 task 19)

### WHAT
Added PHP parameter symbols, local `new` inferred facts, typed property facts, and `$this`/`self`/`static`/`parent` `receiver_type` on identifier and pending rows. Class constant `STATUS` is in the basic fixture.

### WHY
Wave 2 task 19 required the three fact shapes plus class-constant golden evidence.

### HOW
- Structural skip for union/intersection types
- `?` and leading `\` stripped via TypeNameRules prefixes
- Syntax-stated `new Type` records inferred facts even for unknown/qualified names
- `$m = make()` still has a symbol and no fact

### IMPACT
`cargo xtask test language php` and `tests::php::type_facts` pass. Goldens regenerated for php.
