---
id: checkpoint_cb2e2a03
timestamp: 2026-09-01T23:14:25.272Z
tags:
  - release
  - v2.39.0
  - receiver-type-facts
  - wave-2
git:
  branch: main
  commit: a3ad76dc
  files:
    - README.md
    - docs/README.md
    - docs/release-evidence/2026-09-01-v2-39-0-release.md
    - docs/release-notes/v2.39.0.md
    - docs/release.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "v2.39.0 published: receiver-type facts waves 1 and 2"
briefId: bump-extraction-identity-epoch-to-8
type: checkpoint
next: Commit the closeout (evidence doc, pointers, .memories), push main, run
  scripts/check-release-state.sh and the runbook closeout checks.
---

## v2.39.0 published: receiver-type facts waves 1 and 2

**WHAT**: Merged `worktree-receiver-type-facts-wave-2` (with the review-fix commit `cfafec90`) fast-forward into `main`, prepared and published `v2.39.0` from `a3ad76dc`. Release Binaries run 33568778560 published all four targets at 2026-09-01T23:13:13Z.

**WHY**: The wave-2 review found and fixed the remaining defects; the user asked to merge locally and ship a release once green. Local `main` was also carrying 31 unpushed wave-1 commits (epoch 8 -> 9, `marker-razorback-v1`), so this release covers both waves.

**HOW**:
- Release prep commit `5a42f1da`: version 2.39.0, release note, `## 2.39.0` output-changes entry, ledger rows, strict clippy cleanup.
- Gates at `5a42f1da`: fmt, diff check, doc sync, xtask tests, clippy -D warnings, strict report 0/0, package-list, preflight, gitleaks, cargo audit, cargo deny, compat-check vs live v2.38.2 binary, default, contract (358 s), certification; Windows default suite on win-test 55 targets / 4373 passed.
- `real-world-release` failed in `erlang_corpus` (baseline predated parameter symbols; visibility read as text). `f438f365` updated the baseline and reads the column as an option; `a3ad76dc` fixed a clippy doc-list lint. Tier passes.
- CI run 33567346266 green on `a3ad76dc`; tag `v2.39.0` pushed; assets downloaded and hashed into `docs/release-evidence/2026-09-01-v2-39-0-release.md`; pointers advanced in `docs/release.md`, `README.md`, `docs/README.md`.

**IMPACT**: Every general-purpose language now emits parameter symbols, declared-type facts, and `receiver_type` metadata for Miller policy v6 Tier 3. Open follow-ups recorded in the wave-2 decision doc "Review outcomes": Lua `setmetatable` locals stay `class`, Elixir `%Foo{}` records without the same-file check, Ruby receiver rides `member_access` rows.
