---
id: checkpoint_64a105ab
timestamp: 2026-09-01T22:19:48.449Z
tags:
  - wave-2
  - receiver-type-facts
  - review
  - release-prep
git:
  branch: main
  commit: 54ad36bd
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Wave-2 receiver-type-facts review: findings fixed on the branch"
briefId: bump-extraction-identity-epoch-to-8
type: checkpoint
next: Commit fixes, prepare v2.39.0 release notes and output-changes entry, run
  release gates, merge ff into main, tag, push.
---

## Wave-2 receiver-type-facts review: findings fixed on the branch

**WHAT**: Reviewed the full wave-2 implementation (28 tasks, 26 languages) on `worktree-receiver-type-facts-wave-2` with six per-language review agents, then fixed every verified defect with seven fix workers plus lead edits.

**WHY**: The closeout claimed a green gate, but `git diff --check` failed, three languages had containing-symbol shifts (escalation triggers), and legacy `infer_types` paths leaked non-base-name values (`[Foo]`, `void Function()`, `*Worker`, `[]const`, `final`, `var`, `Unknown`) into artifacts that the basic-only evidence scan never saw.

**HOW** (fixed):
- Go multi-return `:=` symbols, top-level-only callee scan, generic receivers; Java `super` generic base name + anonymous-class `this`; C# indexer `type` field + golden evidence; Rust innermost impl.
- Swift/Kotlin/GDScript: super base name, legacy path reduces or skips, dead grammar arms, secondary-constructor test, local `const` kind rule, precomputed class sets.
- Dart: `local_function_declaration` symbols, structural type reduction, legacy keyword rows dropped, pre-pass type names, multi-declarator locals. Callable spans stay whole-declaration (documented ruling).
- C/C++/Zig: multi-word sized types record nothing, function-pointer params, `ns::Worker` -> `Worker`, no duplicate pending rows, legacy regex inference removed, slice types, nearest-symbol-ancestor rule.
- VB.NET qualified names + array suffixes; PowerShell constructor containing filter, `string[]`, legacy dedupe; F# type abbreviations; QML containing filter.
- Ruby/Elixir/Erlang/Razor/Bash/Lua/PHP/R/Scala cleanups; Task 27 fixture replacements restored as additions; R6 member symbols now span their own `name = value` argument (was the whole class call, so containing keys flipped on every line change).
- Legacy `@spec`/metadata inference in Elixir, Scala, Kotlin, Erlang reduces to base names.
- Decision doc: stale exception references removed; "Review outcomes" section records the rulings (Dart spans, Elixir `%Foo{}` without same-file check, Lua setmetatable stays class, VB arrays kept, Ruby member_access receiver).
- Evidence doc: rescan covers every fixture dir, query checks both ends of the value.

**IMPACT**: Branch gate green after fixes: fmt, `git diff --check main`, strict report (0/0), `cargo test --workspace` 4911 passed, capability + golden suites. Next: commit, release prep for v2.39.0, merge to main (harness blocks git on the primary checkout from this worktree session), tag, publish.
