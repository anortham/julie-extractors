---
id: checkpoint_0dde2b09
timestamp: 2026-09-01T18:56:42.813Z
tags:
  - receiver-type-facts
  - wave-2
  - plan
git:
  branch: main
  commit: 54ad36bd
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Wave-1 merged to main; wave-2 plan drafted
briefId: bump-extraction-identity-epoch-to-8
type: decision
decision: Wave 2 of receiver type facts covers 20 languages (18 debt entries
  plus scala and qml) and the five deferred wave-1 refinements, in one plan with
  28 tasks.
alternatives:
  - "Split wave 2 into typed and dynamic language plans: rejected because 19
    open_gaps entries name one plan path"
  - "Skip scala and qml: rejected by the user on 2026-09-01"
next: Get plan approval, then execute with subagent-driven-development in
  .claude/worktrees/receiver-type-facts-wave-2.
---

## Wave-1 merged to main; wave-2 plan drafted

**WHAT**
- Fast-forwarded main to 54ad36bd (wave 1, 30 commits). Removed the four merged agent worktrees and the feature worktree; pruned two dead Miller registry rows.
- Wrote `docs/plans/2026-09-08-receiver-type-facts-wave-2.md` in worktree `.claude/worktrees/receiver-type-facts-wave-2` (branch `worktree-receiver-type-facts-wave-2`, from local main). Not committed yet; awaiting plan approval.

**WHY**
- 19 `open_gaps` entries in capabilities.json name that exact plan path. The user asked for the plan and added scala and qml to the list.

**HOW**
- Five survey agents mapped every wave-2 extractor. Key discoveries baked into the plan:
  - kotlin, swift, gdscript already emit function locals under the wrong kind (`property`/`field`), parented correctly; scala emits local `var` as `variable` and local `val` as `constant`. Wave 2 retargets kinds instead of adding walkers.
  - dart emits no locals at all; vbnet emits no `Dim` locals.
  - powershell and bash already have parameter symbols but no `role` metadata; powershell skips class-method parameters.
  - fsharp hand-builds `TypeInfo` rows (always `is_inferred=false`) and its self identifier is not a keyword.
  - razor reimplements C# extraction; nothing from csharp wave 1 flows through.
  - php class constants already extract (golden lacks evidence); r class factories fail only on namespaced `R6::R6Class` in `call_name`.
  - swift `variable_declaration` arm and zig `const_declaration` check are dead code for the pinned grammars.
- Plan shape: Task 1 contract (decision doc with a per-language rules table, marker `.receiver-type-facts-v2`), Batch A wave-1 refinements (python re-parenting, rust/go/java receiver_type, go constructor calls, java bindings, csharp indexer), Batch B typed languages, Batch C dynamic languages plus razor, then evidence scan and closeout.

**IMPACT**
- Next: user reviews the plan; on approval run razorback:subagent-driven-development in the wave-2 worktree.
