---
id: checkpoint_4f091edf
timestamp: 2026-07-18T19:50:27.594Z
tags:
  - release-gates
  - golden-fixtures
  - ktor
git:
  branch: main
  commit: fadbb06e
  files:
    - fixtures/extraction/kotlin/ktor_routes/expected.json
summary: "Slow release gates run: caught and fixed a stale golden encoding a
  ktor false positive"
type: checkpoint
evidence:
  - "full slow suite: 3400 passed, 0 failed"
  - "git diff: 50 deletions in kotlin/ktor_routes/expected.json, both removed
    facts were client_request false positives"
---

## Slow release gates run: caught and fixed a stale golden encoding a ktor false positive

Ran the full feature-gated release suite (test-golden, test-capability-matrix, test-certification, test-downstream-smoke, test-real-world) — the gates the fast default suite deliberately skips.

- **WHAT**: One failure: `kotlin:ktor_routes` golden mismatch. The expected.json had TWO ktor http.client_request.v1 facts baked in from the pre-hardening collector: `map.get("/not-a-route")` (a literal Map lookup — a shipped false positive) and unproven bare `client.get("/elsewhere")`. The new ktor receiver proof correctly silences both.
- **HOW**: Regenerated with `UPDATE_GOLDEN=1 cargo test --features test-golden` — diff removes exactly those two facts (50 lines).
- **IMPACT**: Validates the receiver-proof work against the goldens; full slow suite now 3400 passed / 0 failed, strict data-quality report exit 0. This was the only slow-gate failure — release-blocking verification is now clean.
