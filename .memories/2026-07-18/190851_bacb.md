---
id: checkpoint_bacb891d
timestamp: 2026-07-18T19:08:51.574Z
tags:
  - http-clients
  - code-review
  - receiver-proofs
  - m2
git:
  branch: main
  commit: 04322be4
  files:
    - crates/julie-extractors/src/base/framework_structural_facts/http_clients/elixir.rs
    - crates/julie-extractors/src/base/framework_structural_facts/http_clients/kotlin.rs
    - crates/julie-extractors/src/base/framework_structural_facts/http_clients/mod.rs
    - crates/julie-extractors/src/base/framework_structural_facts/http_clients/php.rs
    - crates/julie-extractors/src/base/framework_structural_facts/http_clients/rust.rs
    - crates/julie-extractors/src/base/structural_fact_registry/framework/mod.rs
    - crates/julie-extractors/src/base/structural_fact_registry/tests.rs
    - crates/julie-extractors/src/tests/http_client/mod.rs
    - fixtures/extraction/capabilities.json
summary: "Round-2 review findings fixed: receiver proofs hardened across all
  four HTTP-client collectors"
type: checkpoint
evidence:
  - "cargo test --workspace: 3334 passed, 0 failed"
  - "cargo clippy --workspace --all-targets: 0 warnings"
  - "language-data-quality-report.mjs --strict: exit 0"
  - git show HEAD:rust.rs confirms options verb pre-existed (finding rejected)
next: Commit the fix set (including .memories/) and push once the user approves.
---

## Round-2 review findings fixed: receiver proofs hardened across all four HTTP-client collectors

The re-verification review (high effort) surfaced 10 more confirmed findings — deeper M2 false-positive holes mostly pre-dating the first fix wave. 8 fixed TDD-style (9 new tests), 1 resolved transitively, 1 rejected as factually wrong.

### WHAT
- **kotlin.rs**: Spring ctor proof anchors the exact type (`AcmeWebClient.create()` no longer proves); OkHttp dotted roots accept only `okhttp3.Request` (not `sdk.Request`); `class_parameter` kinds + last-token name matching prove constructor-injected Spring clients (`class Api(private val web: WebClient)` now emits — was a systematic miss); ktor gained a full receiver proof (`HttpClient`-typed param/class param, same-file `HttpClient(...)` local via `local_is_http_client`, or inline ctor) — `headers.get("Authorization")` no longer emits.
- **php.rs**: `scope_names_symfony_http_client` anchors the last namespace segment (AcmeHttpClient::create() rejected) at all three sites; Guzzle variable receivers now require proof (`ident_is_guzzle_client`: Client/ClientInterface typed param or same-scope `new Client()` assignment) — `$repository->get('users.cache.key')` no longer emits.
- **rust.rs**: bare `Client::new()`/`Client::builder()` ctor and bare `Client` param type are gated on a parser-backed `use reqwest...` declaration (`has_reqwest_use` threaded through the proof helpers) — an AWS SDK `Client::new()` in a file with a reqwest comment no longer emits.
- **capabilities.json**: re-added narrowed open_gaps entries `kotlin.http_client.instance_receiver`, `php.http_client.instance_receiver`, `elixir.http_client.tesla_middleware_base_url` recording the still-real residual silences.

### Decisions
- **Rejected finding "rust OPTIONS widening"**: `git show HEAD:...rust.rs` proves the deleted map already had `"options" => Some("OPTIONS")` — no behavior change; verifier evidence was wrong.
- **Kept PHP case-tolerant verb tokens**: PHP method names are case-insensitive, so `$client->GET()` on a PROVEN receiver is a real request; the false-positive risk was the missing receiver proof (now fixed), not the casing.

### IMPACT
Both review rounds' findings are closed: 27 + 9 fixed/documented, 1 rejected with evidence. Verified: workspace 3334 passed / 0 failed, clippy clean, strict data-quality report exit 0. Working tree has the uncommitted fix set on main [ahead 10]; commit + push await user approval.
