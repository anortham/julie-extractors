---
id: checkpoint_9edd60fe
timestamp: 2026-08-05T21:21:23.924Z
tags:
  - rebind
  - p2
  - post-review
  - codex
git:
  branch: rebind-verb
  commit: 58d2cb4
summary: "P2 post-review: Codex pre-merge finding fixed, branch ready to finish"
---

## P2 post-review: Codex pre-merge finding fixed, branch ready to finish

**Review outcome (Codex, adversarial, full diff d803e70..e79f32d):** verdict needs-attention, 1 finding (high, conf 0.99): check/use gap in `write_rebind` — validation on a dropped read-only connection, unconditional write phase, plus default `SQLITE_OPEN_CREATE` on the write open.

**Classification:** real-improvement (defense-in-depth for direct CLI callers; Miller's staging protocol already serializes) → FIXED in 58d2cb4: `check_validated_identity` inside the write transaction (refuses with new `artifact_changed` code, exit 1 recoverable, expected/found details; rollback via transaction drop) + `open_with_flags(READ_WRITE)` (no CREATE — vanished artifact = `db_open_failed`, no stray file). Reviewer's broader ask (scan/update/delete revalidation) classified out-of-scope: pre-existing caller-serialized concurrency model, untouched by this branch. Fix worker's red test corrected the finding's overstated damage claim (pre-fix left an empty stray file + misleading `db_write_failed`; no metadata rows were written).

**Verification at 58d2cb4:** unit tests 4/4 (both identity halves + no-CREATE + positive control, red-first proven), rebind_contract 10/10, rebind_equivalence 3/3, artifact crate 0 failures (21-entry ERROR_CODES vector holds), full CLI crate green, fmt+clippy clean, xtask default (31 suites) + contract re-run green.

**Branch state:** rebind-verb = 5 commits (9f6c9cd verb, e7bfa74 docs, fea3ce8 equivalence, e79f32d bookkeeping, 58d2cb4 review fix). NEXT: finishing-a-development-branch; release/tag/pin-bump stays user-gated.
