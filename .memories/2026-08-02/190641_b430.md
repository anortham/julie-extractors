---
id: checkpoint_b4309711
timestamp: 2026-08-02T19:06:41.628Z
tags:
  - scale-fixes
  - sdd-execution
  - phase-boundary
git:
  branch: scale-fixes
  commit: 9fb9703
summary: "Scale-fixes execution: plan committed, T1 landed, T2+T3 batch in flight"
next: Review T2+T3 reports on arrival; stage + commit per parallel-lead-commit
  contract; apply pool stack_size; dispatch T4/T5.
---

## Scale-fixes execution: plan committed, T1 landed, T2+T3 batch in flight

**WHAT:** Phase 1 of the scale-fixes plan (docs/plans/2026-08-02-scale-fixes-plan.md, branch
scale-fixes) is done: five-track root-cause investigation complete (all verified-in-code), plan
committed (62e9f89), T1 human-path error rendering landed (15134e5, inline review clean — status
word + generic diagnostic lines + counts; ReportCode::as_str/ALL; JSON byte-identity proven by
before/after sha256). T2 (recursion guards) and T3 (identity root fixes + trigger demotion to
first-write-wins) dispatched as a parallel-lead-commit batch with disjoint file ownership.

**Key facts for resume:** worktree ~/.config/razorback/worktrees/julie-extractors/scale-fixes;
builds need RUSTUP_TOOLCHAIN=1.97.1 (default stable is 1.94 < rust-version 1.95). Root causes:
identity conflict = containing-symbol pass disagreement (PowerShell one-line filter
identifiers.rs:590; C multi-declarator tie + HashMap/Vec order, creation_methods.rs:306), NOT hash
collision; stack overflow = unguarded blazor_navigation walkers + collect_stats vs
GitHub_10215.cs's 17,602-operator statement; spool bloat = dead code_context (47%) + JSON envelope
+ 3x import re-parse; write phase = live 54-index maintenance + in-timer resolution. Contract
decisions user-approved: drop identifiers.code_context; trigger → first-write-wins + warning.

**Carve-out reminder:** rayon pool .stack_size(16MiB) at commands.rs:1363 is LEAD work after T3
lands (commands.rs T3-owned during batch). T1 concern: append T3's new ReportCode variant to
ReportCode::ALL (fixed-size array).

**IMPACT/NEXT:** After batch: lead inline review both, stage owned files per commit-mode contract,
apply pool stack_size, then T4 (spool diet) → T5 (write throughput) → T6 scale validation
(dotnet/runtime @ a2f953fe266 + ~/.hermes/hermes-agent green at default stacks). One release
bundling with fleet-safety W4-W6 flags; release + pin bump approval-gated. Evidence dossiers in
session scratchpad track-*.md files; standing repro clone in scratchpad/dotnet-runtime.
