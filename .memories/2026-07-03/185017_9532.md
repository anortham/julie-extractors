---
id: checkpoint_9532b134
timestamp: 2026-07-03T18:50:17.770Z
tags:
  - review
  - code-review
  - structural-facts
  - http-boundary
  - web
  - tree-sitter
  - go
  - route-normalization
  - sourcemask
  - join-key
  - bug-fix
  - verification
  - julie-extractors
git:
  branch: main
  commit: e77a1ef
summary: Deep review of web/HTTP structural facts (v2.5.10→v2.7.0), verified
  fixes, completed the one residual
briefId: julie-extractors-backend-http-boundary-coverage-v2
type: learning
impact: All 25 web/HTTP structural-fact review findings resolved and verified
  (2642 tests green, fmt+clippy clean). The Go {$} anchor now normalizes
  correctly at any depth. Durable review doc with per-finding resolution map
  checked in.
evidence:
  - cargo test -p julie-extractors → 2642 passed, 0 failed
  - cargo fmt --all -- --check clean
  - cargo clippy -p julie-extractors --all-targets clean
  - standalone repro proved /items/{$} → /items/:$ before fix, /items/ after
  - commit e77a1ef on main
symbols:
  - normalize_braces_with_dots_template
  - collect_grouped_receivers
  - build_flags
  - collect_imports
  - is_alpine_attribute_name
next: "Push main to origin when ready (ahead by 2: dfc4bd2 + e77a1ef). Optional
  low-priority follow-up: regex heuristic in build_flags misses keyword-preceded
  regexes (return /.../, typeof, case)."
---

## Deep review of web/HTTP structural facts (v2.5.10→v2.7.0), verified fixes, completed the one residual

Ran a 12-dimension adversarial multi-agent review of both structural-fact surfaces (frontend `web_structural_facts/`, backend `framework_structural_facts/` + `http_clients/`, shared `http_boundary.rs`), targeting the post-fix tree (HEAD `2f445fa`) and explicitly excluding the 38 findings already closed by the 2026-07-02 fix lane.

- **Review result:** 35 candidates → 25 verified (0 critical, 5 high, 9 medium, 9 low), each with file:line + concrete failure scenario. Written up in `docs/reviews/2026-07-03-web-structural-facts-deep-review.md`.
- **Biggest risks found:** silent whole-file false-negatives — `SourceMask` (`scan.rs`) didn't model regex/char-rune/C# `@$"`/`"""` literals, so a stray quote flipped string-parity and dropped every later route/client fact; brittle import gates (Python multiline/black imports, Express `{ Router }`) zeroed a file's routes; plus a **Go group-receiver fixpoint infinite hang** (`go_http.rs` — name-keyed map with no monotonic termination).
- **Verified the user's fix commit `dfc4bd2`:** 24/25 findings correctly fixed with regression tests; 2640 tests pass, fmt+clippy clean. Adversarially spot-checked the Go hang (now monotonic None→Literal→Poisoned), Rails scope-stack balance, Python module-import wiring, and the new doc-drift conformance test.
- **Found + completed the 1 partial fix:** the Go 1.22 `{$}` anchor fix only handled root `/{$}`; proved via standalone repro that `/items/{$}` still normalized to `/items/:$`. Fixed `normalize_braces_with_dots_template` to strip `{$}` at any depth (`/items/{$}` → `/items/`), added a normalizer unit test + end-to-end emission test.

**Impact:** the web/HTTP join graph is now materially more trustworthy; all 25 review findings resolved. Committed as `e77a1ef` (not yet pushed; `main` is ahead of origin by 2).
