# Release Notes

Write release notes as `v{version}.md` before staging a release package.

Each file records the released `julie-extract` binary version, contract changes,
parser dependency changes, certification evidence, real-world evidence, and
checksums included in the package manifest when package assets are staged or
published.

Current notes:

- `v0.1.0.md`
