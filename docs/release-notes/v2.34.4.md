# julie-extract v2.34.4

v2.34.4 is a compatible patch candidate. v2.34.3 remains the published
release until the release workflow succeeds.

classification: compatible

## Windows and test-role closure

- Windows test hardening and path/report fixes improve cross-platform release
  confidence.
- Test-role closure records 21 supported capability cells and 7 source-backed N/A
  entries across C, C++, Rust, Zig, HTML, SQL, Markdown, JSON, TOML, YAML,
  and XML.

## Compatibility

Artifacts stay schema 7, JSONL stays contract v5, family stores stay store
schema 2, and store format epoch stays 1. Extraction identity epoch is 4.

Consumer action: replace the binary and re-extract or rebuild standalone
artifacts, or let epoch-4 file versions populate on the next family-store
import or update.
