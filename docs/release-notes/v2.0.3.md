# v2.0.3

This is a patch release for the v2.0.x standalone extraction product line.

## Fixes

- Bumped the Rust extractor crate `EXTRACTION_CONTRACT_VERSION` to
  `2026-06-03.ecmascript-swift-shape-v3` so downstream Rust consumers that
  compose that string into their own index version can detect the v2.0.x
  Swift, JavaScript, and TypeScript extraction-shape changes.
- Added a changed-path guard for golden expected-output fixtures. Any
  `fixtures/extraction/**/expected.json` change now requires
  `crates/julie-extractors/src/lib.rs` in the same changed-path gate so
  reviewers explicitly consider whether `EXTRACTION_CONTRACT_VERSION` must move.
- Fixed changed-path routing for xtask edits so `cargo xtask test changed`
  runs `cargo test -p xtask` when xtask source or tests change.

## Contract Changes

- Rust crate extraction contract string changes from
  `2026-05-29.bridge-anchors-v2` to
  `2026-06-03.ecmascript-swift-shape-v3`.
- CLI commands, exit codes, SQLite schema version, JSONL schema version, and
  artifact integer `extract_contract_version` remain unchanged.
- JSON report schema remains version 1.
- Release package contents are unchanged.

## Verification

- `cargo fmt --check`
- `cargo test -p xtask`
- `cargo xtask test changed Cargo.lock crates/julie-extract-artifact/Cargo.toml crates/julie-extract-cli/Cargo.toml crates/julie-extractors/Cargo.toml crates/julie-extractors/src/lib.rs crates/julie-extractors/tests/downstream_smoke.rs docs/release-notes/README.md docs/release-notes/v2.0.3.md xtask/src/test_tiers.rs xtask/tests/test_tiers.rs`
- `cargo xtask test contract`
- `cargo test -p julie-extractors --features test-downstream-smoke --test downstream_smoke julie_extractors_works_as_path_dependency_in_downstream_crate -- --exact`
- `cargo build --release -p julie-extract-cli --bin julie-extract`
- `cargo xtask release package --version 2.0.3 --target aarch64-apple-darwin --out-dir target/release-package/v2.0.3-aarch64-apple-darwin --binary target/release/julie-extract`
- `(cd target/release-package/v2.0.3-aarch64-apple-darwin && shasum -a 256 -c dist/aarch64-apple-darwin/julie-extract.sha256)`
- `target/release/julie-extract --version`
- `cargo clippy --no-deps -p julie-extract-artifact -p julie-extract-cli -p xtask --lib --bins -- -D warnings`
- `cargo test -p xtask --test test_tiers test_changed_expected_golden_output_requires_extractor_contract_review`
- GitHub CI run `26861514211` passed at commit
  `a9b38398760705ac413f9b84e864f08131aab440`.
- GitHub release workflow run `26861685095` published `v2.0.3`.
