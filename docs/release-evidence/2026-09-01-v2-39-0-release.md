# v2.39.0 release evidence, 2026-09-01

## Source

- Release commit and tag: `v2.39.0` = `a3ad76dc54571f250cb5d20802f8fac2a46e4851`
  on `main` (fast-forward of `worktree-receiver-type-facts-wave-2`), 87
  commits since `v2.38.2`: the 31 unpushed wave-1 commits that were already on
  local `main`, the wave-2 branch, the review-fix commit `cfafec90`, and the
  release preparation.
- Release Binaries run:
  [#33568778560](https://github.com/anortham/julie-extractors/actions/runs/33568778560):
  success; stable release published at 2026-09-01T23:13:13Z (not draft, not
  prerelease).
- CI run at the release commit:
  [#33567346266](https://github.com/anortham/julie-extractors/actions/runs/33567346266):
  Fast Gates, Extractor Compatibility, and the Windows Capacity Store Probe
  all passed.

## Gates at the release commit

All at `a3ad76dc` unless noted. `f438f365` and `a3ad76dc` change only the
Erlang real-world corpus test file (baseline counts and a doc comment), so
the extractor binary is identical to `5a42f1da`.

- Linux chain, exit 0 end to end at `5a42f1da`: `cargo fmt --all -- --check`;
  `git diff --check main`; `scripts/check-agent-doc-sync.sh`;
  `cargo test -p xtask`;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
  `node scripts/language-data-quality-report.mjs --strict` (silent_cells 0,
  quality_bar_debts 0); `cargo xtask release package-list`;
  `cargo xtask release preflight --version 2.39.0`;
  `gitleaks git --log-opts=v2.38.2..HEAD .` (no leaks); `cargo audit` (no
  vulnerabilities); `cargo deny --all-features check` (advisories, bans,
  licenses, sources ok); `cargo xtask compat-check` against the live
  v2.38.2 Linux binary (dumps differ, ledger entry `## 2.39.0` declares the
  change); `cargo xtask test default` (65 s); `cargo xtask test contract`
  (358 s); `cargo xtask test certification` (27 s).
- `cargo xtask test real-world-release` failed at `5a42f1da` in
  `erlang_corpus`: the committed per-file symbol baseline predated the new
  parameter symbols, and one query read the nullable `visibility` column as
  text. `f438f365` updates the baseline and reads the column as an option;
  the tier passes end to end at that commit. `a3ad76dc` re-proves
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`,
  `cargo fmt --all -- --check`, and `git diff --check main`.
- Branch gate before release prep at `cfafec90`: `cargo test --workspace`
  4911 passed, `cargo xtask test capability`, `cargo xtask test golden`,
  strict report 0/0.
- Windows default suite on the win-test guest at `5a42f1da`: exit 0, 55 test
  targets ok, 4373 tests passed, 0 failures (log
  `20260901T222754Z-julie-extractors-3629093.log`). The same suite passed at
  `cfafec90` (4373 passed).
- Wave-2 evidence scan (`docs/findings/2026-09-08-receiver-type-facts-wave-2-evidence.md`):
  0 corrupt `resolved_type` values across every fixture directory of the 21
  languages, parameter symbols in all 21, `receiver_type` on identifier and
  pending rows in every applicable language.

## Live assets (downloaded and hashed post-publication)

| Asset | Size (bytes) | SHA-256 |
| --- | --- | --- |
| `julie-extract-v2.39.0-aarch64-apple-darwin.tar.gz` | 14784677 | `84ff97474c64b6db9bbbd0b9e16fad386ec1afab12f3c583027ac21fc34c07bb` |
| `julie-extract-v2.39.0-x86_64-apple-darwin.tar.gz` | 14580827 | `7e1a3a7096fbf29a6037a3fc9d74c4139fcc38fa8d1cbfb76b77798173d2428b` |
| `julie-extract-v2.39.0-x86_64-pc-windows-msvc.zip` | 13686903 | `ab0b5892f8171ddba2cd666afa6a594190b0b48d6393b9ed5e7444e65454e14f` |
| `julie-extract-v2.39.0-x86_64-unknown-linux-gnu.tar.gz` | 14938042 | `3674e112db6afd19b6053efa9889bf9ff3f0bdad03033da2b1688675a729e2e7` |
