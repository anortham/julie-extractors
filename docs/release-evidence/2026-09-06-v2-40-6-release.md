# julie-extract 2.40.6 release verification

The user approved publishing this patch and then releasing Miller with the updated pin.
Publication is pending. This document records the verified candidate; live asset evidence
will be added after the release workflow completes.

## Scope

Behavioral source: `03cb3325406fb089ee1f529a9b04843e0e312dbe`.
The fix avoids rewriting an already-correct SQLite auto-vacuum setting during store writer
setup. Reader protection, FULL durability, checkpointing, schemas, parser behavior, and
the extraction-identity epoch are unchanged.

Preparation advances the three shipped crate versions and their lockfile entries to 2.40.6.
No external dependencies change. The xtask crate stays at 2.0.0.

## Verified gates

- Behavioral Linux default tier: 4,817 passed, 11 ignored, 128 seconds. Reused for version-only preparation.
- Behavioral Windows connection, coordinator, and reader-registration suites: exit 0 on
  the local NTFS guest. Native stdout was not captured; no per-test count is claimed.
- Versioned `cargo test -p xtask`: 119 passed.
- Versioned `cargo xtask test contract`: 376 passed, 3 ignored. Includes all-language store promotion.
- Strict artifact/CLI Clippy with all targets/features and `-D warnings`: passed.
- Formatting and diff whitespace checks: passed.
- Locked metadata: three shipped crates 2.40.6, xtask 2.0.0.
- Release preflight: four targets, 35 inputs.
- Locked Release build: passed; binary reports `julie-extract 2.40.6`.
- Linux package staged; embedded binary checksum and release-note bytes verified.

Local binary SHA-256: `e949e84ee57930558ae47b719cc4116de78a8d323b45d251db6d5d99164d6051`.
This is a local build hash, not a download pin. Raw candidate logs are in
`/tmp/julie-2406-release/`.

The measured performance evidence is in
[reader setup write regression](../evidence/2026-09-06-reader-pragma-cost.md).
