# Release Evidence

This directory records release-readiness evidence generated by repository gates.

Evidence files should name:

- commit under test
- exact command
- output paths
- hard gate result
- report-only metrics
- any tradeoffs or open decisions

Do not copy large generated artifacts into git. Keep SQLite, JSONL, and raw
reports under `target/`, and record the values needed to replay or audit the
gate.
