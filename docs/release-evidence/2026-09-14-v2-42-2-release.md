# julie-extract 2.42.2 release verification

Candidate prepared for v2.42.2 release under user authorization.

## Scope

The release contains the maintainability cleanup verified at `3a9f34e6` and recorded in `.memories/autonomous-run-2026-09-14-maintainability-cleanup.md`.

- Removed 828 lines of duplicated or retired code and tests.
- Removed the retired development-only reference-resolution report generator; historical artifacts remain preserved.
- No external dependency updates. The xtask crate remains 2.0.0.
- SQLite schema v7, JSONL v5, report schema v3, family-store schema v2, extraction contract, and identity epoch 10 are unchanged.

## Existing cleanup verification

The cleanup commit passed formatting, metadata, xtask, default, contract, data-quality, and focused Windows checks. Its full command results and compatibility evidence are recorded in the linked cleanup verification note.

## Release-version verification

- `cargo fmt --check`, `cargo metadata --no-deps --format-version 1`, `cargo deny --all-features check`, and agent-document sync: passed.
- `cargo clippy --workspace --all-targets --all-features --no-deps -- -D warnings`: passed (39.68s).
- `node scripts/language-data-quality-report.mjs --strict`: passed (`silent_cells=0`, `quality_bar_debts=0`).
- `cargo test -p xtask`: 119 passed.
- `cargo xtask test default`: 4,870 passed (121s wall clock).
- `cargo xtask test contract`: 381 passed (327.96s).
- `cargo xtask release preflight --version 2.42.2`: passed (4 targets, 35 inputs).
- The local binary reports `julie-extract 2.42.2`; SHA-256: `7fbcc4311e02c8cda3dd3b740aa12891a96dcb83afce07d3c6ecd4db1693cdf7`.
- `target/release-verification/v2.42.2/compat/current.dump.txt` and the downloaded v2.42.1 `previous.dump.txt` are byte-identical across 18 tables.
- The staged Linux package at `target/release-verification/v2.42.2/package-linux` passed its embedded checksum check.

## Windows evidence

The unchanged cleanup source was previously verified on Windows at `3a9f34e6`: 51 writer-contract tests and the two spool/force-scan tests passed. This evidence predates the version-only 2.42.2 manifest bump.

This candidate is not tagged, pushed, or published. Next: source CI, then the Release Binaries workflow.
