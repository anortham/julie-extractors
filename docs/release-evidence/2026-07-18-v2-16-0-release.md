# v2.16.0 Release Evidence — 2026-07-18

## Release identity

- Version: `2.16.0`
- Purpose: deferred HTTP client-request lane (Kotlin, PHP, Elixir, Rust
  clients), receiver-proof hardening across three review rounds, cURL
  emission-contract decision (ADR 0007)
- Release notes: `docs/release-notes/v2.16.0.md`
- Publish status: published
- Release URL: https://github.com/anortham/julie-extractors/releases/tag/v2.16.0
- Published: `2026-07-18T20:23:10Z`
- Release commit: `c8324f84`
- CI workflow: https://github.com/anortham/julie-extractors/actions/runs/29658920667
- Release workflow: https://github.com/anortham/julie-extractors/actions/runs/29659203820
- Assets: aarch64-apple-darwin, x86_64-apple-darwin, x86_64-pc-windows-msvc,
  x86_64-unknown-linux-gnu (`julie-extract` archives)

## Gates (all green at the release tree)

- `cargo fmt --all --check` — clean
- `cargo clippy --workspace --all-targets` — 0 warnings
- `cargo test -p xtask` — pass (exit 0)
- `cargo xtask test default` — pass (exit 0)
- `cargo xtask test contract` — pass (exit 0)
- `cargo xtask test golden` — pass (exit 0)
- `cargo xtask test capability` — pass (exit 0)
- `cargo xtask test certification` — pass (exit 0)
- `cargo xtask test real-world-smoke` — pass (exit 0)
- `cargo xtask dogfood repo` — exit 0, 1,664 files, `rescan_files_failed: 0`
- `node scripts/language-data-quality-report.mjs --strict` — `silent_cells: 0`,
  `quality_bar_debts: 0`, exit 0
- `cargo xtask release preflight --version 2.16.0` — ok: 4 targets, 23 inputs
- Full feature-gated workspace suite (`test-golden`, `test-capability-matrix`,
  `test-certification`, `test-downstream-smoke`, `test-real-world`) —
  3,400+ passed, 0 failed

## Adversarial review evidence

- Workflow-backed high-effort review round 1 (26 agents): 27 confirmed
  findings on the in-flight lane — all fixed or contract-recorded.
- Workflow-backed high-effort re-verification round 2 (25 agents): 10
  confirmed findings — 9 fixed/resolved, 1 rejected with git evidence (the
  claimed rust OPTIONS-verb widening did not exist).
- Codex 0.144.5 cross-model review (read-only, full source diff): 10
  findings — 5 fixed, 1 recorded as `elixir.http_client.alias_resolution`,
  2 declined with fixture evidence (curl_exec gating contradicts the shipped
  golden contract; see ADR 0007), 2 already mitigated by conflict rules.
- 31 new emission tests across the three rounds, each observed failing
  before its fix; the `test-golden` release gate additionally caught and
  corrected a stale `ktor_routes` golden encoding a `map.get(...)` false
  positive.

## Known limitations (recorded in release notes and capabilities.json)

- Struct-field / DI-provided / cross-file client receivers stay silent in all
  four languages (M2 instance-receiver open_gaps entries).
- Elixir module receivers match literal alias names (`alias ... , as:` is not
  resolved).
- Kotlin file gates remain content-needle based; emission is receiver-proof
  gated.
