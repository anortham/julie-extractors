# julie-extract 2.41.1 release-candidate verification

This candidate packages the agent-usefulness extraction repairs completed after
the published v2.41.0 tag. It is not published yet.

## Contract identity

- Artifact schema 7, JSONL contract 5, report schema 3, store schema 2.
- Extraction identity epoch 10, advanced from 9 because extraction output changes.
- Three shipped crates and the lockfile report 2.41.1; xtask remains 2.0.0.
- Release preflight found four targets and 35 package inputs.

## Verification

- Producer default tier: 4,853 passed, 11 ignored, no unresolved failures.
- Producer contract tier: 376 passed, 3 ignored, no unresolved failures.
- Post-gate Java repair: 108 focused Java tests and one canonical golden gate passed;
  a source-built Miller native selection test passed.
- Parser certification passed, including 39 capability tests, the pending-shape
  contract, and both parser-upgrade inventory tests.
- Real-world release profile passed for every registered corpus command.
- Strict language-quality report: 40 languages, zero silent cells, zero quality-bar debt.
- Release dogfood scanned 2,327 files and 361,371 symbols with schema 7 / JSONL 5;
  its no-change rescan reported all 2,327 files unchanged and zero failures.
- Locked Release build reports `julie-extract 2.41.1`; Linux binary SHA-256 is
  `ab34975238c037d75dc868f32127eefe6d8f7bf06d6b37a69a0b6b54bbe03342`.
- Linux package staging succeeded with the exact 35-input manifest.

The first Windows default run exposed three test-only portability assumptions:
one second epoch assertion still expected 9, and two tests constructed Unix
absolute paths on Windows. Production code did not fail. The tests now derive
absolute paths from a real temporary directory and both epoch assertions require
10. All three focused cases pass on Linux; the corrected commit is requalified
on the Windows guest before publication.

## Epoch upgrade proof

The published v2.41.0 Linux binary first imported an unchanged Go fixture at
epoch 9. The candidate then imported the identical content hash at epoch 10.
Both immutable file versions remain. The new return-position `Result`
identifier appears at bytes 52-58 only in epoch 10; the constructor occurrence
at bytes 68-74 exists in both. The family advanced to binary 2.41.1 and epoch 10
while preserving its existing `min_reader_version` of 2.41.0.
New families created by the candidate stamp `min_reader_version` 2.41.1;
downstream adoption therefore includes the matching reader-capability update.

Raw candidate logs and staged files are under
`/tmp/julie-2411-package.hQ00jG/`. Published archive digests must be recorded
only after GitHub produces the four immutable assets.
