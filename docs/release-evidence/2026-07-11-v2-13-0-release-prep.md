# v2.13.0 Release Preparation Evidence — 2026-07-11

## Candidate identity

- Version: `2.13.0`
- Purpose: certified Blazor and Razor extraction support
- Release notes: `docs/release-notes/v2.13.0.md`
- Publish status: prepared locally; not tagged, pushed, or published
- Razor grammar revision:
  `f82b737c77f5e3ef26bd655eda622b281479bbbc`
- The final grammar review commit is present only in the local grammar worktree;
  remote publication remains an approval boundary.

## Certification scope

- Five attribute-expression golden groups cover implicit expressions, explicit
  expressions, directive modifiers, directives/render fragments, and explicit
  render modes.
- Five integration fixture groups cover code-behind identity, imports,
  scoped-asset adjacency, constrained type parameters, render modes, and
  cascading parameters.
- Razor capability evidence certifies class and property symbols plus
  `razor.route_reference.v1`, `blazor.component_reference.v1`, and
  `http.client_request.v1`.
- The strict language-quality report records 36 languages,
  `silent_cells: 0`, and `quality_bar_debts: 0`.

## Live corpus evidence

A clean scan of `/Users/murphy/source/Terraform` processed 418 paths, extracted
388 supported files, reported 30 unsupported paths, and had zero failed files.
The fresh final-pin artifact is
`target/blazor-review-fixes/terraform-f82b737.sqlite`; it contains 28 Razor
files and zero Razor ERROR/MISSING diagnostics. SQL diagnostics are 283 errors
plus one missing node across six files; 225 errors are in `db/baseline.sql`.
The independent SQL quality work is tracked by
https://github.com/anortham/julie-extractors/issues/10.

## Approval boundary

The candidate is local-only. No commit was pushed, no tag was created, and no
release was published. The grammar revision must be available from the remote
before a portable release or downstream Miller pin can resolve it.
