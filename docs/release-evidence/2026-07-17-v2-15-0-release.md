# v2.15.0 Release Evidence — 2026-07-17

## Release identity

- Version: `2.15.0`
- Purpose: structural-fact expansion, Kotlin test-role completion, CLI
  oversized-file fix, contract hardening
- Release notes: `docs/release-notes/v2.15.0.md`
- Publish status: published
- Release URL: https://github.com/anortham/julie-extractors/releases/tag/v2.15.0
- Published: `2026-07-18T00:07:07Z`
- Release commit: `19dcc0629221c44bfffa7c77fb616b16786b1e1d`
- Feature commit: `469e77e6`
- CI workflow: https://github.com/anortham/julie-extractors/actions/runs/29621839703
- Release workflow: https://github.com/anortham/julie-extractors/actions/runs/29621840156
- Assets: aarch64-apple-darwin, x86_64-apple-darwin, x86_64-pc-windows-msvc,
  x86_64-unknown-linux-gnu (`julie-extract` archives + checksums)
- Registry: 194 structural-fact patterns, contract JSON regenerated

This evidence update was committed after publication as an evidence-only
follow-up. The tag and binary assets remain at the release commit above.

## Gates (all green at the release tree)

- `cargo fmt --all --check` — clean
- `cargo clippy --workspace --all-targets` — 0 warnings
- `cargo test -p xtask` — 60 passed
- `cargo xtask test default` — 3,226 passed, 0 failed (exit 0)
- `cargo xtask test contract` — 134 passed, 0 failed (exit 0)
- `cargo xtask test certification` — exit 0
- `cargo xtask test real-world-smoke` — exit 0 (re-run after final fix round)
- `cargo xtask dogfood repo` — exit 0, `rescan_files_failed: 0` (re-run after
  final fix round)
- `node scripts/language-data-quality-report.mjs --strict` — `silent_cells: 0`,
  `quality_bar_debts: 0`, exit 0
- Gated suites: golden fixtures pass; `structural_fact` with `test-golden`
  194 passed; capability matrix 39 passed
- CLI crates: `operations_contract` 45 passed; `report_contract` 8 passed

## Adversarial review evidence

- Multi-agent review (31 agents): 10 confirmed findings — all fixed.
  Lane reports under `.razorback/sdd/task-{a,b,c,d,e}-report.md`
  (git-ignored working scratch; summarized in `.memories/` checkpoints).
- Codex 0.144.5 cross-model review (read-only, full uncommitted RC diff):
  6 confirmed findings — all fixed (`task-f-report.md`). Codex explicitly
  found no defects in the HTML identity/landmark/style handling or the CLI
  oversized-row preservation in their final state.
- Bonus root-cause beyond the original findings: `scan` (not just `update`)
  deleted artifact rows for oversized files; both paths now preserve rows.

## Known limitations (recorded in release notes)

- Parenless multi-parameter T-SQL procedure name corruption (vendored grammar).
- Markdown pattern tables cover a curated ~79-row subset of 194 patterns;
  two razor/blazor reference patterns lack markdown rows.
- `reports.md` and frozen `sqlite-schema-v3.md` cite the pre-split registry
  path.
