# v2.11.0 Release Evidence — 2026-07-07

## Release Identity

- Version: `2.11.0`
- Purpose: release-audit closeout for `v2.10.0`
- Release notes: `docs/release-notes/v2.11.0.md`
- Local prep branch: `codex/prep-2.11.0`
- Publish status: prepared locally only; tag, push, and publication require
  explicit user approval.

## What Changed

- `deny.toml` allow-list now matches the actual pinned Razor parser fork:
  `https://github.com/anortham/tree-sitter-razor`.
- `Cargo.lock` updates `crossbeam-epoch` `0.9.18` -> `0.9.20` to resolve
  `RUSTSEC-2026-0204`.
- Release package manifest now includes `docs/contracts/sqlite-schema-v4.md`.
- `xtask/tests/release_contract.rs` covers both the schema-v4 package manifest
  requirement and git parser source allow-list drift.
- `README.md` and `v2.10.0` release docs were corrected from live GitHub release
  facts and downloaded-asset verification.
- Crate versions bumped `2.10.0` -> `2.11.0` across all three release crates and
  `Cargo.lock`.

## Verification

Local release-prep gate passed on `2026-07-07` from branch
`codex/prep-2.11.0`:

- `cargo fmt --all -- --check`
- `cargo metadata --format-version 1`
- `cargo deny --all-features check`
- `cargo clippy --workspace --all-targets --all-features --no-deps -- -D warnings`
- `scripts/check-agent-doc-sync.sh`
- `cargo test -p xtask`
- `cargo xtask test default`
- `cargo xtask test contract`
- `node scripts/language-data-quality-report.mjs --strict`
- `cargo build --release -p julie-extract-cli --bin julie-extract`
- `target/release/julie-extract --version` -> `julie-extract 2.11.0`
- `cargo xtask release package --version 2.11.0 --target aarch64-apple-darwin`
- `cargo xtask release preflight --version 2.11.0`
- `cargo xtask dogfood repo --root . --binary target/release/julie-extract`
- `cargo xtask test certification`

Release-package staging for `aarch64-apple-darwin` includes
`docs/contracts/sqlite-schema-v4.md` and `docs/release-notes/v2.11.0.md`.

Dogfood output from the locally built `2.11.0` binary:

- SQLite schema version: `4`
- Extract contract version: `3`
- JSONL schema version: `3`
- Files: `1,472`
- Symbols: `132,536`
- Identifiers: `307,002`
- JSONL records: `937,916`
- Scan duration: `17,085 ms`
- Rescan unchanged files: `1,472`
- Rows per second: `7,843.36`

## Publish Closeout Checklist

After explicit publish approval and successful `v2.11.0` Release Binaries run:

- Download all four assets.
- Compare archive SHA-256s to GitHub asset digests.
- Verify packaged checksum files.
- Run `julie-extract --version` from the downloaded host binary.
- Run downloaded-binary dogfood.
- Update `README.md`, this evidence file, and the live GitHub release body with
  the published `v2.11.0` asset facts.
