# julie-extract 3.6.1 release verification

## Publication

- Source CI [run 36041213489](https://github.com/anortham/julie-extractors/actions/runs/36041213489) passed on the release commit.
- Specialist Gates [run 36041244351](https://github.com/anortham/julie-extractors/actions/runs/36041244351),
  dispatched on the release commit with version `3.6.1`, passed parser
  certification, real-world smoke, real-world release, dogfood, and release
  package staging.
- Release Binaries [run 36072962389](https://github.com/anortham/julie-extractors/actions/runs/36072962389) passed.
- The [GitHub release](https://github.com/anortham/julie-extractors/releases/tag/v3.6.1)
  published at `2026-09-24T23:47:24Z` with six archives; it is neither a draft
  nor a prerelease.
- The annotated `v3.6.1` tag dereferences to
  `52b605fd79bf8d56c888316b38ee38674b8b1637`.

## Live asset verification

Every archive was downloaded independently. Its size and SHA-256 match the
GitHub API. The Linux x86_64 binary matches its embedded checksum and reports
`julie-extract 3.6.1`. Every archive includes the 3.6.1 release note.

| Target | Archive bytes | Archive SHA-256 |
| --- | ---: | --- |
| aarch64-apple-darwin | 15,408,386 | `d78bf18d2b9cf936e8f1d74503aa9e27c8f55c08b832fe2e22bc193466df28e1` |
| aarch64-pc-windows-msvc | 13,806,685 | `1af77436f6a1d156247489aba68e99685554996eaf3456481d34756e362f9d9f` |
| aarch64-unknown-linux-gnu | 15,289,877 | `3e2adb0e1dab8096eb02167fdf8085571d7053c4beced447991359494ec12bd8` |
| x86_64-apple-darwin | 15,223,649 | `a165eb84fca10665b5a9a0a10ed7c08c7e9c9f374156081d116009423f2e2819` |
| x86_64-pc-windows-msvc | 14,364,754 | `104f01ecfea2ed3213ad44ad5040b89ae302836ab186e8c8d62be49c2320b9b0` |
| x86_64-unknown-linux-gnu | 15,562,225 | `a3bf8b7fe8bf916fe0f4c635106eeee128292b0844e21af260b20ac4c3bd7132` |

## Release verification

- Locally on the release commit: `cargo fmt --check`, `cargo test -p xtask`,
  `scripts/check-agent-doc-sync.sh`, workspace Clippy with `-D warnings`, the
  default tier, the contract tier (187 passed), and
  `cargo xtask release preflight --version 3.6.1` (6 targets, 27 inputs)
  passed.
- `cargo xtask compat-check` against the published 3.6.0 binary: 18
  extraction tables byte-identical on the resolution fixture.
- Windows guest (`win-test`, NTFS clone at the release commit): the default
  tier passed 6,662 tests with 7 ignored.
- Flask scan with the release binary: every `self.x` property row in
  `src/flask/app.py` keeps the receiver (`self.cli = cli.AppGroup()`). The
  only Python `property` rows without `self.` are class-level `__slots__`.
- code-kb real-project corpus check (`scripts/release-corpus-check.py` in
  code-kb, 15 projects) against code-kb 2.2.0 with julie-extract 3.6.0: PASS.
  No scan failed, no symbol was lost, and scan time did not change. Call edges
  and typed receiver calls are identical. The only extractor-driven output
  change is the `self.` receiver in flask property signatures.
- This evidence is committed after publication. It is an evidence-only
  follow-up and does not alter the source commit or `v3.6.1` tag.
