# v2.37.2 release evidence, 2026-08-27

## Source

- Release commit and tag: `v2.37.2` = `4e119046d0ca0ffe997432ca4663f650903023a5` on `main` (2 commits since `v2.37.1`).
- Candidate CI run: [#33078708496](https://github.com/anortham/julie-extractors/actions/runs/33078708496): success.
- Release Binaries run: [#33078709579](https://github.com/anortham/julie-extractors/actions/runs/33078709579): success; stable release published at 2026-08-27T14:03:47Z (not draft, not prerelease).

## Gates at the release commit

All at `4e119046` unless noted:

- Linux chain, exit 0 end to end: `cargo fmt --check`; `cargo test -p xtask`;
  `cargo xtask test default`; `cargo xtask test contract`;
  `cargo xtask test certification`;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
  `node scripts/language-data-quality-report.mjs --strict`;
  `cargo xtask release package-list`; `scripts/check-agent-doc-sync.sh`;
  `git diff --check`.
- Certification ran because the release changes `Cargo.lock` and
  `crates/julie-extractors/Cargo.toml`, both certification-tier paths. Both
  changes are version-only.
- Windows default suite on the win-test guest at `4e119046`: exit 0, 55 test
  targets ok, 0 failures.
- `cargo xtask release preflight --version 2.37.2`: ok, 4 targets, 35 inputs.
- Security: `gitleaks git --log-opts="v2.37.1..HEAD" .` scanned 3 commits and
  found no leaks; `cargo audit` found no vulnerabilities in 173 dependencies;
  `cargo deny --all-features check` passed advisories, bans, licenses,
  sources ok.
- Real-world tiers were skipped on purpose: the release changes the store
  request path only and touches no extractor body.

## Live assets (downloaded and hashed post-publication)

| Asset | Size (bytes) | SHA-256 |
| --- | --- | --- |
| `julie-extract-v2.37.2-aarch64-apple-darwin.tar.gz` | 13258057 | `bf837cf8a4e66c7234aa9a4a1f7859dc1f92d57cbea4f368f381bd860ec74b9c` |
| `julie-extract-v2.37.2-x86_64-apple-darwin.tar.gz` | 13151812 | `509d569a4673d577adb5eae1f10de074a23e763b00a840db577667b25439ab95` |
| `julie-extract-v2.37.2-x86_64-pc-windows-msvc.zip` | 12339623 | `ab420153ab0307af48308664da1986048971b49f0a7ff16b91d5dc0f136db1d0` |
| `julie-extract-v2.37.2-x86_64-unknown-linux-gnu.tar.gz` | 13488433 | `50d6c70834ef30e399492261da09372cd5770d03ef77acf30f9a7b2bec217105` |

Downloaded-binary verification (Linux archive): the embedded
`julie-extract.sha256` digest
`2ae7df95869cfd0d558de1ffdcc90546bb313a30aa1c0db327cde195b378f617` matches the
extracted binary, and the binary reports `julie-extract 2.37.2`.

Feature smoke with the downloaded Linux binary: after a two-file `store
import`, deleting `util.rs` from disk and running `store update --file
util.rs` exits 0 and reports `operation: update`, `state: committed`,
`manifest.disposition: created`, generation 2. The serving manifest then
holds only `lib.rs` with status `indexed`.

## Post-tag follow-ups on `main`

The closeout commit carrying this evidence and the published-release pointers
is an approved evidence/docs-only follow-up, not part of the v2.37.2 source
tree.
