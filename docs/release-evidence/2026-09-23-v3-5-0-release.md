# julie-extract 3.5.0 release verification

## Publication

- Source CI [run 35861252269](https://github.com/anortham/julie-extractors/actions/runs/35861252269) passed.
- Specialist Gates [run 35861286537](https://github.com/anortham/julie-extractors/actions/runs/35861286537),
  dispatched on the release commit with version `3.5.0`, passed parser
  certification, real-world smoke, real-world release, dogfood, and release
  package staging.
- Release Binaries [run 35862810041](https://github.com/anortham/julie-extractors/actions/runs/35862810041) passed.
- The [GitHub release](https://github.com/anortham/julie-extractors/releases/tag/v3.5.0)
  published at `2026-09-23T13:05:33Z` with six archives; it is neither a draft
  nor a prerelease.
- The annotated `v3.5.0` tag dereferences to
  `0f178f72452ebf007474e8b0abfa8e57d1bbe02e`.

## Live asset verification

Every archive was downloaded independently. Its size and SHA-256 match the
GitHub API, and every packaged binary SHA-256 matches its embedded checksum.

| Target | Archive bytes | Archive SHA-256 |
| --- | ---: | --- |
| aarch64-apple-darwin | 14,924,774 | `d2cd48ea973fea58d2e091a19e9ed2141ed6160a497314f107b7134b9acdd0bd` |
| aarch64-pc-windows-msvc | 13,360,608 | `88e89d808ba3d161bbd8ebb8c049b05181d02424fd9d49f53520a6cd089aa216` |
| aarch64-unknown-linux-gnu | 14,765,481 | `568be97a46d79203dc7840d9defc585144ec48351eada4811f71b2f85bb1f612` |
| x86_64-apple-darwin | 14,696,024 | `a1287296ffa17c5c71c39bf1f138360c4fe14cb8a86dfbb0e36259c530b3a3d5` |
| x86_64-pc-windows-msvc | 13,849,917 | `f416711033ea8b9ce0a59d22ced96db334704a1d9a3a68e995daf7c56c32fe59` |
| x86_64-unknown-linux-gnu | 15,047,073 | `121ab9dc9be8095fc9f9ddea2fc51ea27fd5eb73e1e1b4c9840861a6ea08ee38` |

The published Linux x86_64 binary reports `julie-extract 3.5.0` and lists 42
languages, `gomod` and `gosum` among them. A scan of
`fixtures/extraction/resolution_contract` returns `ok` with 51 files, 0 failed
files, SQLite schema 7, and extract contract 4. A scan of the `gomod/basic` and
`gosum/basic` fixture files writes 12 `gomod` symbols, 26 `gomod` facts, and 11
`gosum` facts with 0 failed files. Every archive includes the 3.5.0 release
note and release documentation.

## Release verification

- CI Fast Gates on the release commit passed 5,780 tests with 7 ignored
  (xtask, default, and contract tiers), plus workspace Clippy on Rust 1.98.1
  and the strict language report (`silent_cells=0`, `quality_bar_debts=0`).
- Locally the contract tier passed 187 tests, `cargo xtask release preflight
  --version 3.5.0` passed (6 targets, 27 inputs), and `cargo xtask
  compat-check` against the published 3.4.0 binary accepted the `3.5.0`
  compatible ledger entry.
- Native Windows default tier passed on `01028667` (5,465 tests, 7 ignored,
  100 s). The two later commits change only docs and the Erlang corpus
  baseline, which runs in the real-world release tier, not the default tier.
- The nightly real-world release gate after 3.4.0 failed on that Erlang
  baseline: the 3.4.0 clause-run fix adds two parameter rows to
  `unicode_util_compat.erl` on purpose (206 to 208). `0f178f72` records the new
  baseline, and the dispatched gate above passes on it. The 3.5.0 release ran
  the real-world release tier before the tag.
- This evidence is committed after publication. It is an evidence-only
  follow-up and does not alter the source commit or `v3.5.0` tag.
