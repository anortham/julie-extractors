# v2.23.0 Release Evidence — 2026-08-03

Published from `origin/main` @ `09791f7` (tag `v2.23.0` on the same commit).
Release Binaries workflow run 30818109939, tag-push trigger, all four targets
green; release is not a draft and not a prerelease, and its body is
`docs/release-notes/v2.23.0.md` (6,903 chars, set by the workflow's
release-notes step rather than left as a placeholder). The downloaded
aarch64 binary reports `julie-extract 2.23.0`.

## Published assets (verified live, SHA-256 of downloaded archives)

| Asset | SHA-256 |
| --- | --- |
| `julie-extract-v2.23.0-aarch64-apple-darwin.tar.gz` | `b2fc63e3a02458406cae82228e6eca5e0b828529162599b7f4afea1b00e80769` |
| `julie-extract-v2.23.0-x86_64-apple-darwin.tar.gz` | `597bb3773dd925a86620622ddd87ac6257af5d2fb64f077105a939078b94fd61` |
| `julie-extract-v2.23.0-x86_64-pc-windows-msvc.zip` | `108c457ecca0018a0135c55873f5447b43d132a5f4f8612c7a84d5dfb2f8ccf4` |
| `julie-extract-v2.23.0-x86_64-unknown-linux-gnu.tar.gz` | `6900e64e960f17aa6ddbf91abdb16142a04f90bfc4bc12f3d5fccbb657911df8` |

## What shipped

The scale-fixes branch (plan `docs/plans/2026-08-02-scale-fixes-plan.md`,
tasks T1–T7): recursion guards with `depth_truncated` diagnostics and an
explicit 16 MiB rayon worker stack; reference-site identity root fixes with
the fatal import abort demoted to a recoverable first-write-wins warning
(`reference_site_payload_conflict`); the spool diet (`identifier.code_context`
always `null`, length-prefixed binary frames, streaming import — 7.3x smaller
on dotnet/runtime); the fresh-artifact bulk-load write path with
`foreign_keys=OFF` plus a whole-DB `PRAGMA foreign_key_check` before COMMIT;
additive `artifact_write_*` profile keys; and per-error human report output.

## Scale gate

dotnet/runtime @ `a2f953fe266` (58,366 files): 2.21.0 aborts with zero rows;
this release writes a complete 22.84 GiB artifact — 24 tables, 53,479,352 rows
content-verified against the pre-fix branch baseline, `file_symbol_insert`
down 99.6%, CPU down 71%, peak RSS 20.97 GiB (from 30.6). hermes-agent scans
clean with zero conflicts. Known residuals (documented in the notes): 4,237
C-only recoverable identity conflicts, resolver throughput decay at the top
end, ~5.5 min `foreign_key_check` at 22.84 GiB.

## Gate evidence at the released tree

All branch gates exit 0 on rustc **1.97.1**: fmt, `cargo test -p xtask`,
`cargo xtask test default`, `cargo xtask test contract` (3,800 workspace
tests, clippy clean on the merged branch). Dogfood
`cargo xtask dogfood repo` exits 0 (1,804 files, 0 failed). Release preflight
`cargo xtask release preflight --version 2.23.0` passed (4 targets, 27
inputs). Full measurement record:
`docs/findings/2026-08-02-scale-fixes-validation.md`.

## Source-control closeout

`main` == `origin/main` @ `09791f7` with tag `v2.23.0` on that commit; the
scale-fixes worktree merged fast-forward (745ea2a) after a rebase onto
v2.22.0's `eb629f0`, so both this branch and the independently released
fleet-safety flags coexist in the released tree.
