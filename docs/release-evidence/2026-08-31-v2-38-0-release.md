# v2.38.0 release evidence, 2026-08-31

## Source

- Release commit and tag: `v2.38.0` = `bab2db1b` (merge of
  `codex/extractor-gap-closure-plans` at `87c1fbce767da33bd64fbf619ef3a7e9ed68366b`)
  on `main`, 30 commits since `v2.37.2`.
- Release Binaries run:
  [#33402796456](https://github.com/anortham/julie-extractors/actions/runs/33402796456):
  success; stable release published at 2026-08-31T14:45:49Z (not draft, not
  prerelease).
- Candidate CI run
  [#33402797143](https://github.com/anortham/julie-extractors/actions/runs/33402797143)
  at the release commit: Fast Gates and the Windows Capacity Store Probe
  passed; the Extractor Compatibility job failed because
  `docs/contracts/extraction-output-changes.md` had no `## 2.38.0` entry for
  the declared output changes. The entry was added in the closeout commit and
  the compat gate was re-proven there against the v2.37.2 binary.

## Gates at the release commit

All at `bab2db1b` unless noted:

- Linux chain, exit 0 end to end: `cargo fmt --check`; `cargo test -p xtask`;
  `cargo xtask test default`; `cargo xtask test contract`;
  `cargo xtask test capability`; `cargo xtask test golden`;
  `cargo xtask test certification`; `cargo xtask test real-world-release`;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
  `node scripts/language-data-quality-report.mjs --strict` (silent_cells 0,
  quality_bar_debts 0, open_gap_backlog 47);
  `cargo xtask release package-list`; `scripts/check-agent-doc-sync.sh`;
  `git diff --check`. 4957 tests passed, 0 failed across the chain.
- Real-world tiers ran because this release changes extractor bodies (F#,
  rust doc-test facts, C# visibility, Go subtests).
- Windows default suite on the win-test guest at `87c1fbce` (the merge
  parent; the merge added only `.memories/` files, so the code is identical):
  exit 0, 55 test targets ok, 4372 tests passed, 0 failures.
- `cargo xtask release preflight --version 2.38.0`: ok, 4 targets, 35 inputs.
- Security: `gitleaks git --log-opts="v2.37.2..HEAD" .` scanned 32 commits
  and found no leaks; `cargo audit` found no vulnerabilities in 174
  dependencies; `cargo deny --all-features check` passed advisories, bans,
  licenses, sources ok.

## Live assets (downloaded and hashed post-publication)

| Asset | Size (bytes) | SHA-256 |
| --- | --- | --- |
| `julie-extract-v2.38.0-aarch64-apple-darwin.tar.gz` | 14609214 | `50b47f0376bfb4bcb14976a0d31229c2a1fbad255bb83d60f913e825a3048a18` |
| `julie-extract-v2.38.0-x86_64-apple-darwin.tar.gz` | 14383784 | `063e482e439d42ede2decaa1cfade871138da4e4b72cccaa12f316650cabf6c4` |
| `julie-extract-v2.38.0-x86_64-pc-windows-msvc.zip` | 13536951 | `c46413e34c6e7c8483a489bc40c589c2009a0db96971996d52412c4f0d72532c` |
| `julie-extract-v2.38.0-x86_64-unknown-linux-gnu.tar.gz` | 14756951 | `32f984a2200505ecc7200cf41cc92a640151124b0e4f67a198506db4f65a46d9` |
