# v2.37.1 release evidence, 2026-08-26

## Source

- Release commit and tag: `v2.37.1` = `1bb2956d30077e22657858dc7febe1fc3e5675bf` on `main` (4 commits since `v2.37.0`).
- Candidate CI run: [#33005015336](https://github.com/anortham/julie-extractors/actions/runs/33005015336): success, including the `Extractor Compatibility` job against the published v2.37.0 Linux binary.
- Release Binaries run: [#33006580572](https://github.com/anortham/julie-extractors/actions/runs/33006580572): success; stable release published at 2026-08-26T19:57:44Z (not draft, not prerelease).

## Gates at the release commit

All at `1bb2956d` unless noted:

- Linux chain, exit 0 end to end: `cargo fmt --check`; `cargo test -p xtask`;
  `cargo xtask test default`; `cargo xtask test contract`;
  `cargo xtask test certification`;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
  `node scripts/language-data-quality-report.mjs --strict`;
  `cargo xtask release package-list`; `scripts/check-agent-doc-sync.sh`;
  `git diff --check`.
- Certification ran because the release changes
  `crates/julie-extractors/src/language_spec/specs.rs`, a certification-tier
  path.
- Windows default suite on the win-test guest at `1bb2956d`: exit 0, 54 test
  targets ok, 0 failures.
- `cargo xtask release preflight --version 2.37.1`: ok, 4 targets, 35 inputs.
- Extraction identity epoch advanced 6 → 7 with a classified
  `## 2.37.1` ledger entry in `docs/contracts/extraction-output-changes.md`.
  The compat fixture holds none of the new extensions, so the CI
  `Extractor Compatibility` job byte-matched v2.37.0; the ledger entry
  declares the change on the strength of the spec diff.
- Security: `gitleaks git v2.37.0..HEAD` scanned 4 commits and found no
  leaks; `cargo audit` found no vulnerabilities in 173 dependencies;
  `cargo deny --all-features check` passed advisories, bans, licenses,
  sources ok.
- Real-world tiers were skipped on purpose: the release adds extension
  claims that route through the existing XML extractor and changes no
  extractor body.

## Live assets (downloaded and hashed post-publication)

| Asset | Size (bytes) | SHA-256 |
| --- | --- | --- |
| `julie-extract-v2.37.1-aarch64-apple-darwin.tar.gz` | 13258421 | `37748ece76649e38d7e20c1c387a10529616ca86fa9ce5d126c69ec3a41a3a68` |
| `julie-extract-v2.37.1-x86_64-apple-darwin.tar.gz` | 13142518 | `a6e40df5ed2606262bf87dd7b91eedf788bb106cc0d7f3f21517b7157d182ad1` |
| `julie-extract-v2.37.1-x86_64-pc-windows-msvc.zip` | 12327229 | `f20399d75cafb00fe0ebf075eb4cdac1fcd35728357d042eafcd11b396a4c10a` |
| `julie-extract-v2.37.1-x86_64-unknown-linux-gnu.tar.gz` | 13478756 | `acbd9f98b7dff2550bf75441bc63f70d6d39cff6657561ca915a07557bc2ece0` |

Downloaded-binary verification (Linux archive): the embedded
`julie-extract.sha256` digest
`69bb71349b426585c4712e1c3f1049248938c1a3265b297ae39398a7bb374a39` matches the
extracted binary, and the binary reports `julie-extract 2.37.1`.

Feature smoke with the downloaded Linux binary: a `scan` over a directory
holding one `App.csproj` indexes the file as `language = xml`,
`status = indexed`, with structural-fact and source-region rows, and its
output row counts equal a scan of the same bytes named `App.xml`.

## Post-tag follow-ups on `main`

The closeout commit carrying this evidence and the published-release pointers
is an approved evidence/docs-only follow-up, not part of the v2.37.1 source
tree.
