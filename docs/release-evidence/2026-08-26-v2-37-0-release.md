# v2.37.0 release evidence, 2026-08-26

## Source

- Release commit and tag: `v2.37.0` = `ca95b335db1a3e3697c76d72d3a5248372c5f215` on `main` (16 commits since `v2.36.0`).
- Candidate CI run: [#32913537352](https://github.com/anortham/julie-extractors/actions/runs/32913537352): success.
- Release Binaries run: [#32913538703](https://github.com/anortham/julie-extractors/actions/runs/32913538703): success; stable release published at 2026-08-26T00:19:46Z (not draft, not prerelease).

## Gates at the release commit

All at `ca95b335` unless noted:

- Linux chain, exit 0 end to end: `cargo fmt --check`; `cargo test -p xtask`;
  `cargo xtask test default`; `cargo xtask test contract`;
  `cargo xtask test certification`;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
  `node scripts/language-data-quality-report.mjs --strict`;
  `cargo xtask release package-list`; `scripts/check-agent-doc-sync.sh`;
  `git diff --check`.
- Real-world tiers were skipped on purpose. The release changes the store
  queue and reports only; extraction output is unchanged, and `compat-check`
  proves it below. Certification ran because `Cargo.lock` changed with the
  version bump.
- Windows default suite on the win-test guest at `ca95b335`: exit 0, 54 test
  targets ok, 0 failures.
- `cargo xtask release preflight --version 2.37.0`: ok, 4 targets, 35 inputs.
- `cargo xtask compat-check --previous-binary <published v2.36.0 linux binary>`:
  exit 0; 18 extraction tables byte-identical between 2.36.0 and 2.37.0, so
  the extraction identity epoch stays 6 with no new ledger entry.
- Security: `gitleaks git v2.36.0..HEAD` found no leaks; `cargo audit` found no
  vulnerabilities; `cargo deny --all-features check` passed advisories, bans,
  licenses, sources ok.
- Review: every task in the store-queue-hygiene change set got a lead inline
  review with two fix rounds, recorded in
  `.razorback/sdd/2026-08-25-store-queue-hygiene/progress.md`.

## Live assets (downloaded and hashed post-publication)

| Asset | Size (bytes) | SHA-256 |
| --- | --- | --- |
| `julie-extract-v2.37.0-aarch64-apple-darwin.tar.gz` | 13256568 | `815873e1def836746f01377ec64ac4b978a56a931f209cf58bd68a32166a92dc` |
| `julie-extract-v2.37.0-x86_64-apple-darwin.tar.gz` | 13147184 | `22de3d2650673e1e0d7fa1bece31b1cda2dd9a381b3b2ab3bf2c2af48c539278` |
| `julie-extract-v2.37.0-x86_64-pc-windows-msvc.zip` | 12330752 | `67233590f69b4d1fe50a401efe40ef697dd45419942fd900186a26d86e986bfb` |
| `julie-extract-v2.37.0-x86_64-unknown-linux-gnu.tar.gz` | 13480920 | `c1ed85445babcd832cc559a8ae244eb8a293e51585cb3c3902979d7cfd531bf5` |

Downloaded-binary verification (Linux archive): the embedded
`julie-extract.sha256` digest
`106d633eb5b8552562bd860cd2b04ac81d3887c03fc1d83a6397a8632fc84e59` matches the
extracted binary, and the binary reports `julie-extract 2.37.0`.

## Post-tag follow-ups on `main`

The closeout commit carrying this evidence and the published-release pointers
is an approved evidence/docs-only follow-up, not part of the v2.37.0 source
tree.
