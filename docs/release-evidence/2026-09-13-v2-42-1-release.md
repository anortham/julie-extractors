# julie-extract 2.42.1 release verification

Candidate prepared for v2.42.1 release under user authorization.

## Scope

Behavioral commits:
- `7a15a069`: Add `.code-kb`, `.worktrees`, `worktrees`, `.idea`, and `.vscode` to `HARD_EXCLUDE_DIRS`.
- `0ef89729`: Add `.claude`, `.venv`, `venv`, `.env`, `.tox`, and `.vs` to `HARD_EXCLUDE_DIRS`.

Preparation advances the three shipped crate versions and lockfile entries to 2.42.1.
No external dependencies change. The xtask crate stays at 2.0.0.
Extraction output, schemas (SQLite schema v7, JSONL v5), and the extraction identity epoch (10) are unchanged.

## Verified gates

- Strict Clippy (`cargo clippy --workspace --all-targets --all-features -- -D warnings`): passed (exit 0 in 25.36s).
- Code formatting and diff checks (`cargo fmt --check`, `git diff --check`): passed.
- Xtask tests (`cargo test -p xtask`): 119 passed, 0 failed.
- Default tier (`cargo xtask test default`): passed (123s wall clock).
- Contract tier (`cargo xtask test contract`): passed (includes all-language store promotion, crash contracts, and equivalence).
- Release preflight (`cargo xtask release preflight --version 2.42.1`): 4 targets, 35 inputs ok.
- Release package staging: verified on `x86_64-unknown-linux-gnu` with all 35 manifest items and SHA-256 checksum generation.
- Local release binary reports `julie-extract 2.42.1`.
- Local binary SHA-256: `617a639d86274781e9501c611af86b0fd6c1750f63966974024831980cbd1dd2`.
